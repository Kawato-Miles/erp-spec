# 工單建立材料選擇深度與計價方式成本計算——設計方案

- 日期：2026-08-27
- 分級：局部變動（母版規格列為後端既存結構、wiki 補正名；新增欄位限生產任務一個實體、單一領域生產執行；不新增實體、不動狀態機）
- 前置：erp-planning-pre-check 已跑（生產執行領域，量化矩陣 11/12/8）；grill 八題拍板完成（見必含段④）
- as-is 事實來源：sens-print-core（材料主檔資料模型）、sensation-api `product/pricing/`（計價引擎 M1-M5）、erp repo prototype（工單建立現況）

---

## 本體

### 一、問題

工單建立（製程規劃）時，材料型生產任務只能選到「材料規格」層。重量計價材料的單價參數在更下一層的**母版規格列**（[[BOM結構]]「材料重量計價尺寸表的每一列」，如同一規格下的 A0 列與 A1 列）；面積計價材料的單價要以**面積數值**查價格矩陣。這兩個輸入現在都選不到、填不了，材料費算不出正確值。

### 二、實體與欄位變動

唯一動欄位的實體是 [[生產任務]]（材料型），新增兩欄（依所引用材料的計價方式大類擇一顯示）：

| 介面欄位 | 顯示條件 | 說明 | 欄位形態 | 來源 | 可否修改 |
|---------|---------|------|---------|------|---------|
| 母版規格列 | 材料計價方式大類＝按重量 | 所選材料規格的重量計價尺寸表中選一列（列上帶尺寸、克重、噸價／磅價／表列單價、每台頁數），單價據此換算；必填 | 單選（所選材料規格的尺寸表列）| 印務於製程規劃時選定 | 製程確認階段可改；製程定案後改選走工單異動 |
| 計價面積 | 材料計價方式大類＝按面積 | 單件用料面積（含出血等實際用料，非印件成品尺寸），供查價格矩陣與計算材料費；必填。**單位由所選材料規格價格矩陣的區間單位帶出，任務端不自選**（防單位錯配造成金額級距錯誤） | 數字（單位唯讀帶出）| 印務於製程規劃時填寫 | 同上 |

裝訂型生產任務同步新增兩欄（解 [[BOM結構]] B1-B3 裝訂計價在工單上算不出的既存缺陷，裁決 10、12）：

| 介面欄位 | 顯示條件 | 說明 | 欄位形態 | 來源 | 可否修改 |
|---------|---------|------|---------|------|---------|
| 台數 | 所選裝訂項目計價方法＝台數計價（B1） | 印務算好台數填入，系統據此查台數×本數區間表算裝訂費；必填。MES 側 B1 的台數輸入自此欄取得，不再自母版規格列換算，「母版列未設每台頁數時擋下」的條件限 EC 側（MODIFIED，EC 側維持引擎自算） | 數字 | 印務於製程規劃時填寫 | 製程確認階段可改；定案後走工單異動 |
| 頁數 | 所選裝訂項目計價方法＝頁數計價（B2）或本數計價（B3） | 每本頁數，系統據此查表算裝訂費（B2 落頁數×本數區間；B3 精確比對頁數項目）；必填。查無對應（含 B3 比對不到頁數項目）時分項顯示 0，沿既有慣例 | 數字 | 印務於製程規劃時填寫 | 同上 |

裝訂三法的本數不另設欄：裝訂型任務以本計，**本數＝任務目標數量**；每本最低金額與總價最低金額由 [[裝訂主檔]] 帶出，系統查表自動套（既有公式，正本 [[BOM結構]] B1-B3）。

- 按數量計價材料不新增欄位：級距由任務目標數量唯一決定，無可挑餘地（拍板 3）。
- 新增各欄納入生產任務**必填驗證**：任務儲存時檢查，缺任一必填即無法儲存，不設特例提示、不加送審特檢（拍板 7）。此擋存機制與時點（儲存時檢查）現行正本未明文，落卡時明文化入 [[生產任務]] 卡（標新增把關，依據拍板 7）。
- 列參數凍結：比照 [[生產任務]] 放損率欄「主檔帶出並凍結」慣例——選定當下的列參數用於計算並隨 [[計價快照]] 凍結金額，主檔後續改列不回寫已建任務。計價快照仍只凍金額分項（既有裁決不變）；追因看任務的母版規格列／計價面積欄。
- 重凍時點：製程確認階段改選母版列、改計價面積、改台數或改頁數，任務儲存時重算並重凍快照（快照隨任務儲存重建，與建立時凍結同一機制）；製程定案後走工單異動，異動確認時重算重凍（比照既有工單異動成本處理）。

[[材料主檔]] 不新增後端欄位（母版規格列即後端既存的重量計價尺寸表列），但 wiki 卡需把計價子表列升格為業務可見子層並補欄位表（落卡清單見文末）。

### 三、成本計算規則（材料型任務的材料費，公式正本 [[BOM結構]] M1-M5，本案不改公式、只補輸入來源）

| 計價方式 | 工單上的輸入 | 材料費 |
|---------|------------|--------|
| 按重量・重量計（M1）| 選母版規格列 | 單張＝列面積(㎡) × 克重 ÷ 1,000,000 × 噸價；材料費＝單張 × 目標數量 |
| 按重量・令價計（M2）| 選母版規格列 | 令價＝克重 × 長(吋) × 寬(吋) × 0.00071117 × 磅價；單張＝令價 ÷ 500；材料費＝單張 × 目標數量 |
| 按重量・單張計（M3）| 選母版規格列 | 材料費＝列表列單價 × 目標數量 |
| 按面積・單獨面積（M4）| 填計價面積 | 格價＝查表(計價面積, 目標數量)；材料費＝計價面積 × 格價 × 目標數量 |
| 按面積・總面積（M4）| 填計價面積 | 總面積＝計價面積 × 目標數量；格價＝查表(總面積, 目標數量)；材料費＝總面積 × 格價 |
| 按數量（M5）| 無（自動落級距）| 材料費＝查表(目標數量) × 目標數量 |

- 目標數量＝預計生產＋放損（既有唯讀衍生欄，口徑正本 [[數量換算規則]]），本案不動數量鏈。**數量單位依計價方式**：重量類材料任務以張計（既有）；按面積、按數量計價材料任務以該料的件數計（MODIFIED——[[數量換算規則]] § 三現行把材料型任務數量口徑寫死為大紙張數，僅對重量類成立，落卡時改寫為依計價方式分述）。
- **MES 不套通用數量乘數**（MODIFIED——[[BOM結構]] § 通用數量乘數屬 EC「部件數量 × 通用數量」的商品結構；MES 任務的目標數量已是絕對投入量，再乘即重複計價。落卡時於該段補 MES 適用範圍註記）。
- 書冊台數換算（頁數 ÷ 每台頁數 × 本數）不進 MES：張數本由印務手填，已涵蓋（拍板 5）。裝訂 B1 台數計價的 MES 側輸入改由裝訂型任務的台數欄承接（見 § 二、裁決 10）；母版規格列上的每台頁數欄僅供 EC 側引擎取用。
- 查無對應價格區間（面積或數量超出矩陣範圍）：材料分項顯示 0，沿 [[生產任務]] 既有「輸入缺漏的分項金額顯示 0，系統不以預設值或推估頂替」慣例；印務回頭修主檔區間或改輸入。
- 所選材料規格的重量計價尺寸表一列都沒維護時：母版規格列無可選值、任務存不了——處理路徑是印務回 [[材料主檔]] 補列（主檔維護本歸印務，既有機制吸收），不設系統內例外分支。

### 四、選擇責任與拼版邊界（拍板 3）

- MES 工單：母版規格列由 [[印務]] **人選**，系統不做選優、不給建議（線上單建立來源不在本次範圍，見 § 五分流表與 § 六）。
- 拼版方案選優（成本計／數量計自動挑母版）**限縮至 EC 商品建立與報價情境**——維護商品的人不是印務，需仰賴系統代算；MES 暫不考慮材料拼版。此為對 [[BOM結構]] § 拼版與大紙張數的 MODIFIED（原文未限定情境），非新增機制。
- 張數換算維持 [[數量換算規則]] § 三既有規則：印務依所選材料規格與拼版在系統外換算、把張數填入預計生產欄；本案補上「母版規格列由印務選定」的明確步驟。

### 五、建立來源分流、配方展開與工單異動

工單建立來源（正本 [[工單]] § 建立來源）對新增欄位的填值分流：

| 建立來源 | 新增欄位的值 |
|---------|-------------|
| 審稿路徑（線下單）＋印務手動增開 | 印務於製程規劃選定／填寫；缺必填擋存 |
| 審稿路徑（線上單，系統自動建） | **不在本次範圍**（裁決 11：先忽略 EC 單整合，本次以線下單為主；填值來源選項留 [[PT-050-線上單自動建工單的計價輸入承接]] 待 EC 整合時裁決） |
| 打樣 NG（製程問題）重跑 | 系統建新打樣工單後走完整製程規劃（[[打樣流程]] 既有行為），各新欄由印務重新選填 |

- [[部件配方]] 的材料欄維持只選到材料規格，不加第四層（配方屬 EC 商品側；拍板 6）。
- 配方展開帶入的材料行，母版規格列／計價面積為空；印務在製程規劃編輯任務時補齊，未補齊者受必填驗證擋存。
- 製程定案後改選母版規格列或改計價面積，走既有工單異動（比照預計生產欄慣例），成本依 [[計價快照]] 既有異動規則處理。

### 六、範圍外（明示不做）

| 項目 | 理由 |
|------|------|
| MES 材料拼版代算 | 拍板 3；拼版模數算法另案 [[PT-042-拼版模數算法]] |
| 書冊台數換算進 MES | 拍板 5，張數手填已涵蓋 |
| 物料庫存掛層下移到母版列 | 拍板 8，進銷存另案；[[物料庫存]] 卡標注已知限制 |
| 部件配方選到第四層 | 拍板 6 |
| EC 線上單整合（自動建工單的計價輸入承接） | 裁決 11：先忽略、以線下單為主；三值來源（EC 存值／建單重算／印務補填）見 [[PT-050-線上單自動建工單的計價輸入承接]]，EC 整合時裁決。Prototype 同步移除 EC 相關訂單與工單假資料 |
| 實際成本口徑變更 | 實際材料成本仍＝報工領用量 × 材料單價（既有，[[生產領域資料結構總覽]] 成本鏈），本案只動預估分項 |

### 七、總覽差異（[[生產領域資料結構總覽]] 比對）

- 關聯：**無新增、無刪除、基數無變**。生產任務對材料主檔的引用為既有「引用 BOM 一類」；本案把引用深度自「材料規格」延伸到「其重量計價尺寸表列」（列屬材料規格的子表，非獨立實體）。
- 總覽卡需同步措辭一處：主檔層描述「[[材料主檔]]（群組→材料→規格）」補計價子表列與備料規格，屬層級描述校正，不動 ER 圖。

---

## 必含段① 邊界完整性表

實體 × 四動作：

| 實體 | 建立 | 修改 | 作廢 | 查詢 |
|------|------|------|------|------|
| 生產任務・母版規格列欄 | 線下單：手動建任務或配方展開後由印務選定，必填擋存；線上單：不在本次範圍（裁決 11）；打樣重跑：印務於新工單製程規劃重新選定 | 製程確認階段可改；定案後走工單異動 | 隨任務作廢，欄值留存供追因（既有任務作廢規則，無獨立處理） | 任務詳情顯示列名稱與帶出參數 |
| 生產任務・計價面積欄 | 線下單：印務填寫，必填擋存；線上單：不在本次範圍 | 同上 | 同上 | 任務詳情顯示 |
| 生產任務・台數欄（B1 限定） | 線下單：印務填寫，必填擋存；線上單：不在本次範圍 | 同上 | 同上 | 任務詳情顯示 |
| 生產任務・頁數欄（B2／B3 限定） | 線下單：印務填寫，必填擋存；線上單：不在本次範圍 | 同上 | 同上 | 任務詳情顯示 |
| 材料主檔・計價子表列 | 印務維護主檔（既有行為，本案不改） | 印務維護；已建任務不受影響（參數已凍結於任務） | 列刪除不回溯已建任務——任務存選定當下的列參數、[[計價快照]] 存凍結金額，不回查主檔（PT-039 既有裁決口徑）；新建任務選不到已刪列 | 主檔維護介面（既有） |
| 計價快照 | 任務建立時凍結金額分項（既有，材料分項改依本案公式計算） | 不可（凍結即不變，既有） | 隨任務（既有） | 預估成本分項視圖（既有） |

狀態 × 進入／離開：**不適用**——本案不新增、不修改任何狀態與轉換；生產任務、工單狀態機均維持現行（理由：新增的是計價輸入欄，不影響生命週期）。

## 必含段② 管理指標段

本案不新增指標（Miles 未明示需求）。既有指標影響檢查：

| 既有指標／視圖 | 來源欄位 | 影響 |
|---------------|---------|------|
| 工單預估成本（計價快照四分項彙總） | 計價快照材料分項 | 來源欄位不變；材料分項數值由「規格層單價 × 張數」修正為依計價方式公式計算，口徑更貼 [[BOM結構]] 正本 |
| 成本對照（預估對實際） | 計價快照 vs 報工實際成本 | 結構不變；預估側準確度提升，對照意義不受影響 |
| [[生產績效指標]] 各項 | 報工與時數 | 不涉及，無影響 |

## 必含段③ 情境四分類表

| 分類 | 情境 | 說明 |
|------|------|------|
| 主情境・系統內 | 印務手動建材料型任務：選材料規格 →（重量）選母版規格列／（面積）填計價面積 → 系統依計價方式算材料費 → 存檔凍結快照 | 100% 系統內走完 |
| 主情境・系統內 | 配方展開建工單：材料行帶到材料規格 → 印務編輯任務補第四層輸入 → 存檔 | 100% 系統內走完 |
| 主情境・系統內 | 裝訂型任務計價：B1 印務填台數、B2／B3 印務填頁數，本數取任務目標數量 → 系統查表算裝訂費 → 存檔凍結 | 100% 系統內走完（裁決 10、12） |
| 不支援 | 線上單自動建工單的計價輸入承接 | 裁決 11：EC 整合先忽略、以線下單為主；三值來源見 [[PT-050-線上單自動建工單的計價輸入承接]] |
| 邊界・系統內 | 主檔調價後的既有任務 | [[計價快照]] 凍結吸收（既有機制） |
| 邊界・系統內 | 製程定案後要改選母版列／改面積 | 工單異動吸收（既有機制） |
| 邊界・系統內 | 面積、數量或裝訂查表查無對應（含 B2 落不到區間、B3 比對不到頁數項目） | 分項顯示 0（既有「空值比假值誠實」慣例吸收），印務修主檔區間或改輸入 |
| 邊界・系統內 | 主檔停用了已被引用的列 | 已建任務參數已凍結不受影響；新任務選不到停用列（既有主檔啟用欄機制吸收） |
| 邊界・系統外 | 印務計算用料面積（成品尺寸＋出血＋工藝餘量） | 誰做：印務。照什麼：依印件規格與既有估料實務（現況本就人工估料）。資料怎麼回：填入計價面積欄。實務驗證：屬印務既有日常作業，非新交付格式 |
| 邊界・系統外 | 印務換算上機張數（拼版） | 既有系統外協作（[[數量換算規則]] § 三明文），本案不改；結果回系統＝填預計生產欄 |
| 不支援 | MES 拼版代算 | 拍板 3；另案 [[PT-042-拼版模數算法]] |
| 不支援 | 書冊台數換算進 MES | 拍板 5 |
| 不支援 | 庫存掛層下移到母版列 | 拍板 8，進銷存另案 |

## 必含段④ grill 拍板紀錄

| # | 問題 | 拍板 | 理由 |
|---|------|------|------|
| 1 | 工單要選到的「規格中的規格」是哪層？ | 重量計價材料的母版尺寸列（後端 MaterialSpecWeightEntry）；計價大類在材料層、子類在材料規格層 | 單價記在列上，不選列成本無單價來源；Miles 舉的 A／B 即同規格下兩列母版 |
| 2 | 三類計價的工單輸入各是什麼？ | 重量：人選母版列。面積：填面積數值，區間與級距自動落格。數量：不選，自動落級距 | 面積格價是每單位面積單價、面積是乘數（同格 0.6 與 0.9 ㎡ 成本差 50%）；EC 後端連區間都不選、由面積自動落格 |
| 3 | 由誰選？ | MES 工單印務人選、不給建議；拼版選優只屬 EC 商品建立；MES 暫不考慮材料拼版 | 維護 EC 商品的人非印務才需系統代算；MES 印務有專業判斷 |
| 4 | 成本公式？ | 與後端同構自動算；BOM 母版列設好參數，工單只選對項目 | 參數維護歸主檔、工單不重複輸入 |
| 5 | 書冊台數換算？ | 不做進 MES | 張數印務手填已涵蓋 |
| 6 | 配方展開的第四層何時補？ | 配方仍只到材料規格；印務在製程規劃補 | 配方屬 EC 商品側，計價細節是印務專業 |
| 7 | 防呆？ | 不設特例；新增計價輸入欄列任務必填，走既有必填驗證，缺即無法儲存 | 錯誤情境能收斂就收斂，不當特例看 |
| 8 | 庫存掛層？ | 不動，進銷存不考慮；重點是工單建立與成本計算正確 | 功能優先度不高、確保正確即可 |
| 9 | 線上單自動建工單時兩欄的值從哪來？（稽核退回後裁決） | 承接 EC 報價結果自動帶入 | （已被裁決 11 收斂範圍：查證發現 EC 未持久保存該三值，前提不成立） |
| 10 | 裝訂 B1 台數在 MES 怎麼來？（稽核退回後裁決） | 裝訂型任務新增台數欄，印務直接填；系統只查表 | MES 計價輸入收斂為同一心智模型：重量選列、面積填數、裝訂填台數；避免跨任務隱式依賴 |
| 11 | EC 未存三值，線上單承接怎麼成立？（復審後裁決） | 先忽略 EC 單整合，本次以線下單為主；prototype 的 EC 相關訂單與工單假資料一併移除 | 整合前提未定，不擋線下單主線；三值來源選項（EC 存值／建單重算／印務補填）留 OQ |
| 12 | 裝訂 B2／B3 的欄位有沒有缺？（Miles 主動指出） | 補「頁數」欄（B2 落區間、B3 精確比對）；本數＝任務目標數量，不另設欄 | 後端 B2／B3 公式查表鍵為（頁數, 本數），缺頁數即算不出 |

隱含假設清單（設計者自補、Miles 未明說）：

1. 計價面積欄填**單件**用料面積；總面積子類型由系統乘目標數量得總面積後落格（EC 引擎同構）。
2. 查無對應價格區間時材料分項顯示 0，沿既有「輸入缺漏顯示 0」慣例。
3. 母版列參數凍結比照放損率欄「主檔帶出並凍結」慣例；計價快照維持只凍金額。
4. 製程定案後改選母版列／改面積走工單異動，比照預計生產欄慣例。
5. 重量類成本採進價口徑（噸價／磅價／表列單價即 EC 引擎同一組參數）。
6. 外發整包（按數量計價材料行）成本＝級距價 × 任務目標數量，與場內數量計材料同式。
7. 打樣 NG（製程問題）重跑走完整製程規劃、各新欄由印務重新選填（對齊 [[打樣流程]] 正本，Miles 未明說）。
8. 裝訂三法的本數＝裝訂型任務目標數量（後端「本數＝通用商品總數」在 MES 的對應口徑，Miles 未明說）。

## 必含段⑤ 守恆段

本案涉及的量與流動等式（生產領域基準：[[生產領域資料結構總覽]] 成本鏈與數量鏈、[[數量換算規則]] § 四料帳平衡）：

| 量 | 等式 | 雙向說明 |
|----|------|---------|
| 金額（預估材料費） | 工單預估材料分項 ＝ Σ 材料型任務計價快照材料分項；每筆＝依計價方式公式（本體 § 三） | 上升：加開材料任務→彙總同步增。下降：任務作廢→自彙總排除（既有計價快照隨任務規則）。同筆換值：改選母版列／改面積／改台數／改頁數→儲存或異動確認時重算重凍（本體 § 二重凍時點），彙總隨之更新。凍結後主檔改價不流入（快照隔離，既有） |
| 金額（實際材料費） | 實際材料成本 ＝ 報工領用量 × 材料單價（既有，不動） | 本案不改實際側；預實對照視圖兩側口徑各自成立 |
| 數量（張數） | 理論用量 ＋ 放損 ＝ 實際領用（既有料帳平衡，[[數量換算規則]] § 四） | 本案不動數量鏈；目標數量僅作計價乘數輸入，數量帳無新流動 |
| 面積 | 不設守恆帳 | 計價面積是計價參數、非存量——無面積庫存帳，不存在進出留存；明示不適用 |

---

## 定案後 propose 前須更新的 wiki 卡清單（經 wiki-amend）

| 卡 | 更新要點 |
|----|---------|
| [[材料主檔]] | 層級描述正名（三層＋計價子表列＋備料規格）；計價子表列補業務可見欄位表（重量：尺寸名／寬高單位／克重／噸價／磅價／表列單價／每台頁數；面積：面積區間×數量區間×格價；數量：級距×價）；範圍外點名改寫（列本身是業務可選規格、排序才是實作） |
| [[BOM結構]] | 分層表補計價子表列層、標明大類在材料層／子類在規格層；拼版方案選優限縮至 EC 情境（MODIFIED）；三類子表對稱說明；§ 通用數量乘數補 MES 適用範圍（MES 不套，MODIFIED）；B1 台數計價補 MES 側輸入＝裝訂型任務台數欄、B2／B3 補 MES 側輸入＝裝訂型任務頁數欄（MODIFIED） |
| [[數量換算規則]] | § 三補「母版規格列由印務選定」步驟與拼版系統外換算的接法；材料型任務數量口徑改依計價方式分述（重量類＝張、面積／數量類＝件，MODIFIED） |
| [[生產任務]] | 數量段新增母版規格列、計價面積、台數（B1 限定）、頁數（B2／B3 限定）各欄；「不設任務層計價輸入欄」引言改寫（面積計價材料改為填數值、裝訂改填台數／頁數，MODIFIED）；必填擋存機制與時點（儲存時檢查）明文化（新增把關，依據拍板 7） |
| [[工單]] | 材料清單欄明寫記到哪層（材料規格＋重量類母版列／面積類計價面積） |
| [[部件配方]] | 材料欄明寫只到材料規格、第四層由工單階段補 |
| [[生產領域資料結構總覽]] | 主檔層層級描述同步 |
| [[計價快照]] | 一句話明確：選定列與面積記於任務欄位、快照仍只凍金額 |
| [[物料庫存]] | 標注已知限制：庫存掛規格層、不細分母版列，進銷存另案 |
| [[工單製程規劃]]（07 情境） | 第 1 步選到層明確化、第 5 步與生產任務卡「選規格選項」收斂；補「選母版列／填面積」步驟 |
| [[印務]]（03 角色） | 順修既有斷點：補 BOM 三類主檔維護職責項 |
