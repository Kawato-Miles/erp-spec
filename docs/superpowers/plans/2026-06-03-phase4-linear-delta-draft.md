# Phase 4 Linear delta 草稿（2026-06-03）

目標 project：**中台 - 訂單管理**（`4927231b-7fe3-407e-8d1f-94eddc3f0a84`，現狀態 Kick off）。
delta 邊界 = 6-01 交付後 2 個 archived change：route-C（refactor-order-receivable-refund-model）+ enhance-order-list-filter。
分流：核心邏輯 + 狀態機變動投中台（正本）；業務平台視圖層沿用、不另繪。

## A. project 描述 block-level 更新段（save_project 須重送完整描述）

### A1. § Design userflow「訂單異動」行
OLD：退款（業務主管核可 → 業務建退款收款紀錄推進）→ 應收即時更新
NEW：退款（業務主管核可 → 核可後應收即生效 + 退款款項核銷對帳應退差額）→ 應收即時更新

### A2. § 功能邏輯「補收 / 退款不對稱」（只改退款段；補收正項 + 大額補收 > 50000 紅標為既有 unify-billing 內容、本次不動）
OLD（退款段）：退款負項走業務主管核可、必綁退款收款紀錄推進、不進正向期次（保留稽核歷史）
NEW（退款段）：退款負項走業務主管核可，核可後應收即生效（與補收對稱、不綁退款收款累計達標）；退款款項自身切「已完成」為物理錨點、核銷對帳「應退差額」（收款淨額 − 應收 > 0），不進正向期次（保留稽核歷史）
（保留不動：補收正項跳過審核直達已執行、即時增額；大額補收（> 50000 元）寫紅標通知主管但不阻擋 —— 此為 unify-billing 既有內容、非本次 route-C delta，senior-pm 第 1 輪誤判為捏造，已查證源於 OQ-BI-4 + 主 spec OrderAdjustment 說明）

### A3. § 功能邏輯「編輯時機」
OLD：印件售價製作前可改、製作後走訂單異動
NEW：明細時點分界 = 訂單完成 / 已取消終態集合 —— 訂單完成前（含製作中 / 出貨中等所有未達終態狀態）印件售價 + 其他費用可由業務 / 諮詢直接增減（含調降，調降一律留活動紀錄供主管事後查見）；進入終態後鎖定、金額異動一律走訂單異動。報價鎖定點 = 訂單完成（審核通過 / 報價待回簽皆不鎖）

### A4. § 功能邏輯「三方對帳」
NEW 補：退款核銷「收款淨額 − 應收 > 0」的應退差額（多筆退款帳平不分筆、匯款證明逐筆挂）；應退差額警示不可忽略（防「帳上已退、實際沒退」）並防超退

### A5. § 功能邏輯 新增行「缺口究責」（修正：不新增欄位，沿用既有訂單業務欄位推導 —— store spec L68 正本）
NEW：缺口究責 —— 缺口第一責任人 = 訂單負責業務（沿用既有訂單負責業務欄位推導、不新增欄位）、監督人 = 該業務之主管（由角色關係推導），供完成前調降 / 應退差額追蹤究責

### A6. § FE「訂單列表」（修正：補付款狀態、移帳齡 —— 對齊 filter change spec L18 篩選欄位集）
OLD：訂單列表（印件子層展開 + 角色可見範圍過濾 + 帳齡 / 售後篩選）
NEW：訂單列表（印件子層展開 + 角色可見範圍過濾 + 篩選器：業務負責人 / 訂單狀態（依所選訂單類型涵蓋完整狀態集）/ 訂單類型 / 付款狀態 / 售後狀態 / 交期起訖區間 / 關鍵字 + 清空篩選 + 搜尋框放大鏡 icon）

### A7. § 訂單異動狀態機 mermaid
- 移除「已執行 --> 已核可: 取消已完成收款致累計不足」（回退機制）
- 改「已核可 --> 已執行: 關聯退款收款切已完成 · 累計達金額」為「已核可 --> 已執行: 核可後應收調整即生效」
- note 改：補收（免審）/ 退款 / 諮詢取消 —— 核可後應收即生效 · 不綁退款收款累計（寫入 Linear 不帶內部代號）

### A8. § 金流狀態機 Payment mermaid
- 改「已完成 --> 已刪除: 取消 · 連動訂單異動回退」為「已完成 --> 已刪除: 取消（邏輯刪除保留稽核）· 重算對帳應退差額」

## B. issue 層更新（save_issue 只傳 id + description，保留 estimate/assignee/cycle）

- **BE-171 訂單異動流程**：退款規則改「核可後應收即生效、不綁收款累計達標、退款款項核銷應退差額」；移除回退；補收不對稱不變。
- **FE-261 中台訂單列表與詳情頁**：列表補篩選器（業務負責人 / 付款狀態 / 交期起訖區間 / 清空 / 放大鏡 icon）；詳情頁明細編輯門控改終態集合；對帳面板應退差額不可忽略。
- **BE-170 訂單主流程**：明細時點分界門控（完成前可增減含調降）+ 完成前調降留活動紀錄 + 缺口究責沿用既有訂單負責業務欄位推導（不新增欄位）。
- DE-52（設計，In Progress）/ FE-260 / BE-169（售後）：route-C 不直接改，沿用 project 描述引用、不覆寫。

## 寫入結果（2026-06-03）
- [x] save_project 中台訂單管理（4927231b）：A1-A8 全套用、狀態保留 Kick off
- [x] save_issue BE-171（Todo）：退款規則去過時、移除回退舊機制
- [x] save_issue FE-261（Todo）：列表篩選欄位集（補付款狀態 / 移帳齡 / 放大鏡）+ 詳情明細時點門控 + 對帳面板應退差額
- [N/A] BE-170（In Progress，Eric 動工中）：依 linear-delivery「進行中 issue 不覆寫」跳過，由 project 描述承載 route-C 變更
- [N/A] DE-52（In Progress 設計）/ FE-260 / BE-169：route-C 不直接改、沿用引用
- senior-pm 獨立重評 4 維度全通過（Round 2）；待 Miles 眼見確認 mermaid 渲染（R3）

## 另案（senior-pm Round 2 附帶發現，非本次交付範圍）
主 spec `openspec/specs/order-management/spec.md` § Requirement「Payment 邏輯刪除」(L2658-2723) 仍描述舊「OA 回退至已核可」機制，與 route-C archive 後移除回退的最終正本（state-machines + 退款 OA 段已正確移除）不一致 —— archive sync 殘留。建議另開 doc-audit 對齊（不影響本次 Linear 交付，草稿採已移除回退的正確版本）。

## C. Linear 操作注意
- save_project 帶 state 保留現狀態（Kick off），防自動跳轉；description 整段覆寫須重送完整內容。
- save_issue 只傳 id + description。
- mermaid 須 Miles 眼見確認（R3）。
