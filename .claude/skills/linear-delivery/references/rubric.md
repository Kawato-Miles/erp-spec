# 交付給開發文件 · 評分標準（Rubric）

> 用法：交付前由**評分者**（與實作者分離的 sub-agent，結構性交付才開；不需 senior-pm——交付內容自 BRD / PRD 整理、上游已定案，評分聚焦 rubric 符合性而非重跑上游查證）逐維度評分；未達合格分數 → 修正 → **重新評分**，以 goal 機制迭代直到達標（硬上限 3 輪、卡住問 Miles、不放水）。
> 重點是 Step 4「**具體拒絕案例（絕對不要做的事）**」—— 禁令越精確，評分者越好抓違規；評分 MUST evidence-anchored（引用草稿具體位置，非空泛評語）。
> **評分尺度（2026-07-06 分數化、2026-07-15 加 D5）**：每維度 **通過 = 2 分 / 部分 = 1 分 / 未通過 = 0 分**，滿分 10 分。**合格分數 = 10 分，且 D4 必須 2 分（一票否決）**；未達 10 分不發布。
> 案例皆取自 2026-06-01 訂單管理交付的真實事件（D5 自 2026-07-15 工作流 v2.0 改版新增）。

---

## 評分輸出格式

```
[交付評分 · 第 N 輪]　總分：N / 10
D1 正本邊界：2 / 1 / 0　← 違反禁令：<引用草稿具體位置> / 修正方向：<...>
D2 分層與顆粒度：...
D3 完整性：...
D4 真實性（不捏造）：2 / 0（一票否決，無 1 分）
D5 交付結構完整性：...
達標判定：是（10 / 10）/ 否
總結：可發布（達標）/ 需迭代（列必修項 → 交回實作者修正後 MUST 重新評分，不得自審放行）
```

---

## D1 · 正本邊界

**定義**：Linear 為對開發的唯一正本，交付內容自包含、跨單據只以 Linear 識別碼互指（Notion 已停用為 BRD，不引用）。OpenSpec spec 是 PM 內部工作版本，不外露；資料欄位表不寫入交付內容（欄位正本在 wiki 實體卡，開發端直接取得）。

**絕對不要**：
- 絕對不要在交付內容寫 `openspec/specs/...` 或任何 `.md` 內部檔路徑
- 絕對不要把「規格正本」指向開發打不開的內部檔
- 絕對不要引用 Notion 頁面 / DB 連結當交付依據（Notion 已停用為 BRD）
- 絕對不要把 PM 內部流程術語（change / delta / archive / OQ 編號）寫進給開發的內容
- 絕對不要寫「欄位正本：wiki ○○卡」「狀態列舉正本為 wiki ○○卡」類**指路句**——交付內容自包含後這種句子只是把讀者導去別處，Miles 拍板 Linear 不放任何指路內容（2026-08-27；wiki 作為取材來源不變，但不出現在票文）

**正例**：資料欄位段以自包含欄位表呈現（業務語言、只列本批相關欄位）；跨 issue 以 Linear 識別碼（如 FE-259）互指。

**反例（2026-06-01 真實）**：資料欄位段寫「規格正本：openspec/specs/order-management/spec.md § Data Model」、各 Backend issue `## Ref` 列 `openspec/specs/...`。Miles 指出對開發與公司而言 Linear 上的內容才是正本，內部工作版本引用不該出現。

**評分**：無任何內部引用 = 通過；殘留 1-2 處 = 部分；多處或核心指向內部檔 = 未通過。

---

## D2 · 分層與顆粒度（v1.9 三層版）

**定義**：三層各司其職——Project＝薄目錄（概述、Use Case、範圍主票清單、跨票遊戲規則），不藏任何規則本體；Feature 主票＝該範圍商業邏輯與驗收條件的唯一正本；職能 sub-issue＝**行為契約**（什麼情況下系統要發生什麼／不許出現什麼）＋驗收測試。內容不重複、由大到小可順讀，讀者不需回頭跳層。

**絕對不要**：
- 絕對不要在 project 描述展開商業邏輯章（正本在主票；project 每範圍只留一句話＋主票原生提及）
- 絕對不要在 sub-issue 複寫主票已寫的商業邏輯（語意相同即算重述，不限逐字；一句原生提及帶過）
- **絕對不要在 sub-issue 寫實作手段詞**——交易、回滾、鎖、快取、樂觀更新、查詢時聚合、旗標、序列產生器、多對多、可空關聯等；行為要求只寫業務可讀的行為契約句，手段由開發決定
- 絕對不要讓 sub-issue 只剩「見主票」一句而無行為要求與驗收測試
- 絕對不要讓一張 sub-issue 出現多個「主要歸屬」——多重歸屬＝主票切太細，回頭以工作範圍重切主票

**正例**：不寫「印件印製維度推進與空工單建立為同一個交易，任一步失敗一起回滾」，寫「訂單管理人確認製作細節時，印件狀態推進與工單草稿建立要同時成立——不允許出現印件已推進、工單卻沒建出來的狀況，反之亦然」。

**反例（2026-08-19 工單管理真實）**：BE 票寫滿「同一個交易」「樂觀併發前提」「查詢時聚合」等偽技術散文 → BE 開發回報「完全看不懂實際上的工作是什麼」；且票內段落標題要求讀者回頭讀 project 的章，形成 project ↔ 票來回跳讀。

**評分**：三層分工乾淨、sub 全為行為契約句且無手段詞 = 通過；零星重述或殘留手段詞 1-3 處 = 部分；project 藏規則本體、sub 空洞或手段詞成片 = 未通過。

---

## D3 · 完整性

**定義**：交付含模組所需的必要區塊；**狀態密集模組每個狀態機附 UML**（狀態 + 轉換 + 觸發）；**有業務可觀測結果的功能附驗收條件**（可勾稽情境、3-5 條、達成 / 未達成二元）。

**絕對不要**：
- 狀態密集模組（多實體各有狀態），絕對不要交付沒有任何狀態流轉圖的文件
- 雙維度狀態（如審稿 / 印製、開票 / 收款），絕對不要擠成一張圖
- 絕對不要漏掉模板規定的必要區塊（概述 / 使用情境 / Spec）；絕對不要把資料欄位表寫進交付內容（欄位正本在 wiki 實體卡）
- 有明確業務結果的功能，絕對不要交付沒有任何驗收條件的 project 描述；絕對不要把通用品質門檻（測試 / code review / 文件）當該模組的驗收條件混進來

**正例**：訂單交付補上 8 類狀態機（訂單 / 印件雙維度 / 打樣 / 訂單異動 / 售後 / 發票 / 折讓 / 請款期次雙維度）各一張 Mermaid 圖。

**反例（本次真實）**：交付初稿涵蓋訂單 / 印件 / 金流 / 發票多個狀態機的模組，卻完全沒有狀態流轉圖 → Miles 指出「只要是狀態的都需要附上 UML」。

**評分**：必要區塊齊 + 狀態機 UML 齊 + 驗收條件齊（可勾稽、未混 DoD）= 通過；缺 1-2 張狀態圖、驗收條件單薄或混入通用門檻、某區塊單薄 = 部分；狀態密集卻無 UML、或缺核心區塊、或有業務結果的功能完全無驗收條件 = 未通過。

---

## D4 · 真實性（不捏造）｜一票否決

**定義**：交付內容的規則 / 狀態 / 數值 MUST 來自規格正本。來源未定義者，停下來記 OQ + 標「另案處理」，不自編。

**絕對不要**：
- 來源（spec）未定義某邏輯時，絕對不要自編一套規則填入交付文件
- 絕對不要用「應該 / 大概 / 通常」的推測當定案寫入
- 絕對不要為了「看起來完整」而補上沒有依據的狀態轉換 / 公式 / 欄位 / 驗收條件情境

**正例（本次真實）**：要畫付款狀態圖時，發現 spec 未定義「部分退款 / 已退款」判定條件 → 停下來問 Miles + 記 `oq-manage` ORD-027 + 交付文件標「依收款累計推導、完整定義另案處理」，**不畫捏造的轉換**。

**反例**：spec 沒寫就自行假設「收到任一筆退款 = 部分退款、退滿 = 已退款」當定案畫進圖裡、發布給開發。

**評分**：全部有據、缺口已顯性化 = 通過；任一處捏造 = **未通過（整體一票否決，不可發布）**。

---

## D5 · 交付結構完整性（v2.0 結構件）

**定義**：交付模板 v3.0 的結構件齊備——Project 含「測試決策」章（受測介面 2-4 個＋測試先行說明，只此一處）與範圍主票清單（每條附原生提及）；每張 Feature 主票掛 PM team、為其 sub-issue 的 parent；每張 sub-issue 含「行為要求」「驗收測試」（直接列清單、每條標受測介面，不重複引言）與「Blocked by」段，parent 掛對主票；阻擋關係建在 sub 層。

**絕對不要**：
- 絕對不要漏掉「測試決策」章；絕對不要另附「Prototype 參照邊界」表（為準／不為準邊界正本在 CLAUDE.md 分工宣告，交付不重述）
- 絕對不要交付沒有「行為要求」「驗收測試」段或「Blocked by」段的 sub-issue；票內絕對不要重複測試先行引言（該說明只在「測試決策」章）
- 絕對不要留沒有 parent 的職能 sub-issue、或 parent 掛錯主票
- 驗收測試絕對不要落在「測試決策」章未議定的受測介面上（含議定文字涵蓋的行為範圍；先回頭補議定，不偷跑）
- 驗收測試的期望值絕對不要用「與實作同一套方式重算」得出（自我循環、永遠會過）——期望值必須取自主票驗收條件的具名案例，且必須是受測介面上看得到的值（引擎中間值不算）
- 絕對不要另開設計票（DE 票）——操作方式與介面由 Prototype 承載

**評分**：結構件全齊 = 通過；零星缺漏 1-2 處（如個別票漏 Blocked by）= 部分；系統性缺失（全部票無驗收測試、無測試決策章）= 未通過。

---

## 演化紀錄

> 依 SKILL.md § Rubric 與模板演化（影片 6 步驟 SOP）新增 / 調整維度後，在此追加版本與理由。

| 版本 | 日期 | 變更 | 理由 |
|------|------|------|------|
| v1.0 | 2026-06-01 | 初版 4 維度（正本邊界 / 分層顆粒度 / 完整性 / 真實性）| 自訂單管理交付的 4 次 Miles 校正提煉：openspec 外露、需求單範本誤用→精煉模板、sub issue 過簡、狀態機缺失、付款狀態缺口；真實性設為一票否決 |
| v1.1 | 2026-06-01 | Step 4 評審者從通用 sub-agent 改為 senior-pm（PM agent）| 業務平台訂單管理交付驗證時，通用評審對 ERP spec 細節不熟，D4 真實性查證誤判：把 order-management「訂單列表最長逾期天數篩選」（L1401，真有）與已移除的「Payment 老化主管聚合」（L2640，v1.12 移除）混淆，在一票否決維度卡最久仍判錯。senior-pm 熟 ERP spec / Vault，能正確查證來源；通用 agent 憑 CLAUDE.md spec 清單描述猜測不可靠 |
| v1.2 | 2026-06-01 | Step 5 改剛性閉環（修完必重評、達標才寫入）+ 評審獨立性（重評餵完整草稿不暗示改動）+ 防 reward hacking（不放水 / 不表面改字過關）+ 硬上限 3 輪卡住問 Miles；評審輸出補迭代輪次 / 達標判定 / evidence-anchored 引用位置 | Miles 要「PRD 修到通過 Goal 為止」。上網研究三類最佳實踐後補強：/goal（supervisor 完全獨立評審防 mission drift）、LLM-as-judge（3 級窄量表 + 禁令對抗 leniency bias + analytic 逐維度 + evidence-anchored）、agentic loop（reward hacking 防呆 + 迭代上限）。修正「業務平台交付時修一輪沒重評就自審寫入」鬆散處 —— 評審才能判通過、實作者不能自判 |
| v1.3 | 2026-06-11 | D3 完整性必要區塊清單加入「驗收條件」（可勾稽情境、3-5 條、二元判定、與功能邏輯說明分工、不混 Definition of Done）；不新增評分維度 | 驗收條件最佳實踐查證：格式採情境式（Given/When/Then）與規則式條列混用、依素材性質分流（Altexsoft / Scrum Alliance）；數量 3-5 條、超量即拆（Scrum Alliance / Mike Cohn）；outcome 導向、可量化閾值、各條獨立（Atlassian / Scrum Alliance）；驗收條件 ≠ Definition of Done 不混寫（Mike Cohn / Atlassian）。「可驗證 / 不寫做法」已由 D2、D4 覆蓋，「附得齊不齊」歸完整性，故併入 D3 避免維度爆炸 |
| v1.4 | 2026-07-06 | D1 正本邊界自「Notion / Linear 雙正本」收斂為「Linear 唯一正本、交付內容自包含」；新增禁令「不引用 Notion 連結當交付依據」；資料欄位正例改自包含欄位表 | 審稿交付盤點時發現 prepress-review spec 無 Notion BRD 連結（Purpose 段 TBD），Miles 拍板：已不再使用 Notion 作為 BRD，交付文件拿掉 Notion 引用 |
| v1.5 | 2026-08-27 | D1 新增禁令：禁 wiki 指路句（「欄位正本：wiki ○○卡」類）；wiki 仍為取材來源但不出現在票文 | 三面同步交付第 3 輪：第 1 輪評分者依 PM-1074 慣例判通過、第 3 輪判違規，兩輪相左呈 Miles，拍板刪除並禁絕 |
| v1.5 | 2026-07-06 | 評分分數化：每維度 通過 2 / 部分 1 / 未通過 0，滿分 8，合格分數 = 8 且 D4 必為 2（一票否決）；評分者自 senior-pm 改為一般 sub-agent（rubric 符合性評分，不重跑上游真實性查證）；資料欄位表自交付內容移除（欄位正本在 wiki 實體卡，開發端直接取得） | 中台審稿管理交付時 Miles 拍板：交付內容自 BRD / PRD 整理、上游已定案，不需 senior-pm 深度查證，改用 goal 機制迭代到合格分數；開發自 wiki 實體取得欄位，project 不複寫 |
| v1.6 | 2026-07-15 | 新增 D5 交付結構完整性（Prototype 參照／參照邊界段／測試決策章／驗收測試段／Blocked by／milestone；禁另開設計票）；滿分 8 → 10、合格分數 = 10 且 D4 必為 2 | 工作流 v2.0 改版（Miles 拍板，設計哲學參照 mattpocock skills repo）：Prototype 升為介面與互動正本、設計票移除、票層驗收改測試先行、票間相依顯性化 |
| v1.7 | 2026-07-15 | D5 對齊模板 v2.1：移除「Prototype 參照邊界」段要求（改為禁令：交付不另附邊界表，正本在 CLAUDE.md）；新增禁令「票內不重複測試先行引言」（說明只在測試決策章一處） | 中台審稿試跑後 Miles 校正：邊界表屬內部規範不外露交付、逐票重複引言是噪音 |
| v1.8 | 2026-07-20 | D5 對齊模板 v2.2：移除「有介面的 Key Feature 附 Prototype 參照」結構件與對應禁令 | Miles 拍板拿掉此規則；Prototype 仍為介面與互動正本，僅交付文件不設參照格 |
| v1.9 | 2026-08-19 | D2 改三層版：project 不藏規則本體、主票為商業邏輯唯一正本、sub-issue 為行為契約；新增禁令「sub-issue 禁實作手段詞」「sub 禁多重主要歸屬」；D5 對齊模板 v3.0：結構件加主票清單原生提及、sub parent 掛對主票、期望值須為受測介面可見值（中間值不算） | 工單管理交付後 BE 開發回報票內偽技術散文讀不懂、project 與票來回跳讀；Miles 拍板三層結構＋行為契約。前身：blocked-cases-log 2026-08-19 案例（D2 語意重述慣性、D5 中間值期望值）提煉 |
