---
type: business-logic
module:
  - prepress-review
related-spec: openspec/specs/prepress-review/spec.md
related-prototype: sens-erp-prototype/src/utils/prepressReview.ts
status: active
last-reviewed: 2026-05-19
---

# 審稿分配規則（業務規則）

> 本卡記載**業務規則**：候選集篩選原則、優先順序、破例派工觸發條件。
>
> **不記載**：演算法的逐步實作細節（屬實作層，留 `prepressReview.ts` 程式碼）。

## 一、自動分配觸發點

```
訂單付款 → hook 觸發
├── 印件 skipReview = true → 走 [[免審決策樹]]
└── 印件 skipReview = false → 觸發自動分配
```

## 二、候選集篩選原則

審稿人員須同時滿足以下三條件才進入**合格候選集**：

| 條件 | 業務含義 |
|------|---------|
| `active = true` | 啟用中（未停用） |
| `availableStatus = '在崗'` | 在崗（非休假 / 離職） |
| `maxDifficultyLevel ≥ printItem.difficulty` | 能力範圍涵蓋此印件難易度 |

→ 難易度匹配邏輯見 [[難易度機制]]。

## 三、合格候選集選擇優先序

當合格候選集非空時，選擇優先序：

1. **能力範圍最貼近**：選 `maxDifficultyLevel` 最小且 ≥ 印件難易度的人（避免高能力人員處理簡單案）
2. **同能力者按負載**：若多人 `maxDifficultyLevel` 相同，選**目前負載最少**的
3. **負載相同按 ID 穩定排序**：確保結果可重現

## 四、破例派工觸發條件

當合格候選集為**空**時（沒有審稿人員能力 ≥ 印件難易度）：

- 啟動「破例派工」邏輯
- 從**所有 `active` 且在崗**的審稿人員中選 `maxDifficultyLevel` **最高**者
- 記錄 note：「被指派者能力 X < 印件難易度 Y」（供[[審稿主管]]覆寫決策）

## 五、主管覆寫

[[審稿主管]]可主動覆寫自動分配結果：

- 覆寫事件記入 ActivityLog
- 用於計算「自動分配命中率」（1 - 覆寫率）

## 六、自動分配命中率（KPI）

| 指標 | 計算 |
|------|------|
| 命中率 | (總自動分配次數 - 被覆寫次數) / 總自動分配次數 |
| 目標 | 越高越好（代表自動分配的業務邏輯接近實際決策）|

→ 此 KPI 應納入 `01-products/erp/kpi/`（Phase B Stage 5 補）。

## 七、相關業務邏輯

- [[難易度機制]]
- [[免審決策樹]]
- [[稿件管理規則]]

## 八、相關角色

- [[業務]] / [[諮詢]] — 提供 `difficulty` 與 `skipReview` 輸入
- [[審稿]] — 受分配對象
- [[審稿主管]] — 覆寫分配、設定每位審稿人員的 `maxDifficultyLevel`

## 九、實作位置（不在 Vault）

- 演算法步驟：`sens-erp-prototype/src/utils/prepressReview.ts` § runAutoAssign / runAssignWithOverflow
- 測試案例：`prepressReview.test.ts`

## 來源

- OpenSpec [prepress-review/spec.md](../../../../openspec/specs/prepress-review/spec.md) design.md D3 / D13
- Prototype `src/utils/prepressReview.ts`（自動分配演算法）
