---
type: state-machine
module:
  - order-management
business-domain:
  - billing-cash
status: active
last-reviewed: 2026-06-02
source:
  # 往「上」指依據：轉換背後的分權／認列／回退規則正本
  - "[[訂單異動規則]]"
implemented-by:
  # 往「下」指被誰實作（OpenSpec Requirement 標題層）
  - "openspec/specs/state-machines/spec.md#Requirement: 訂單異動（OrderAdjustment）狀態機"
  - "openspec/specs/state-machines/spec.md#Requirement: OrderAdjustment 狀態機修訂（補收正項跳過審核中間態）"
  - "openspec/specs/state-machines/spec.md#Requirement: 諮詢取消退費 OA 系統建已核可"
related-spec: openspec/specs/state-machines/spec.md
---

# 訂單異動狀態

[[訂單]] 成立後遇到金額增減（追加印量、加運費、急件費、退費）時，用「訂單異動單」記錄這筆增減；本卡只講這張單據自己會經過哪些進度、什麼時候從一步走到下一步，跟主訂單分開算。誰能核可、錢什麼時候算進帳、退錯了怎麼還原，這些規則寫在 [[訂單異動規則]] 那張，這裡不重講。

## 這張卡要回答的問題

> 這段只講狀態怎麼分、什麼情況會跳下一步、是誰按的，每個問題在正文都有對應結論。規則對不對（為什麼多收錢不用審、退錢要核可）寫在 [[訂單異動規則]] 那張，不在本卡回答。

- 訂單異動單有哪幾種狀態？哪些是走到底就不再動的終態（已執行、已取消）？
- 補收（公司多收錢）、退款（公司退錢）、諮詢取消退費三種情況，為什麼走不同的路線？
- 每一步是誰按、還是系統自己往下走？要滿足什麼條件才會跳到下一步？

## 營運背景

這張異動單自己跑自己的流程：金額要加要減的審核、什麼時候算進帳，按它自己的步調走，不會卡住主訂單的生產跟出貨。狀態之所以分補收（公司多收錢）與退款（公司退錢出去）兩種走法，是因為兩個方向的審核強度不同——為什麼這樣分（誰能核可、為什麼這樣定）寫在 [[訂單異動規則]] 那張，本卡只記「哪個狀態、什麼情況會跳到下一個狀態」。

## 狀態清單

| 狀態 | 說明 | 對應營運需求 |
|------|------|------------|
| 草稿 | [[業務]]／[[諮詢|諮詢人員]] 建好異動單但還沒送出 | 給一個暫存空間，金額、原因填到一半可先存 |
| 待主管審核 | 業務送出後等 [[業務主管]] 核可（只有退款異動單會經過這一站）| 落實退款需主管把關，補收不經此站 |
| 已核可 | 主管已同意（此態主要落在諮詢取消這種系統建已核可、尚未確認執行的單上；一般退款由主管核可當下即推進已執行）| 給諮詢人員在實際退款前還能調整退款金額的空間 |
| 已退回 | 主管退回（附原因），業務改完可重送 | 讓主管擋下不合理退款，並給業務修正機會 |
| 已執行 | 終態；這筆增減正式計入訂單應收總額 | 補收建單即計入、退款由主管核可當下即計入（與實際退錢脫鉤，實際退錢由對帳應退差額盯住）|
| 已取消 | 終態；業務在草稿或已退回階段主動撤掉 | 異動單建錯或不必要時可作廢，不留無效單據 |

> 各狀態怎麼判斷、金額怎麼算進帳、有哪些一定要守住的規則，都見 [[訂單異動規則]]，本卡不重複寫。

## 狀態怎麼一步步走

退款這條走法（公司退錢給客戶、由業務開單；系統裡把退款金額記成負數）：

```
草稿 ─提交審核─▶ 待主管審核 ─主管核可（核可當下即生效）─▶ 已執行
待主管審核 ─退回─▶ 已退回 ─修改重交─▶ 待主管審核
草稿／已退回 ─取消─▶ 已取消
```

（已執行為終態、不再退回上一步；錢實際退出去與否由對帳的應退差額盯住，不改這張單的狀態。退款款項建錯時取消它，對帳應退差額會重新出現、引導業務重退，這張單維持已執行。）

補收這條走法（公司向客戶多收錢、由業務開單、不用主管審）：

```
草稿 ─業務按「存檔並馬上生效」─▶ 已執行（跳過主管審核那兩站，直接生效）
```

諮詢取消退費這條走法（系統自動開的單，固定退 1000 元；系統裡記成負數）：

```
系統自動建單 ─▶ 已核可 ─諮詢人員確認（不再綁退款款項累計）─▶ 已執行
```

## 什麼情況會跳到下一步、是誰按的

| 從哪一步到哪一步 | 是誰按的、什麼事讓它跳 | 條件 |
|------|---------|------|
| 草稿 → 待主管審核 | 業務點「提交審核」 | 限退款異動單 |
| 草稿 → 已執行 | 業務點「存檔並馬上生效」 | 只有補收的單這樣走（不用審）。系統會把生效時間記成當下、把核可人記成開單的業務自己，這筆補收不用去對應到某一筆收款紀錄 |
| 草稿／已退回 → 已取消 | 業務點「取消」 | — |
| 待主管審核 → 已執行 | 業務主管核可 | 限退款異動單。主管核可當下系統就把這張單推到「已執行」、生效時間記成核可時點、應收當下減去這筆金額，不必等實際退錢、不綁退款款項累計 |
| 待主管審核 → 已退回 | 業務主管退回（附原因）| — |
| 已退回 → 待主管審核 | 業務修改後重交 | — |
| 系統自動建單 → 已核可 | 諮詢取消時系統自動開這張單 | 諮詢取消時系統自動開這張單，核可人就記成「系統」，生效時間先留空，讓諮詢人員在退款前還能調整金額 |
| 已核可 → 已執行 | 諮詢人員確認執行 | 諮詢取消的單由諮詢人員確認後推到「已執行」、填上生效時間、應收當下減去，不再綁退款款項累計 |

> 「為什麼多收錢不用審、退錢要主管核可、諮詢取消由系統直接核可、退款認列為什麼跟實際退錢脫鉤」這些誰能核可、錢什麼時候算帳的原因，寫在 [[訂單異動規則]] 那張。

## 邊界

- 異動單不卡主訂單：就算這張異動單還沒處理完（例如還在等主管核可），主訂單的生產、出貨進度照常往前跑，不會被這張單卡住。主訂單自己要不要取消、最後怎麼收尾（取消或完成）→ 看 [[訂單狀態]]，不在本卡。
- 這張單一旦「已執行」（補收建單即生效、退款主管核可即生效，認列與實際退錢脫鉤），訂單該收的總金額會跟著重算，但系統不會自動去動 [[分期請款狀態|請款期次]]（不會自動幫你開一期或改一期）；每一期什麼時候開發票、收了多少錢，還有期次改過幾次的紀錄，都看 [[分期請款狀態]] 那張。請款期次有變動，也不會把這張異動單或主訂單退回上一步。
- 售後服務單本身的受理、處理、結案進度 → 走 [[售後服務]]；這裡只管：在售後服務單裡開出來的退款單、補印收費單，它們的進度怎麼跑。
- 金額怎麼算進帳、有哪些一定要守住的規則、補收跟退款各種情況怎麼判斷的完整內容 → 見 [[訂單異動規則]]（規則正本）與 [[付款發票邏輯]]（情境索引）。

## 相關連結

- 想看誰能核可、錢什麼時候算帳、退錯怎麼還原 → 看 [[訂單異動規則]]（規則寫那邊，這張只畫進度走法，規則的原因跟算法都回頭指這張）
- 相關實體：[[訂單]]（異動單依附的主實體）、[[售後服務]]（售後單內可建立關聯退款／補印收費異動單）
- 相關狀態：[[訂單狀態]]（異動單不會卡住主訂單往下走）、[[分期請款狀態]]（異動單就算生效了，系統也不會自動幫你開新的一期請款）
- 相關角色：[[業務]] / [[諮詢|諮詢人員]]（建立異動單，補收直接執行、退款送審）、[[業務主管]]（退款核可，補收不經手）、[[會計]]（月結對帳依對帳應退差額核對退款款項是否退好）

## 來源

- OpenSpec [state-machines/spec.md § 訂單異動狀態機](../../../../openspec/specs/state-machines/spec.md)（裡面含：多收錢的單跳過中間的審核站、諮詢取消的退費單由系統直接核可、多收錢跟退錢走不一樣的流程）
- 這張卡前後改過什麼、為什麼改 → 看 [[business-logic-changelog#訂單異動狀態]]
