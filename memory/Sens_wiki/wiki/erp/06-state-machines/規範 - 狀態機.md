---
type: meta
status: active
last-reviewed: 2026-07-28
---

# 狀態機卡撰寫規範（state-machine 層）

> 三層結構：動筆從 [[範本 - 狀態機]]（骨架）複製起手；合規樣貌對照 [[範例 - 狀態機]]（凍結快照）；共用治理（撰寫流程／停下鐵則／撰寫紀律／修改既有卡原則）見 [[卡片撰寫共用規範]]——**本卡只寫狀態機單元的特定標準**。
> 設計依據：[[index]] § Wiki 憲章（本質、第一讀者、兩判準、BRD 自足、單元定位模式）；方法論依據：BABOK 狀態建模（狀態表完整性）、Wiegers 多重表徵互核、UML 狀態機記法、SBVR／RuleSpeak 商業可讀原則、Business Rules 獨立原則（規則存在於軟體之外）。本規範依「雙向開發」迭代：每張卡稽核時發現的缺口回饋本卡，三層同 commit 更新。

---

## 〇、狀態機卡是什麼（先懂定位再動筆）

> 本節目的：每條規則都從這裡的定位推導出來。讀懂定位，遇到本規範沒涵蓋的情況也能正確裁量。
> Wiki 的本質、第一讀者、讀者情境、兩個判準（紙本測試／商業語言）、BRD 自足與「範圍外＝防護欄」的完整定義見 **[[index]] § Wiki 憲章**——本節不重述，只講狀態機單元的特定定位。

**狀態機卡是兩件東西**：

1. **狀態詞彙的正本**——「製作中」「已收訖」是全公司協作的共同語言，業務、印務、會計說同一個詞必須同義。本卡首先是一份受控詞彙表：每個狀態＝一個業務詞＋它的定義＋它存在的營運理由。
2. **商業邏輯在「單據處境」維度的投影**——規則正本在 `04-business-logic/`，本層回答「這些決定反映在單據處境上是什麼樣子」：單據在什麼處境、因什麼事件、變成什麼處境。

**與鄰層的關係**（投影不互相複寫）：

- 對 `04-business-logic/`：規則的「為什麼」正本在 04，本卡只寫狀態怎麼變、引用不複述。
- 對服務藍圖：藍圖是**旅程視角**（時間軸、角色接力），本卡是**單據處境視角**。同一事件（主管核准訂單）在藍圖是一個步驟、在本卡是一條轉換。

**兩個判準在本單元的應用與已知陷阱**：

- 紙本測試：「主管核可後鎖定成交條件」紙本時代也成立 → 寫；歸納演算法的優先序只存在於軟體 → 不寫本體，但在「範圍外」點名（防實作腦補）。
- 已知陷阱：**把上游實體的旅程寫成本卡的「前段」**（訂單卡「三條前段路徑」教訓）——付費與諮詢過程屬諮詢單，不是諮詢訂單的前段。

**本單元主要伺候的讀者情境**（依憲章五情境）：商業自足理解（狀態詞彙表）、規劃自查（轉換與動機）、訪談確認（UML 圖）。

---

## 一、判斷表

> 本節目的：不是每個實體都值得開狀態機卡；開錯卡比缺卡更難收拾（會被引用）。

### 這個實體需要狀態機卡嗎（任一答「否」即不開）

| 順序 | 問自己 | 否的話 |
|------|--------|--------|
| 1 | 實體有生命週期嗎？（從建立走到結束，中間有可命名的處境） | 不開卡 |
| 2 | 狀態有三個以上，或處境變化有業務把關意義嗎？（角色審核、責任交接） | 純兩態開關寫在 `05-entities/` 實體卡 |
| 3 | 是獨立實體的狀態，而不是既有實體的第二條進度線嗎？ | 第二條進度線併入既有卡作雙維度 |

### 結構變體（三選一，第一個命中即答案）

| 順序 | 問自己 | 變體 | 既有範例 |
|------|--------|------|---------|
| 1 | 同一實體有兩條互不干擾、各自推進的進度線嗎？（負責人、節奏、可逆性不同） | **雙維度** | 分期請款（開發票線 × 收款線）、印件（審稿線 × 印製線） |
| 2 | 狀態多且可分成幾個階段、階段間單向不可回頭嗎？ | **分段式** | 訂單（審核段 → 審稿段 → 製作段 → 出貨段） |
| 3 | 都不是 | **單鏈式** | 工單、需求單、諮詢單 |

### 歸位

- 檔名＝實體名＋「狀態」（如 `訂單狀態.md`），放 `06-state-machines/`（平目錄）。
- 一張卡＝一個實體的完整狀態機；雙維度的兩條進度線在同一張卡內分節。

---

## 二、前提確認（MUST 全部已確認才能動筆）

> 本節目的：把「寫的時候才發現不知道」提前到動筆前。答不出來的就是默會知識缺口——停下問，不准編。

| 前提 | 確認狀態 |
|------|---------|
| 狀態清單的來源依據在哪？（訪談／管理層拍板／法規／既有營運實務） | 已確認 / 待確認 → 停下 |
| 這個實體有幾種建立來路？各來路由誰、什麼事件觸發建立？建立之前的流程屬於哪個實體？（來路＝建立來源；前段＝本實體自身的狀態階段——兩回事） | 已確認 / 待確認 → 停下 |
| 初始狀態與終態是哪些？ | 已確認 / 待確認 → 停下 |
| 每個轉換由誰觸發？（人工角色／系統自動） | 已確認 / 待確認 → 停下（**不可假設觸發者**） |
| 轉換背後的規則正本在哪張商業規則卡？ | 已確認 / 找不到 → 先補 `04-business-logic/` 卡或開 OQ |
| 負向路徑（取消／退回／例外）怎麼走？ | 已確認 / 確認不存在（寫進範圍外） / 待確認 → 停下 |
| 與哪些其他狀態機有上下游互動？ | 已確認 / 暫無 |

---

## 三、撰寫規則

> 本節目的：規則強度分級——MUST 不容裁量、SHOULD 偏離須說明、MAY 自由。每條附理由，理解理由才能在沒涵蓋的情況正確裁量。共用紀律（回引不複寫／禁中英夾雜／OQ 外移／檔名即標題／零迭代史／不編造）見 [[卡片撰寫共用規範]] § 四，不在此重列。

| 強度 | 規則 | 理由 |
|------|------|------|
| MUST | 本單元的專業表示法＝**UML 狀態圖（mermaid `stateDiagram-v2`）＋狀態列舉表**：兩者皆為必備段，不以純文字敘述替代任一方 | 狀態機的完整性只有在圖與表併存時才驗得出來（可達性靠圖、詞彙與營運理由靠表） |
| MUST | 段名一律依 [[範本 - 狀態機]] 骨架、不加實體名前綴（寫「狀態列舉」，不寫「訂單狀態列舉」） | 段名跨卡統一，稽核與跨卡查找依字面即可判定 |
| MUST | 每段內容過紙本測試與商業語言兩判準（§ 〇） | 本卡是 BRD；軟體細節寫進來會讓商業讀者讀不懂、讓正本與實作雙重維護 |
| MUST | **狀態列舉只寫一次**：本卡「狀態列舉（正本）」段是該實體狀態的唯一正本，其他卡引用狀態名、不另列清單 | 兩份清單必然失去同步，協作詞彙一分岔即斷裂 |
| MUST | 命名一致：狀態名用名詞或完成式（已Ｘ／待Ｘ／Ｘ中）、觸發事件用動詞片語；不用流水號、不重排、不重用 | 狀態名是全公司共同詞彙；重排或重用會讓既有引用靜默指錯 |
| MUST | 正文零實作規格引用（不出現 OpenSpec、程式檔名、演算法） | 第一讀者讀不到那些文件；BRD 必須自足 |
| MUST | 狀態列舉表含「狀態／說明／對應營運需求」三欄；初始標「初始；」、終態標「終態；」 | 詞彙表三要素：詞、定義、存在理由；起點終點是生命週期完整性的底線 |
| MUST | 狀態機圖（mermaid UML）與轉換表**雙表徵互核**：圖上每條轉換在表有對應列，反之亦然 | 兩種表徵互轉能暴露「沒有定義結果的處境與事件組合」（Wiegers 方法，走查實證有效） |
| MUST | 觸發事件寫「誰做了什麼」：「〔角色〕做〔動作〕」或「系統於〔業務事件〕自動推進」 | 固定句型全卡一致，業務讀者學一次讀懂所有卡；觸發者缺席＝責任不明 |
| MUST | 狀態名、角色名、單據名與狀態列舉、角色卡、實體卡**一字不差** | 詞彙先行：同一個詞在全 vault 同義，否則協作斷裂 |
| MUST | 概述敘事不越界：「前段／路徑／段落」只指本實體自身的狀態階段；上游實體流程以「來路」一句帶過；業務專名不挪用為泛描述；多來路用開放措辭（「現階段支援」）不寫死數量 | 投影混淆是已知陷阱；寫死數量會在新增類型時全面翻修 |
| MUST | 轉換背後的規則回引商業規則卡，不複寫規則本體與計算公式 | 規則只寫一次；複寫第二份必然失去同步 |
| MUST | 寫「範圍外」段：明確列出本卡不涵蓋什麼、屬於誰；**容易被實作腦補的主題點名「存在但不在本卡」** | 範圍外是防下游過度設計與捏造的防護欄 |
| SHOULD | 同一狀態因同一類事件分流到不同目標時，分流條件互斥不重疊 | 條件重疊＝同一情況兩種結果，營運會吵架 |
| SHOULD | 關鍵轉換的營運動機停在「轉換 → 動機一句 → 1 個例子」三件套；例子用真實業務格式 | 動機防止後人無意推翻有理由的設計；超過三件套的論述代表它夠格立 04 規則 |
| MAY | 簡單狀態機（三態以下無分支）省略動機段，動機併入列舉表的「對應營運需求」欄 | 顯然的事不必開段落 |

---

## 四、修改既有卡前的檢查

> 本節目的：狀態列舉是商業決策正本，改動是商業行為不是編輯行為（共用原則見 [[卡片撰寫共用規範]] § 五）。

| 修改類型 | 改之前要確認什麼 |
|---------|----------------|
| 新增／移除狀態 | 商業決策依據（管理層拍板／訪談定案）；沿引用反查連帶影響的卡 |
| 改轉換的觸發者或條件 | 同上；下游實作規格須由變更工作流同步（不在本卡處理） |
| 純措辭修正 | 不需商業依據，但仍須通過語言紀律稽核 |

---

## 五、邊際情境處理

> 本節目的：判斷表走不通時的標準出口，不准自行發明歸法。

| 卡住的情境 | 標準處理 |
|-----------|---------|
| 一個實體疑似有兩套狀態 | 問「兩條進度線的負責人、節奏、可逆性是否不同？」不同 → 雙維度；相同 → 其實是一條鏈，重新梳理 |
| 某狀態不確定該不該存在 | 開 OQ（`oq-manage` mode B），停下 |
| 某轉換講不出觸發者 | 停下問使用者，**禁止**寫「系統自動」充數 |
| 狀態跨實體向上反映（如工單做完帶動訂單前進） | 各實體各自開卡；上層卡只寫「接收下層結果」，不複寫下層內部轉換 |
| 不確定某內容歸本卡還是商業規則卡 | 問「這是『處境怎麼變』（本卡）還是『為什麼這樣定』（04）？」答不出來 → 停下 |
| 段落內子狀態越長越多 | 先與使用者評估，不擅自拆 |

---

## 六、停下條件（狀態機特有；共用四條見 [[卡片撰寫共用規範]] § 三）

> 本節目的：高風險點的強制人工確認——讓 agent 亂猜風險高，停下來問風險可控。

1. **觸發者不明**：某轉換講不出是誰做的 → 停下，**禁止**寫「系統自動」充數。
2. **結果未定義**：某處境遇到某事件的結果沒人說得出來 → 停下（這正是狀態機卡要逼出來的缺漏）。

---

## 七、產出格式

一張卡＝一個實體的狀態機。**檔名即標題**（卡內不寫 `# 標題`）。**正文零實作規格引用、無正文來源段**（正確性來源由 frontmatter `source` 承載；卡的迭代史查 wiki/log.md 與 git，不在卡內記）。frontmatter 樣板與各段填寫提示見 [[範本 - 狀態機]]（骨架），完整合規樣貌見 [[範例 - 狀態機]]。

**段落結構**（八段；「為誰」＝這段伺候的讀者情境）：

| 段落 | 內容 | 為誰 | 必/選 |
|------|------|------|-------|
| 卡頂引言（無段名） | `讀完這張卡，你會知道：` ＋ 3 到 5 條，動詞開頭一條一行（哪個實體的狀態機與涵蓋哪段生命週期、有哪些狀態、怎麼轉與誰觸發、為什麼分這幾格）。來路用開放措辭不寫死數量；規則正本歸屬寫「相關卡」段，不寫進卡頂（通則見 [[卡片撰寫共用規範]] § 一之二） | 第一次接觸的 PM | * |
| 狀態列舉（正本） | 「狀態／說明／對應營運需求」三欄表；初始與終態標注。**本段是該實體狀態的唯一正本，狀態新增修改直接在此維護** | 全公司共同詞彙 | * |
| 狀態機圖（UML） | mermaid `stateDiagram-v2` 依 UML 記法，圖前附一小段記法與分組語意說明 | 訪談確認、一眼掌握 | * |
| 轉換條件與觸發事件 | 表格：轉換／觸發事件（誰做了什麼）／條件；與圖互核 | 規劃自查、防誤解 | * |
| 關鍵轉換的營運動機 | 「轉換 → 動機一句 → 1 例子」，只挑不顯然的 | 防後人推翻有理由的設計 | ? 簡單狀態機可併入列舉表 |
| 與其他狀態機的關係 | 上下游互動與閘門（wiki link 指過去不複寫對方內部轉換） | 邊界導航 | * |
| 範圍外 | 明列不涵蓋什麼、屬於誰；易被實作腦補的主題點名「存在但不在本卡」 | 防下游捏造 | * |
| 相關卡 | 依分類列 wiki link（規則／流程／實體／狀態機／角色），只列有連結的分類 | wiki 互連 | * |

**frontmatter**：欄位樣板見 [[範本 - 狀態機]]。狀態機卡的 frontmatter **不設 `module`／`implemented-by`／`related-spec`**——與實作模組、實作文件的對應屬 PRD 層，wiki 不承載；`source` 只往上指商業規則卡、服務藍圖或外部依據（拍板、法規），禁指同層、下層與實作文件。

### 結構變體的段落調整

- **分段式**：狀態列舉依段落分子節（每段落一個三欄子表），列舉末尾寫「段落間可逆性規則」。
- **雙維度**：狀態列舉、狀態機圖、轉換表各分兩個子節（每維度自己的表與圖），加「兩維度的關聯」小節講清楚閘門關係與互不影響的部分。

### 狀態機圖（UML）記法規約

記法的正面樣貌對照 [[範例 - 狀態機]]。

- `direction LR`；初始 `[*] --> 初始狀態`、終態 `--> [*]`
- 建立分流用 `<<choice>>` 選擇偽狀態；轉換標籤「觸發事件 [守衛條件]」；狀態顯示名與列舉一字不差
- 段落＝複合狀態（`state "段落名" as 別名 { }`）；雙維度＝兩個複合狀態各框一條進度線
- **群組型轉換**（任一非終態皆可取消／流失）：**自段落複合狀態的邊界出發畫轉換**——依 UML 語意，從複合狀態邊界出發的轉換適用其內全部子狀態；圖前說明交代此語意
- **歸納型轉換**：落點由系統依下層狀態歸納時（向上反映驅動的常見模式），畫 `<<choice>>` 節點＋「落入對應狀態」概括；**詳細優先序不畫也不寫**（屬實作規格），但在「範圍外」點名
- 圖前說明段：一小段文字交代記法與分組語意

> 過渡註記：2026-06-10 前曾以同名 `.canvas` 檔承載狀態圖（訂單、需求單兩個 canvas 仍在本目錄），已由卡內 mermaid 段取代；既有 canvas 去留待使用者處置。

---

## 八、不收清單

> 本節目的：越界內容的路由表。發現越界：判斷歸屬 → 確認目標卡存在 → 已有則刪、沒有則搬移後刪。

| 不收 | 歸屬 | 判斷訊號 |
|------|------|---------|
| 規則本體、計算公式、認列邏輯 | `04-business-logic/` | 看到「為什麼這樣定」的完整論述、公式 |
| 系統行為細則（驗收情境、演算法、優先序） | 實作規格層（不過紙本測試） | 看到 Given/When/Then、欄位行為、優先序編號 |
| 介面操作 | 原型實作層 | 看到按鈕、彈窗、頁籤等介面措辭 |
| 欄位名稱與定義 | `05-entities/` 實體卡 | 看到欄位名、型別 |
| 端到端跨角色旅程敘述 | 業務情境卡（實例）／服務藍圖（型） | 看到多角色接力的長篇情境 |
| 步驟級操作細節 | 業務情境卡（能力型） | 看到「第 1 步…第 2 步…」 |

---

## 九、擴張準則

> 本節目的：卡的成長路徑有規則，不靠感覺。**卡過長是內容越界堆積的症狀，處理是歸位不是放行。**

| 情境 | 做法 |
|------|------|
| 新實體有生命週期 | 新開卡 |
| 同一實體發現第二條進度線 | 併入既有卡改雙維度 |
| 卡明顯過長（粗標：正文超過 150 行） | **先過 § 八路由表檢查是否塞了該歸位的內容並外移**；歸位後仍過長 → 與使用者評估實體是否該拆（如子實體獨立開卡），不擅自拆 |

---

## 十、稽核維度（goal-driven；稽核者逐項勾）

> 本節目的：把稽核從被動清單變成**可驗證的完成目標**。一張狀態機卡的「完成」＝ **L1 共用維度**（八條，正本見 [[卡片撰寫共用規範]] § 四之二，本節不複寫）**＋下列 L2 狀態機單元維度**全部通過，以 [[範例 - 狀態機]] 為通過樣本對照。執行者／稽核者分離原則見 [[卡片撰寫共用規範]] § 二。

| # | 維度 | 通過標準 | 標竿對照（[[範例 - 狀態機]]） |
|---|------|---------|---------------------|
| 1 | 結構對齊 | 八段齊備（含變體調整）；無正文來源段 | 段落完整 |
| 2 | 狀態正本 | 三欄齊；初始與終態顯式標注 | 六個狀態標注齊 |
| 3 | 圖表互核 | mermaid 圖每條轉換在轉換表有對應列，反之亦然 | 七條轉換雙向對得上 |
| 4 | 可達性 | 每個非終態有離開路徑；每個非初始狀態有進入路徑 | 無孤立狀態 |
| 5 | 觸發者完備 | 轉換表每列標明角色或系統，無留白 | 每列標到業務或印務主管 |
| 6 | 條件互斥 | 同源同事件分流的條件不重疊 | 退回重評與直接改成本條件分明 |
| 7 | 負向路徑 | 取消／退回／例外有定義，或在範圍外明示排除 | 流失為群組型轉換 |
| 8 | 規則不複寫 | 規則本體與公式回引 04，本卡無複寫 | 報價算法指規則卡 |
| 9 | BRD 自足 | 正文零實作規格引用（無 OpenSpec、程式檔名、演算法）；每段過紙本測試 | 正文無實作文件 |
| 10 | 觸發事件句型 | 觸發事件採固定句型「誰做了什麼」（L1-3 商業語言的狀態機特化） | 觸發事件皆「誰做了什麼」 |
| 11 | 敘事不越界 | 概述不含上游實體流程；「前段／路徑／段落」皆指本實體自身狀態；專名未挪用；來路開放措辭 | 上游諮詢僅一句帶過 |
| 12 | 範圍外防護 | 範圍外段存在；易被實作腦補的主題已點名「存在但不在本卡」 | 資料帶入明細點名 |
| 13 | UML 圖 | mermaid 段存在且記法齊備（初始／終止／「事件 [條件]」標籤；分段用複合狀態；群組型轉換自複合狀態邊界出發） | 圖面分組邊界出發 |
| 14 | 連結方向 | `source` 指商業卡或外部依據（禁同層、下層、實作文件） | source 指規則卡 |

---

## 相關卡

- [[卡片撰寫共用規範]] — 三層結構治理、撰寫流程、停下鐵則、撰寫紀律、修改既有卡原則（本規範的共用層）
- [[index]] § Wiki 憲章 — 本質、第一讀者、兩判準、BRD 自足、單元定位模式（上位依據）
- [[範本 - 狀態機]] — 骨架（寫新卡的起手稿）
- [[範例 - 狀態機]] — 凍結範例（合規樣貌與稽核通過樣本）
- [[wiki-schema]] — frontmatter 規範
- [[erp_index]] — 分層體系（本卡所屬「流程／狀態／角色／資料」四視角層）
- [[business-domain-taxonomy]] — 領域分類
- [[log]] — 全知識庫唯一操作史（卡的歷史＝搜 `[[卡名]]`＋git）
- `04-business-logic/規範 - 商業邏輯.md`、`03-roles/規範 - 角色.md`、`05-entities/規範 - 實體.md` — 同構 sibling 規範
