## 1. 測試影響清單

依 Sens `erp-prototype-tests/docs/scenario-catalog.md` 節號列出：

| 類別 | 項目 |
|------|------|
| 改既有測試 | 10.3 改驗轉交單單頭、無單別篩選；7.19 改驗製程定案後「僅備註與目的站點可改」、需轉交開關仍鎖定 |
| 刪除情境 | 16.11 回運待辦 |
| 新增情境 | 10.22 改目的站點後已建的轉交單不動、之後新建的單取新站；10.23 已作廢或報廢的任務不可改目的站點、已完成仍可改；10.24 下游到料量不比對站點、只增不減，入庫成品判定仍只看品檢站 |
| mock 變動 | 先改 `(prototype)/MOCK-DATA-CHAIN.md` 刪轉交單別規則，再改 `production-floor/_lib/mock-data.js` 移除轉交單的單別與原轉交單兩欄 |

delta Scenario 與測試的對應：

| delta Scenario | 覆蓋節號 |
|----------------|---------|
| 已有報工與在途轉交單時仍可改目的站點 | 10.22、10.23 |
| 已完成任務可改、已作廢或報廢任務不可改 | 10.23 |
| 製程核可後印務主管可改且不必重審 | 10.23 |
| 製程核可前印務主管不可改 | 10.23 |
| 生管不可改目的站點 | 10.23 |
| 改站不改寫已建轉交單、新建單取新站 | 10.22 |
| 待搬運的單改去處時作廢後重新建單 | 10.22 |
| 作廢重開沿用原單目的地 | 10.22 |
| 送錯站已點收由現場直接搬 | 10.22（無建回運單入口）、10.24（送錯站的已點收量照樣計入） |
| 印務主管建立轉交單（目的地取自目的站點） | 10.22 |
| 生管自待搬視圖建單 | 10.22 |
| 已點收不可作廢 | 無專屬測試；擋下行為沿用既有規則，本變更只改提示文字（見 7.1） |
| 已有報工後擋下需轉交變更（目的站點仍可改） | 7.19、10.23 |
| 到料量不比對站點 | 10.24 |
| 目的站點變更記入生產任務歷程 | 10.22 |
| 製程核可後仍可改目的站點（work-order） | 7.19、10.22 |

## 2. 測試先行

- [x] 2.1 情境目錄第十章新增 10.22、10.23、10.24，修改 10.3、7.19，刪除 16.11；期望值取自 delta Scenario 的 THEN 描述（Sens commit 7abdec2）
- [x] 2.2 新增純函式測試 `tests/unit/production-floor/transfer-destination-change.test.mjs`（10.22、10.23、10.24）與 Playwright `tests/e2e/10-report-transfer/transfer-tickets.spec.mjs`（10.22），改寫 `tests/e2e/07-process-planning/task-form.spec.mjs`（7.19）（Sens commit 7abdec2）

## 3. Prototype 實作

- [x] 3.1 MOCK-DATA-CHAIN 與 mock-data 移除轉交單別與原轉交單連結（erp commit 57538252）
- [x] 3.2 轉交單不分單別：刪單別與原轉交單兩格、單別篩選、回運標籤、建回運單入口與對話框；點收不再有回運減記分支；已點收作廢提示改為現場溝通後直接搬（erp commit 57538252）
- [x] 3.3 目的站點與製程編輯分開判定：負責印務、編輯（代理）成員在任務非已作廢、報廢時可改，製程核可後印務主管也可改、不必重審；需轉交標記鎖定不變（erp commit 57538252）
- [x] 3.4 改目的站點不動已建轉交單；作廢重開沿用原單目的地；生產任務歷程記原站、新站、修改人、時間（erp commit 57538252）
- [x] 3.5 下游到料量改為前置已點收明細總和、不比對站點；入庫成品判定改用比對品檢站的函式（erp commit 57538252）
- [x] 3.6 mock 一致性檢查拿掉已刪除的轉交單別判斷（erp commit 4669ea12）
- [x] 3.7 主管在生產管理唯讀：不能接收工作、派工、報工、作廢報工、手動完成、調整工作包、建或改轉交單、代點收（erp commit b03f7566）

## 4. 驗證

- [x] 4.1 smoke 主流程通過；第十章 e2e 23 項全部通過；unit 452 項全部通過；全套 224 項通過、7 項既有待補（erp commit 57538252 訊息附測試結果）

## 5. 後端交付

- [ ] 5.1 轉交單刪除單別與原轉交單連結兩欄，處置既有回運單資料
- [ ] 5.2 移除建回運單與回運點收減記到料量、解除原明細佔用的行為
- [ ] 5.3 生產任務目的站點的修改權限：負責印務、編輯（代理）成員於任務非已作廢、報廢時可改；製程核可後另加印務主管；生管不可改；核可後修改不觸發重新送審
- [ ] 5.4 生產任務歷程新增目的站點變更事件（時間、修改人、原站點、新站點）
- [ ] 5.5 轉交單目的地取自建單當下來源任務的目的站點；作廢重開沿用原單目的地；改單不改目的地
- [ ] 5.6 場內需轉交前置的到料量改為前置已點收明細總和、不比對站點；外發前置口徑待 PT-063 拍板後另案處理

## 6. Linear 交付

- [ ] 6.1 由 Miles 觸發 `linear-delivery`，將 production-execution、work-order 更新後的主 spec 交付到 Linear

## 7. 待補測試

- [ ] 7.1 情境目錄第十章補「已點收的轉交單按作廢被擋下，提示指引送錯站由現場溝通後直接搬」，並補對應 Playwright 測試
- [ ] 7.2 10.23 補畫面測試：已作廢或報廢任務的表單中目的站點唯讀
