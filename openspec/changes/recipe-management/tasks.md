## 1. mock 資料與純函式（模組層 `_lib/`）

- [x] 1.1 建 `recipes/_lib/mock-data.js`：印件款式 mock（單一部件款式 1 筆、多部件含款式層工序段 1 筆、跨款式共用部件 1 組共 4-5 筆），欄位 snake_case、名稱貼真實印刷業品項（A4 騎馬釘型錄 24 頁、吊牌鑰匙圈禮盒組、馬克杯鑰匙圈組）
- [x] 1.2 同檔補部件配方 mock（型錄本體／壓克力吊牌／金屬鑰匙圈／禮盒外盒 4-6 筆，含版本與狀態；鑰匙圈需被兩個款式引用以驗證共用）
- [x] 1.3 工序段引用既有 `work-orders/_lib/bom-master-mock.js` 的三主檔與設備，不另造一套主檔資料
- [x] 1.4 建 `recipes/_lib/expansion.js` 純函式：依款式部件清單與合併旗標算出「將建立幾張工單、各含哪些工序段」（輸入款式與調整後組合，輸出工單草稿結構陣列）
- [x] 1.5 同檔補純函式：配方段 → 生產任務的映射（同段掛材料與工序時拆兩筆、材料型設為工序型前置、計畫設備留空、放損率取工序主檔預設、數量依每組數量與用量倍率下行進位）
- [x] 1.6 建 `recipes/_lib/store.js`：action 命名對齊未來 API 動詞（`createStyle` / `updateStyle` / `createRecipe` / `reviseRecipe` / `activateRecipe` / `expandToWorkOrders` / `distillFromWorkOrder`）

## 2. 印件款式頁（清單＋詳情）

- [x] 2.1 建 `(prototype)/recipes/styles/page.js`：套 `resource-page.recipe.md`，篩選（款式名稱／產品類型／狀態）＋表格（款式名稱、產品類型、部件數、狀態、維護者）
- [x] 2.2 建 `(prototype)/recipes/styles/detail/page.js`：套 `detail-page.recipe.md`，頁首動作列（編輯、切換狀態）＋基本資料區塊
- [x] 2.3 詳情頁加部件清單區塊：表格（部件配方、每組數量、是否獨立開工單、顯示排序）＋行尾編輯刪除＋新增部件行（PanelDialog，部件配方以下拉挑選、每組數量須大於零）
- [x] 2.4 詳情頁加款式層工序段區塊（與部件配方詳情共用工序段表格元件），標示「所有部件湊齊後才執行」
- [x] 2.5 狀態切換：草稿→生效→停用；僅生效款式可被引用（驗收時於印件頁確認草稿款式不出現在選單）

## 3. 部件配方頁（清單＋詳情）

- [x] 3.1 建 `(prototype)/recipes/components/page.js`：篩選（部件名稱／產品類型／狀態）＋表格（部件名稱、產品類型、版本、狀態、被幾個款式引用）
- [x] 3.2 建 `(prototype)/recipes/components/detail/page.js`：基本資料＋「被哪些款式引用」清單（design 的風險緩解項）
- [x] 3.3 詳情頁加工序段表格：序號、工序／材料／裝訂（三主檔項目）、產線、能力需求、預設用量倍率、依賴前段、是否計入完成度；行尾編輯刪除、序號可改
- [x] 3.4 工序段新增／編輯面板：三主檔選擇鏈沿用 M1 的 `TaskFormDialog` 選擇鏈邏輯（材料三層／工序兩層／裝訂單層），但不提供機台、數量、色數欄位
- [x] 3.5 配方改版動作：`reviseRecipe` 產生新版並將舊版轉停用；同部件僅一份生效配方（嘗試啟用第二份時擋下並提示）

## 4. 印件頁引用展開

- [x] 4.1 在印件相關頁面加「印件款式」欄位呈現與「引用款式展開工單草稿」入口（款式為空時不顯示該入口）
- [x] 4.2 建引用 dialog 第一步：選生效款式（清單含款式名稱、產品類型、部件數）
- [x] 4.3 第二步預覽：列出帶出的部件組合（部件配方、每組數量、是否獨立開工單），可增部件、刪部件、改每組數量；同步顯示「將建立 N 張工單草稿」
- [x] 4.4 確認展開：呼叫 `expandToWorkOrders`，建立多張工單草稿並各自寫入來源款式／來源部件配方／來源配方版本；成功後 message 回饋張數與工單編號
- [x] 4.5 驗證調整不回寫款式：展開後回款式詳情確認部件清單維持原值

## 5. 工單詳情沉澱為部件配方

- [x] 5.1 在工單詳情頁首動作列加「存成部件配方」（製程審核完成之後的狀態才顯示）
- [x] 5.2 沉澱 dialog：指定所屬款式（可選現有或建立新款式）與部件名稱
- [x] 5.3 預覽將寫入的工序段清單，並明示不會存入的欄位（目標數量、建議日期、計畫設備、色數、預估成本）
- [x] 5.4 確認沉澱：同部件已有生效配方時走改版（版號＋1、舊版停用）並在回饋中標明；否則建立第 1 版
- [x] 5.5 打樣工單沉澱時顯示提示（一次性試作、作法未定案），仍允許繼續

## 6. 展開結果與 M1 銜接驗證

- [x] 6.1 展開產生的工單草稿在 M1 工單列表可見、可進詳情
- [x] 6.2 工單詳情製程規劃 Tab 顯示展開帶入的生產任務：材料型與工序型成對、材料型為工序型前置、計畫設備留空、放損率已帶主檔預設
- [x] 6.3 工單詳情顯示展開來源三欄（唯讀）
- [x] 6.4 展開帶入的任務可用 M1 既有的編輯／刪除動作調整（驗證兩種來源行為一致）
- [x] 6.5 多部件款式展開後，合併部件的工序段確實落在最前一個獨立開單部件的工單

## 7. 收尾

- [x] 7.1 `npx eslint .` 無錯誤
- [x] 7.2 dev server 實測四頁與兩條動線（展開、沉澱），console 無錯誤
- [x] 7.3 更新 `production-stage-high-level-design.md` § 5 的頁面清單狀態（實作頁面與規劃一致，另補「引用展開」獨立頁作為印件層入口的示範）
- [x] 7.4 commit 至分支 `prototype/production-stage` 並 push（不直推 main）
