## 1. 印件詳情頁基礎結構盤點

- [x] 1.1 確認 `/work-orders/print-items` 路由與印件總覽頁元件位置
- [x] 1.2 盤點現有印件詳情頁元件，確認既有區塊（基本資訊、預計產線、稿件、成品縮圖）的版面結構
- [x] 1.3 確認資料來源：印件、其下工單、各工單下生產任務、工單狀態、生產任務狀態與報工數量

## 2. 工單與生產任務區塊 UI 實作

- [x] 2.1 設計區塊版面：以工單分組（預設展開），工單列顯示「工單編號 / 負責印務 / 工單狀態」
- [x] 2.2 工單下列出生產任務，每筆顯示「PT 編號 / 工序名稱 / 預計數量 / 完成數量 / 入庫數量 / QC 狀態徽章 / 狀態」
- [x] 2.3 印件層級狀態詞置於頁首區，顯示印件印製狀態（依 state-machines spec）
- [x] 2.4 視覺區分自己負責工單與他人負責工單（如 Tag 標示「我負責」/「印務乙負責」）
- [x] 2.5 區塊資料載入：抓取印件下所有工單與生產任務（不依工單負責人過濾），含預計 / 完成 / 入庫三欄與最近 QC 結果
- [x] 2.6 印件狀態讀取路徑：與印件總覽共用同一查詢函式或 cache 來源，驗證兩處狀態同步

## 3. 報工權限守門

- [x] 3.1 UI 守門：報工按鈕與勾選框依 `WorkOrder.assigned_to == current_user` 判斷啟用 / 禁用
- [x] 3.2 禁用提示：hover 顯示「此工單由其他印務負責，無法在此報工」
- [x] 3.3 API 守門：報工 API 在伺服器端再次檢查工單負責人，繞過 UI 直接呼叫 SHALL 回傳權限錯誤（錯誤碼設計由開發決定）
- [x] 3.4 印務主管登入時套用同規則（不享跨工單代報特權）
- [x] 3.5 API 守門被觸發時寫入稽核日誌：使用者 ID / 目標 PT ID / 來源 channel / 被拒原因 / 時間戳

## 3.5 ProductionTaskWorkRecord source_channel 欄位（必修）

- [x] 3.5.1 ProductionTaskWorkRecord 新增 `source_channel` enum 欄位（craftsman_platform / supplier_platform / dispatch_board / work_order_detail / print_item_detail）
- [x] 3.5.2 五處報工提交點（任務平台 / 供應商平台 / 日程面板 / 工單詳情頁 / 印件詳情頁）均正確帶入對應 source_channel
- [x] 3.5.3 source_channel 由系統自動填入，UI 不顯示供修改
- [x] 3.5.4 既有報工紀錄遷移策略：歷史資料統一回填 `legacy` 或依 reported_by 角色推測（依開發判斷）

## 4. 報工元件重用與整合

- [x] 4.1 確認工單詳情頁既有批次報工元件位置與 Props
- [x] 4.2 重用該元件至印件詳情頁，新增「所屬工單編號」標註於每筆生產任務列
- [x] 4.3 單筆報工：點擊 PT 列「報工」按鈕開啟單筆面板（重用元件）
- [x] 4.4 批次報工：勾選多筆 PT 後點擊「批次報工」開啟批次面板（重用元件）
- [x] 4.5 提交後行為與工單詳情頁完全一致：建立 ProductionTaskWorkRecord、累加 pt_produced_qty、首次報工觸發狀態轉換、source_channel 帶入 `print_item_detail`
- [x] 4.6 批次提交部分失敗處理：採個別提交策略（成功的記、失敗的明確告知），不原子回滾（依開發判斷實作細節）

## 5. 跨工單透明化情境驗證

- [ ] 5.1 模擬跨印務印件：印件 B 含工單 #3（甲）+ 工單 #4（乙），驗證甲視角看到完整明細
- [ ] 5.2 驗證工單 #4 下生產任務的報工按鈕為禁用、勾選框為禁用、hover 提示正確
- [ ] 5.3 驗證甲只能勾選工單 #3 的 PT，批次報工面板不出現工單 #4 的 PT
- [ ] 5.4 驗證印務乙視角下，工單 #3 的 PT 同樣為禁用，工單 #4 的 PT 可操作

## 6. 規格一致性與文件對齊

- [x] 6.1 對照 state-machines spec 確認狀態詞顯示與該文件一致
- [x] 6.2 對照 production-task spec § 批次報工操作 確認元件行為一致
- [x] 6.3 對照 user-roles spec 印務角色職責 確認權限規則一致
- [x] 6.4 印件詳情頁 MUST NOT 出現 QC 建立、工單異動、PT 新增 / 編輯入口（這些仍於工單詳情頁）

## 7. 觀感與邊界提示

- [x] 7.1 於印件詳情頁加入說明文字或 Tooltip：「此頁可查看印件下所有工單，但僅可操作自己負責的工單」
- [x] 7.2 報工面板的「所屬工單」標註採次要視覺樣式（小字、淡色），不干擾數量輸入主視線

## 8. Prototype 驗收

- [ ] 8.1 從印件總覽點擊任一印件，能正確進入印件詳情頁
- [ ] 8.2 可見區塊：基本資訊、預計產線、稿件、成品縮圖、工單與生產任務（新增）
- [ ] 8.3 自己負責工單下的 PT 可成功單筆報工，pt_produced_qty 正確累加
- [ ] 8.4 自己負責工單下的多筆 PT 可成功批次報工，每筆建立獨立 ProductionTaskWorkRecord
- [ ] 8.5 首次報工觸發 PT 狀態從「待處理」轉為「製作中」，向上傳遞至工單 / 印件 / 訂單
- [ ] 8.6 他人負責工單下的 PT 報工操作確實被守門（UI + API 雙層），且寫入稽核日誌
- [ ] 8.7 五處報工管道的 source_channel 均正確記錄至 ProductionTaskWorkRecord
- [ ] 8.8 PT 列正確顯示「預計 / 完成 / 入庫」三欄與 QC 狀態徽章；QC 失敗情境下三欄差距可正確判讀重印缺口
- [ ] 8.9 印件總覽與印件詳情頁的印件印製狀態同步顯示，不出現不一致
