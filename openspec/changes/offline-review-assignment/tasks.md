# 實作任務：線下單審稿人員分派機制變更

## 1. 型別與狀態同步

- [ ] 1.1 `src/types/order.ts` 印件審稿維度 enum 新增「待分派」；印件型別確認「負責審稿人員」欄位（reviewerId 可為空）語意——完成判準：`npx tsc -p tsconfig.app.json --noEmit` 通過
- [ ] 1.2 狀態同步邏輯納入待分派轉換（印件建立初始＝待分派；分派→稿件未上傳／等待審稿；免審→待分派直達合格；改派不改狀態）——完成判準：同步規則單元對照 delta spec § 印件審稿狀態與 Round 同步的表逐列驗證
- [ ] 1.3 線上單上傳觸發自動分派、線下單上傳不觸發（停留待分派＋通知訂單管理人）的分流——完成判準：線上/線下各一筆 mock 印件上傳後狀態符合預期

## 2. 分派與改派

- [ ] 2.1 分派/改派共用 Dialog：候選全列（在崗）＋難易度/能力參考標示＋原因欄（分派選填、改派必填）——完成判準：能力低於難易度的人選可被選取且顯示標示
- [ ] 2.2 移除 `overrideAssignment` 能力過濾（throw error）邏輯，改為改派 action（雙角色、原因必填、ActivityLog 含原/新審稿人員）——完成判準：改派後待審清單歸屬更新、審稿狀態不變、紀錄可查
- [ ] 2.3 批次分派與批次改派（勾選多印件派同一人、各印件分別記 ActivityLog）——完成判準：勾 3 件批次分派後 3 筆 ActivityLog
- [ ] 2.4 訂單管理人角色加入角色切換機制，分派/改派權限對訂單管理人與審稿主管開放、其他角色不可見——完成判準：以業務角色檢視時無分派操作

## 3. 批次審稿

- [ ] 3.1 審稿人員待審清單支援勾選同訂單內多個自己負責的待審印件、進入批次審稿介面（跨訂單勾選不可批審）——完成判準：勾選兩張訂單的印件時批審按鈕不可用
- [ ] 3.2 批次合格：一張縮圖套用整批（同檔案為各印件輪次分別建立縮圖檔案紀錄）＋審稿後檔案逐印件必傳（缺件拒絕送出並指出印件）——完成判準：三件批審合格後各印件 current_round 縮圖為同一檔案、審稿後檔案各異
- [ ] 3.3 批次不合格：退件原因整批共用、審稿備註預設共用可逐件覆寫——完成判準：兩件批退後各自 Round 的原因相同、備註一件為覆寫值
- [ ] 3.4 批次內移除個別印件、其餘照批送出——完成判準：移除件維持原待審狀態
- [ ] 3.5 逐印件落紀錄：批審後各印件 ReviewRound（round_no 各自遞增）、狀態轉移、ActivityLog 逐件驗證；B2C 批次合格逐件自動確認可製作並建工單——完成判準：對照 delta spec § 批次審稿五條 Scenario 逐條通過

## 4. 訂單詳情整合與 Slack 討論

- [ ] 4.1 訂單詳情印件清單支援勾選、「開啟討論」按鈕（僅已回簽線下單顯示）——完成判準：回簽前訂單不顯示按鈕
- [ ] 4.2 mock webhook：`openReviewDiscussion` action 產生模擬討論串連結存於訂單、payload（訂單編號/客戶/印件名稱與難易度/mention 訂單管理人）記入 ActivityLog——完成判準：開啟討論後訂單詳情顯示討論串連結
- [ ] 4.3 回簽閘門：分派與開啟討論操作於訂單「已回簽」前不可用——完成判準：狀態「審核通過」的訂單無此二操作

## 5. 種子資料與驗證

- [ ] 5.1 補線下單待分派情境種子資料（含「稿件已上傳未分派」「已分派未上傳」兩種邊界，業務格式編號）——完成判準：訂單管理人視角可見待分派清單
- [ ] 5.2 e2e：線下單「回簽 → 開討論 → 分派 → 上傳 → 批次審稿合格 → 確認可製作」全流程 spec——完成判準：`npx playwright test` 該 spec 通過且無 console.error
- [ ] 5.3 e2e 回歸：線上單自動分派全流程不受影響——完成判準：既有 smoke spec 全部通過
- [ ] 5.4 檢查既有以「稿件未上傳」判斷的 UI 邏輯（語意改為已分派等稿）——完成判準：grep 稿件未上傳引用點逐一確認後列於 PR 說明
