# 任務清單

> 實作範圍：erp repo `/Users/b-f-03-029/erp`（apps/erp/src/app/(prototype)/work-orders/），MUST 經 repo 內 skill `prototype-from-prompt`。

## 1. mock 主檔補計價子表

- [x] 1.1 `_lib/bom-master-mock.js`：重量類材料規格補 `weight_entries`（尺寸名／寬高與單位／克重／噸價或磅價或表列單價／每台頁數），重量計、令價計、單張計三子類各至少一個材料有 2 列以上
- [x] 1.2 面積類材料規格補價格矩陣（`area_tiers` × `quantity_axis_tiers` × `price_cells`，含區間單位），單獨面積與總面積各至少一個材料
- [x] 1.3 數量類材料規格補 `quantity_price_tiers`（外發整包沿用）
- [x] 1.4 裝訂主檔補三法查表（台數×本數、頁數×本數、頁數項目×本數）與每本最低金額、總價最低金額（既有已齊，僅改輸入來源）
- [ ] 1.5 移除 EC 相關訂單與工單假資料（裁決 11；受影響頁面同步檢查不留死引用）

## 2. 任務表單四欄

- [x] 2.1 `TaskFormDialog`：材料型任務依所選材料計價方式大類條件展開——按重量顯示「母版規格列」單選（選項＝所選材料規格的 weight_entries，顯示尺寸名與參數）、按面積顯示「計價面積」數字欄（單位由矩陣帶出唯讀）、按數量不顯示額外欄
- [x] 2.2 裝訂型任務依計價方法條件展開——台數計價顯示「台數」欄、頁數計價與本數計價顯示「頁數」欄；不出現本數欄
- [x] 2.3 四欄納入既有必填驗證（儲存時檢查，缺即擋存）；`bom_ref` 增 `weight_entry_id`、任務增 `pricing_area / binding_sheets / binding_pages`
- [x] 2.4 任務表單顯示選定列名稱、計價面積（含單位）、台數或頁數
- [x] 2.5 mock 既有任務補齊計價輸入（15 筆母版列、1 筆計價面積、1 筆台數）並重算 20 筆凍結成本

## 3. 成本計算分支

- [x] 3.1 `estimate-cost.js` 材料費六式：M1 重量計（列面積×克重÷1,000,000×噸價×目標數量）、M2 令價計（克重×長吋×寬吋×0.00071117×磅價÷500×目標數量）、M3 單張計（表列單價×目標數量）、M4 單獨面積（面積×格價×目標數量）、M4 總面積（面積×目標數量×格價）、M5 數量（級距價×目標數量）
- [x] 3.2 裝訂費三式：每本單價＝max(查表, 每本最低)、裝訂費＝max(每本單價×本數, 總價最低)，本數＝任務目標數量；B1 查（台數, 本數）、B2 落（頁數, 本數）區間、B3 頁數精確比對
- [x] 3.3 查無對應區間（含 B3 比對不到）分項回 0，不取近似值；不套通用數量乘數
- [ ] 3.4 編輯改計價輸入存檔即重算覆寫 `est_cost`（沿既有凍結機制）

## 4. 驗證

- [x] 4.1 驗算錨（單張計）：表列單價 12 元 × 目標數量 1,050 張 ＝ 12,600 元（純函式驗算通過）
- [x] 4.2 驗算錨（單獨面積）：0.6 ㎡ × 格價 100 × 1,000 ＝ 60,000；同格 0.9 ㎡ ＝ 90,000（面積是乘數）
- [x] 4.3 驗算錨（裝訂 B1）：台數 4、本數 1,000 → 17,500；本數 50 觸總價最低 → 2,000
- [x] 4.4 擋存驗證：重量類未選母版列於介面實測擋下儲存並顯示「請選擇母版規格列」；面積、台數、頁數走同一段驗證
- [x] 4.5 超區間與缺輸入驗證：未填計價面積、未選母版列、B3 頁數比對不到 → 分項顯示 0
- [ ] 4.6 EC 假資料移除後工單清單與詳情頁無錯誤（console 無未定義引用）

> 階段一（第 1、3 組與計算側驗證）已提交（erp repo 1f7d30c），16 項純函式驗算全數通過。
> 階段二（第 2 組表單四欄與 mock 補值）已提交（erp repo 144f4d6），介面實測通過：
> 母版規格列／計價面積／台數／頁數四欄依計價方式正確條件顯示、擋存生效、
> 新增雪銅紙 150g 菊全 20,000 張材料費顯示 NT$ 28,635（與純函式驗算一致）。
> 未完成：1.5 與 4.6 的 EC 假資料移除——工單模組內無 EC 假資料，實際落點在訂單模組
> （3 筆線上單 mock ＋ `orders/_lib/online-order-actions.js` 自動建工單邏輯），
> 移除會退化既有已交付功能，範圍待 Miles 裁決。
