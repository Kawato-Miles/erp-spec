## 1. 資料聚合層（read model，零新增欄位）

- [ ] 1.1 在 `utils/prepressReview.ts` 新增母集合計算函式：輸入 orders，輸出「底下至少一件印件審稿維度 ∈ {等待審稿, 已補件, 不合格}」的訂單；排除狀態=已取消的訂單、排除印製維度=已棄用的印件
- [ ] 1.2 新增「訂單審稿停留天數」計算（牆鐘時長口徑）：最早一件印件首輪 `submittedAt` → 全部印件最終 `reviewedAt`（未全部審畢計至今日）的日曆工作天；附單元測試驗算多印件 / 補件循環情境
- [ ] 1.3 新增「距交期天數」與「審稿關卡逾期」判定：距 `Order.客戶交期` < 安全緩衝（暫定 ≤ 2 工作天）且仍在母集合；附單元測試
- [ ] 1.4 新增「訂單退件率」計算供與停留天數配對顯示（不合格輪次 / 總輪次）

## 2. 審稿訂單脈絡查詢視圖頁面

- [ ] 2.1 新增頁面元件 `pages/prepress/OrderReviewView.tsx`（暫名），於 `App.tsx` 加路由、`AppSidebar.tsx` 於審稿平台與中台各加側邊欄入口
- [ ] 2.2 訂單母列呈現：訂單編號 / 客戶 / 交期 / 距交期天數 / 停留天數（與退件率配對）；驗證母列 MUST NOT 出現「待審 N / 共 M」彙總標籤
- [ ] 2.3 訂單可展開列出該訂單全部印件（含已合格，以審稿維度狀態 Badge 標示）
- [ ] 2.4 依登入角色切資料範圍（沿用 PrintItemDashboard role-based 範式）：審稿人員僅見「底下有自己負責印件」的訂單、審稿主管見全部；主管與審稿列表欄位/排序/操作一致
- [ ] 2.5 列表篩選功能（承載「看自己 vs 看全部」與實務差異，取代主管專屬視圖）
- [ ] 2.6 自己負責印件提供「去審」跳轉至 `/prepress/inbox/:id`；非自己負責印件唯讀、無「去審」動作；驗證此頁不出現就地審稿動作

## 3. 訂單詳情限訂單層級版（審稿平台）

- [ ] 3.1 訂單母列點訂單編號 → 開啟限訂單層級訂單詳情，僅顯示「資訊（訂單編號/客戶/交期/案名）/ 印件清單 / 活動紀錄」三頁籤
- [ ] 3.2 驗證審稿角色版訂單詳情隱藏金額 / 發票 / 對帳 / 訂單異動 / 帳務 / 工單 / QC / 出貨頁籤（對齊 ORD-039 已拍板）

## 4. 角色權限調整

- [ ] 4.1 審稿人員 / 審稿主管對訂單模組權限調整為 R（限定唯讀）：可唯讀訂單脈絡限定欄位、不可編輯；驗證無法進入訂單模組完整功能、無法編輯任何訂單資料

## 5. 指標呈現

- [ ] 5.1 訂單母列「停留天數」MUST 與「退件率」配對顯示（不可單列停留天數）；逾期訂單以視覺標示（如標紅）並可彙總計數
- [ ] 5.2 驗證指標全部由既有時間戳 / 交期聚合，無新增資料欄位

## 6. 端對端驗證（Playwright）

- [ ] 6.1 撰寫 e2e：審稿人員開啟查詢視圖 → 只見有自己印件的訂單 → 展開看全部印件（非自己唯讀）→ 點自己印件跳回我的待審
- [ ] 6.2 撰寫 e2e：審稿主管開啟查詢視圖 → 見全部待審訂單 → 列表與審稿人員版一致、差異用篩選
- [ ] 6.3 撰寫 e2e：點訂單編號進限訂單層級詳情（三頁籤、無金額/帳務頁籤）
- [ ] 6.4 驗證 console 無 error / pageerror（沿用既有 e2e 嚴格斷言）
