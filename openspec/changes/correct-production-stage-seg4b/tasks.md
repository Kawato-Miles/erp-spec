# Tasks: correct-production-stage-seg4b

> 實作面：erp repo `apps/erp/src/app/(prototype)/`、分支 `prototype/production-stage`、經 `prototype-from-prompt` skill、dev server `erp-verify`（3020）。每批完成後 Opus 稽核 → Fable 裁決 → commit。規則正本＝delta specs 與 wiki 卡。

## 1. 分派與上傳（線下單把關）

- [x] 1.1 線下單上傳稿件不觸發自動分派：`uploadArtwork` 補訂單來源判斷（線上單才跑 `runAutoAssign`）——驗證：線下單上傳後印件維持待分派、線上單照舊自動分派（P1）
- [x] 1.2 線上單自動分派候選補「能力等級有值」要件（空值不進候選）；「在崗」過濾刪除（含 `_lib/prepressReview.js` 候選函式與相關顯示）——驗證：能力空值者不被自動分派、請假概念不出現在任何過濾（P12）
- [x] 1.3 審稿主管分派權限修正：`pending-assign` 頁對 `reviewer_supervisor` 放行（與 selector 一致）——驗證：主管角色可開待分派頁並執行分派（P6）
- [x] 1.4 「稿件上傳開放」旗標＋角色分流（B2B 業務代傳）；候選為空的提示（提示無可選人員、換人不成立）；批次分派收窄至同訂單——驗證：未開放印件上傳鈕不可用、非業務角色不見代傳入口、空候選有提示（P9）
- [x] 1.5 待審清單排序改「交期近的先審」（交期 selector 既有、改 sort 鍵）——驗證：清單依交期升冪（P7）
- [x] 1.6 上傳即建輪（批 1 稽核 P-01／P-02 裁決）：`uploadArtwork` 上傳當下建輪並綁檔（無審稿人員時 reviewer 留空、狀態維持待分派）；`assignReviewer` 改補寫既有未完成輪次不建輪——驗證：先傳檔後分派的線下印件檔案可見、分派後輪次 reviewer 正確且無重複輪

## 2. 免審與訂單層歸納

- [x] 2.1 免審輪次補建：印件建立（含加開、clone）時免審者自動建 `source=免審稿` 合格輪次（不產生審稿後檔案與縮圖）——驗證：新建免審印件有一筆合格輪、輪上無審稿後檔案（P2）
- [x] 2.2 免審 B2B 分流：`confirmSignBack` 的 `allSkip` 直達句改 wiki 口徑（B2B 免審合格後仍等業務確認可製作才離開審稿段）——驗證：全免審 B2B 訂單回簽後停在審稿段、業務逐件確認後才進製作等待中（P2）
- [x] 2.3 訂單層審稿歸納：實作派生六規則（含待分派依稿件有無分流）、掛審稿事件後重算；「待補件」可被寫入；製作段不退回、退回重審以警示標記呈現——驗證：各八態組合的訂單落點正確、補件中訂單顯示待補件（P3）
- [x] 2.4 mock 對齊：手種訂單審稿段狀態與派生規則不符者修正——驗證：全部 mock 訂單跑派生無矛盾（P3 連帶）

## 3. 審稿作業補齊

- [x] 3.1 退回重審動作：合格→待改稿（限合格、原因選填、不建輪次、寫印件事件與訂單活動紀錄）——驗證：合格印件可退回、已確認可製作不可退、原因空白可送出（P4）
- [x] 3.2 審稿備註送出後修改（限原審稿人員＋稽核留痕）；批次審稿補「共用備註＋逐件覆寫」——驗證：原審稿人員可改備註且留痕、批次審稿共用備註可逐件覆寫（P10）
- [x] 3.3 Slack mention 對象改取該訂單的訂單管理人欄位（去硬寫人名）——驗證：不同訂單管理人的訂單發起討論串 mention 各自正確（P5）
- [x] 3.4 打樣結果判定者收窄業務／諮詢（去業務主管）；NG-稿件判定門控補審稿維度前提（已確認可製作）——驗證：業務主管無判定入口、原打樣印件審稿維度未達已確認可製作時 NG-稿件擋下（P11、P13）

## 4. 指標移除與收尾

- [ ] 4.1 逾期指標移除：`isOrderOverdueInReview` 與相關顯示刪除（AR-14 不做）——驗證：審稿頁無逾期標記與 buffer 邏輯（P8）
- [ ] 4.2 ACCEPTANCE.md 續補 4B 驗收項（每任務群組至少一條端到端）＋既有審稿驗收項基準同步——驗證：驗收項可照走
- [ ] 4.3 跨模組資料流稽核（Opus）：免審全鏈（建印件→合格輪→B2B 確認→工單草稿）、訂單層歸納鏈（審稿事件→派生→訂單落點）、退回重審鏈——驗證：零斷鏈
- [ ] 4.4 `openspec validate correct-production-stage-seg4b --strict` 通過＋免登入白名單確認——驗證：validate 過、3020 可直開
