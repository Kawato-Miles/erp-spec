# 段 3 實作任務（品檢、齊套與完工判定、出貨、送達、訂單完成＋五項附屬）

> 實作約束見 design.md § Context（erp repo `prototype/production-stage` 分支、`prototype-from-prompt` skill、只動 `(prototype)`、dev server 用 launch.json 的 `erp-verify`／port 3020、平行對話有未提交變更勿誤 stage）。批次順序依 design § Migration Plan；每批完成走「Opus 稽核 → Fable 裁決 → commit」再進下一批。delta spec 與 prototype 同批完成，不留「spec 待補」。驗收操作續補 `ACCEPTANCE.md`。

## 1. 品檢規格與品檢頁（qc G1／G3、S1–S3；P1、P2、P4、P6）

- [x] 1.1 `qc` delta 落地：§ 待驗清單（來源含外發生產任務）、§ 分次驗收記錄（更正沖銷規則＋單筆在站量檢核三句＋攔截點指向裝箱回報）、§ 通過即入庫（完工良品數正名＋刪待搬視圖預設、改品檢區就地揀貨＋例外移置不成單）、§ 缺口處置（印件層缺口口徑分層並存＋處置可分次＋客戶照收改判良品）
- [x] 1.2 品檢頁到站量改由「目的地為品檢站且已點收」的轉交明細推導（移除靜態模擬值），依印件彙總
- [x] 1.3 品檢頁改行動版版型（沿段 2 師傅行動版範式，窄版單欄）；不通過原因改六分組固定選項、移除「其他」
- [x] 1.4 補品檢更正入口（沖銷式）：新增一筆負數紀錄、原紀錄唯讀不可編輯刪除；累計以代數和呈現
- [x] 1.5 單筆驗收檢核：通過＋不通過超過送出當下在站量時整筆擋下並顯示當下在站量（不做部分成立）
- [x] 1.6 缺口處置對話框補處置數量輸入、一次一筆可分次（不再全額吃掉）；兩軸（缺口是否補做 × 瑕疵品去向）各自獨立選
- [x] 1.7 印件詳情頁品檢紀錄與缺口處置兩個 Tab 補完整動線（段 1 只承接兩張唯讀表）；移除工單／生產任務層的品質欄位殘留
- [x] 1.8 品檢紀錄與品質帳收斂為單一資料源（現況兩模組各持一份、互不同步）
- [x] 1.9 打樣印件走同一條品檢路徑（不另立分支、不另設介面）

## 2. 補做發起與短出結案（work-order G2／G14；P3）

- [x] 2.1 `work-order` delta 落地：§ 異動下修與製程調整的數量重算（刪短出下修 Scenario、補短出不走下修）、§ 補生產承接（入口印件層＋預帶依齊套帳推算＋補做來源欄防重複發起＋重開不拉回印件）、ADDED § 短出結案
- [x] 2.2 補做發起動作實作：自印件層一鍵發起 → 建工單異動 → 預帶補做生產任務（起補工序與各工序補做量依齊套帳推算、數量預填處置合計）
- [x] 2.3 補做生產任務掛「補做來源」（指向缺口處置紀錄與其品檢紀錄）；已承接的處置於發起介面標示並擋下重複勾選
- [x] 2.4 短出結案動作實作：印務於印件層結案 → 印製維度轉已送達、購買數量與累計已出貨數量不下修、由上而下批次推工單與生產任務完成（已報工數不動）
- [x] 2.5 補做使工單重開時印件與訂單不回退（工單以「異動」態承載）

## 3. 出貨規格單一落點與額度檢核（shipment G11／G12／G13／G16–G19＋order-management G6／G7／G9；P5）

- [x] 3.1 `shipment` delta 落地：§ 出貨單建立與額度檢核（檢核時點／整張擋下／改明細重跑／兩條出路／訂單終態不建單／不加印製維度把關）、§ 揀貨與裝箱回報（每箱幾個＋回報完成才推進＋移除異常按鈕與自動通知＋差異兩來源分流）、§ 出貨與送達確認（人工推進＋憑證輸入）、§ 送達後的收尾判定（單頭層累計＋排除異常與已作廢＋≥ 購買數量）、§ 異常與作廢的額度回補（理由必填＋回補方向唯一）、ADDED § 數量帳守恆鏈
- [x] 3.2 `order-management` delta 的四條 REMOVED 落地（出貨單管理、多印件分次出貨追蹤、出貨單狀態機、出貨單掛訂單層），確認 Reason 與 Migration 逐條指得到 `shipment` 承接處
- [x] 3.3 prototype 出貨模組兩套實作收斂為一套（保留與正本對齊者、以路由層切換，收斂前先搜引用點）；出貨單狀態統一七值
- [x] 3.4 額度檢核實作：送出當下取最新額度、任一明細超額整張擋下並顯示當下額度、擋下訊息帶兩條出路；改明細數量時重跑同一檢核
- [x] 3.5 裝箱回報補「每箱幾個」欄；回報總數與出貨明細數量並排呈現讓差異看得見；回報完成才推進待出貨
- [x] 3.6 移除另存的實裝差異欄與系統內異常通知按鈕；補作廢／異常理由輸入欄與送達憑證輸入欄（三方式各自形式）
- [x] 3.7 可出貨額度改衍生計算含回補項（完工良品數 − 累計已出貨 ＋ 異常與作廢回補），累計已出貨與累計送達皆不存欄位

## 4. 送達與完成連動（order-management G10＋prepress-review G15；P7）

- [x] 4.1 `order-management` delta 的 ADDED § 訂單完成判定 落地（全部未棄用印件已送達＋打樣結果＝OK 守衛引 prepress-review＋不看款項＋全數棄用走取消）
- [x] 4.2 `prepress-review` delta 的 ADDED § 打樣週期數量歸零 落地（六欄逐一點名、週期界線＝最新打樣工單建立時點、生產數量不歸零、舊紀錄不刪）
- [x] 4.3 送達確認連動實作：出貨單已送達 → 累計送達數（單頭層加總）→ 印件印製維度已送達（排除異常與已作廢、≥ 購買數量）→ 訂單完成
- [x] 4.4 打樣結果連動：填 NG-製程問題時印製維度回等待中並按週期歸零六欄（最小資料層觸發入口，完整動線歸段 4）；打樣結果非 OK 時訂單停在出貨中
- [x] 4.5 已棄用印件退出訂單完成度計算分母與出貨清單

## 5. 外發回廠與貨運單頁面（dispatch-order G20／G21；P8、A8、A10）

- [x] 5.1 `dispatch-order` delta 落地：§ 任務交付即派單發送（交付產線單一觸發＋發送要求已指派供應商）、§ 派單狀態轉換（刪指派即發稿、Scenario 改指派不切狀態）、§ 回廠點收（認列載體改貨運單＋認列不鎖可重送＋重量差異只提示＋外發貨進品檢站）、§ 運費與關稅攤回（建單兩路徑＋運費兩層＋關稅即時算不落庫＋外包成本含固定運費）
- [x] 5.2 發稿觸發者收斂實作（行為變更）：移除「指派供應商即切已發稿」，改交付產線時要求已指派供應商、一次完成發送並切已發稿並記發稿時間
- [x] 5.3 新建貨運單頁面（併櫃視角、桌機）：列表與詳情、認列工單、逐筆點收與秤重、重量差異即時提示、運費關稅攤回明細、關稅單據
- [x] 5.4 認列工單自派單點收介面遷回貨運單（派單側不再承載認列入口，避免兩個入口）
- [x] 5.5 建單兩路徑實作：供應商發貨建單（一張）、管理端一次輸入多組運單號建多張單（運費按重量比例拆、餘數落最後一張、之後不隨實秤重算）
- [x] 5.6 關稅一張運單最多一筆、稅單號碼跨運單查重；攤回讀取時即時算不落庫；分攤金額由畫面算好逐列送出
- [x] 5.7 外包實際成本補固定運費分項（派單運費紀錄子表人工登錄那幾筆的加總），成本達成率與毛利率的分子取數隨之完整
- [x] 5.8 A8：派單明細改讀生產任務的點收與產出數字（取消副本與同步邏輯）
- [x] 5.9 A10：補齊或移除五張孤兒派單所指的工單（修 mock，不新增資料結構或校驗欄位）

## 6. 轉交來源放寬與情境表（production-execution G23／S4＋business-scenarios G8；P1 待驗清單）

- [x] 6.1 `production-execution` delta 落地：§ 場內轉交 適用路徑改「生產任務 → 產線／生產任務 → 品檢站」、來源含外發生產任務、暫存區三類排除補界線、明寫品檢完成品不進轉交體系
- [x] 6.2 `business-scenarios` delta 落地：兩張全流程表補「製程已確認」與「工單已交付」兩站、品質帳欄正名（移除 QC 單據狀態語意）、§ 品檢與出貨 收尾口徑與補做預帶對齊
- [x] 6.3 prototype 待搬視圖：外發生產任務在印務依點收報工後出現於可搬清單、目的地可選品檢站（段 2 視圖本體不動、只確認來源涵蓋）
- [x] 6.4 品檢待驗清單涵蓋外發批次（來源為外發生產任務的轉交單經品檢人員點收後入列）

## 7. 收尾貫通與驗收

- [x] 7.1 端到端推演自驗：品檢點收 → 分次驗收 → 完工良品數與額度 → 建出貨單（額度檢核）→ 揀貨裝箱回報 → 出貨與送達確認 → 累計送達 → 印件已送達 → 訂單完成，逐條追跨模組寫入（handover 坑 2 對策）
- [x] 7.2 誤記更正端到端推演：品檢誤記 → 揀貨實點差異在單上可見 → 業務轉印務 → 品檢補負數沖銷 → 額度回真數 → 業務改明細或作廢重建 → 守恆鏈重新成立
- [x] 7.3 外發端到端推演：貨運單認列 → 點收秤重 → 印務切終態並報工 → 運費關稅攤回外包成本 → 生管建轉交單送品檢站 → 品檢人員點收 → 進待驗清單
- [x] 7.4 mock 資料校準：貼近真實情境（業務格式編號）、涵蓋分次驗收、誤記沖銷、短出結案、異常回補、作廢回補、打樣重打、併櫃貨運單各情境的展示資料
- [x] 7.5 措辭清整：全庫「入庫數／QC 入庫數／入庫累計」改「完工良品數」、「印件數量」改「購買數量」；英文術語改中文
- [x] 7.6 `ACCEPTANCE.md` 續補段 3 驗收操作項（含行動版品檢頁以 `resize_window` 驗證步驟、貨運單頁面桌機驗證步驟）
- [x] 7.7 `openspec validate --strict` 全過；design.md § 另案處理 三項確認已登記、未混入本 change
