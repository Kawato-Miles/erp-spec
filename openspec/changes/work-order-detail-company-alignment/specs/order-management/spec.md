# order-management 規格差異檔：接單業務改名、印件屬性、印件備註可改角色與廠客引用

> 基準文字：訂單列表權限與可見範圍、製作後印件規格異動系統自動通知、新增印件欄位與必填檢核、加開印件、訂單階段印件規格編輯時機取自尚未歸檔的 change `delivery-date-naming-convergence`；其餘取自主 spec。

## ADDED Requirements

### Requirement: 訂單客戶欄引用廠客主檔

訂單的客戶欄 SHALL 指向 wiki [廠客](../../../memory/Sens_wiki/wiki/erp/05-entities/廠客.md) 主檔的一筆資料——客戶與廠商共用同一張主檔，同一家公司只有一筆。系統 SHALL NOT 另設獨立的客戶主檔。欄位表與身分交叉規則的正本見該 wiki 卡，本 spec SHALL NOT 複寫。

訂單上與客戶有關的顯示 SHALL 為兩項：客戶名稱（取廠客的廠客簡稱）與客戶編號（取廠客的客戶編號）。兩項皆為唯讀衍生顯示，訂單 SHALL NOT 另存欄位。窗口聯絡人 SHALL 自該廠客的聯絡窗口子表挑一筆。

可選為訂單客戶的廠客 SHALL 限客戶身分非「無」者；客戶身分的維護角色 SHALL 為 [業務](../../../memory/Sens_wiki/wiki/erp/03-roles/業務.md)，廠商身分的維護角色 SHALL 為 [印務主管](../../../memory/Sens_wiki/wiki/erp/03-roles/印務主管.md)。

**Priority**: P1

**Rationale**: 同一家公司常常既向我們下單、也承接我們的外發；兩張主檔會讓統一編號、聯絡窗口與付款條件各記一份，對帳時對不起來。訂單只顯示不存欄，是因為客戶改了簡稱之後，訂單上那份副本就是第二個真相。客戶編號帶到訂單與工單上顯示，是因為業務與印務對客戶的日常稱呼不一致，編號是唯一講得準的那個識別。

#### Scenario: 業務建訂單時挑廠客

- **GIVEN** 廠客主檔有一筆客戶身分為企客、廠商身分為外包的資料，客戶編號 MBC000006
- **WHEN** 業務在訂單挑定該廠客
- **THEN** 訂單 SHALL 顯示其廠客簡稱為客戶名稱、顯示客戶編號 MBC000006
- **AND** 窗口聯絡人 SHALL 自該廠客的聯絡窗口子表挑選

#### Scenario: 廠客改簡稱後訂單顯示隨之更新

- **GIVEN** 一張訂單的客戶為某廠客
- **WHEN** 業務於廠客主檔改寫該廠客的廠客簡稱
- **THEN** 訂單顯示的客戶名稱 SHALL 隨之改變
- **AND** 訂單 SHALL NOT 保留舊名的副本

#### Scenario: 客戶身分為無的廠客不可選為訂單客戶

- **GIVEN** 廠客主檔有一筆客戶身分為「無」、廠商身分為外包的資料
- **WHEN** 業務在訂單的客戶欄搜尋該筆
- **THEN** 系統 SHALL NOT 列為可選項

### Requirement: 接單業務改派的狀態約束

業務主管改派接單業務（諮詢單的負責諮詢人員、需求單 / 訂單的接單業務）SHALL 為「只換人」的管理動作，與單據業務狀態維度正交。

**改派 MUST NOT 觸發任何狀態轉移、MUST NOT 改變單據狀態**（不回退、不推進）。負責人欄位變更與狀態機運行完全獨立。

**終態禁改派**（各單據終態無後續業績產生，改派無意義）：

- 訂單：`已取消` 禁改派；`訂單完成` **例外可改派**（售後服務負責人衍生自訂單負責人，業務離職後完成訂單售後須能改派接手，見 [order-management § 訂單接單業務改派](../order-management/spec.md)）
- 需求單：`成交`（已轉訂單，改訂單）/ `流失` 禁改派
- 諮詢單：`已轉需求單` / `完成諮詢` / `已取消` 禁改派；`待諮詢`（含已認領）可改派且維持狀態（不回退）

**Priority**: P0

**Rationale**: 改派負責人是管理動作，若連帶觸發狀態轉移會導致訂單流程異常（如已進入製作的訂單被回退），必須與狀態機運行完全解耦。

#### Scenario: 改派不觸發狀態轉移

- **GIVEN** 任一單據處於允許改派的狀態
- **WHEN** 業務主管改派該單據接單業務
- **THEN** 系統 SHALL 僅更新負責人欄位
- **AND** 單據狀態 MUST 維持改派前狀態（不回退、不推進、不觸發任何狀態機轉移）

#### Scenario: 訂單完成狀態因售後繼承例外允許改派

- **GIVEN** 訂單 `status = 訂單完成`
- **WHEN** 業務主管改派該訂單接單業務
- **THEN** 系統 SHALL 允許改派（未取消即可）
- **AND** 訂單狀態 MUST 維持「訂單完成」
- **AND** 該訂單後續售後事件接單業務 SHALL 衍生為新負責人

#### Scenario: 各單據終態禁止改派

- **GIVEN** 訂單 `已取消` / 需求單 `成交` 或 `流失` / 諮詢單 `已轉需求單`、`完成諮詢` 或 `已取消`
- **WHEN** 業務主管開啟該單據詳情頁
- **THEN** 「改派負責人」入口 SHALL disabled

### Requirement: 訂單接單業務改派

業務主管 SHALL 可於訂單詳情頁改派訂單接單業務。改派為改負責人的管理動作，與「分享」（不改負責人、業務可做、US-ORD-004）為兩種獨立機制。改派的通用規則（理由分類五值必填、五要素留痕、候選人以 Role 模組權限篩選、全公司範圍、改派不改狀態）沿用 wiki [業務主管](../../../memory/Sens_wiki/wiki/erp/03-roles/業務主管.md) § 主要職責「改派接單業務」與 wiki [訂單接單業務改派](../../../memory/Sens_wiki/wiki/erp/07-scenarios/訂單接單業務改派.md)。本 Requirement 定義訂單模組特有規則。

**允許改派狀態**：訂單狀態非「已取消」即可改派（含已完成），對齊「訂單未取消即可編輯」粗粒度規則。理由：售後服務的負責人衍生自訂單負責人，業務離職後完成訂單的售後須能改派接手。已取消訂單禁改派（無售後意義）。

**售後負責人衍生**：售後服務的接單業務衍生自訂單接單業務（非獨立欄位）。改派訂單接單業務 SHALL 連帶轉移該訂單後續售後事件的歸屬至新接單業務。

**電商訂單防呆**：電商來源訂單若無接單業務（接單業務為空），改派入口 SHALL disabled（電商訂單負責人機制後階段才開發）。

**Priority**: P0

**Rationale**: 業務離職或工作負荷調整時訂單須能改派接手，售後負責人衍生自訂單負責人，改派連帶轉移確保售後不斷線。

#### Scenario: 業務主管改派進行中訂單接單業務

- **GIVEN** 訂單狀態非「已取消」、有接單業務
- **WHEN** 業務主管點「改派負責人」、選新負責人（候選 = 具訂單管理權限的使用者）、必選理由分類、確認
- **THEN** 系統 SHALL 更新接單業務為新負責人
- **AND** SHALL 寫入活動紀錄五要素（原 / 新負責人、改派時間、理由分類與補述、操作主管）
- **AND** MUST NOT 改變訂單狀態

#### Scenario: 業務主管改派已完成訂單（售後歸屬連帶轉移）

- **GIVEN** 訂單狀態 = 訂單完成、原業務已離職
- **WHEN** 業務主管以理由分類「離職交接」改派接單業務給接手業務
- **THEN** 系統 SHALL 允許改派（完成訂單未取消）
- **AND** 該訂單後續售後事件的接單業務 SHALL 衍生為新負責人
- **AND** SHALL 寫入活動紀錄五要素

#### Scenario: 已取消訂單禁止改派

- **GIVEN** 訂單狀態 = 已取消
- **WHEN** 業務主管開啟該訂單詳情頁
- **THEN** 「改派負責人」入口 SHALL disabled
- **AND** SHALL 顯示提示（已取消訂單無法改派）

#### Scenario: 電商訂單無接單業務時改派 disabled

- **GIVEN** 電商來源訂單接單業務為空
- **WHEN** 業務主管開啟該訂單詳情頁
- **THEN** 「改派負責人」入口 SHALL disabled
- **AND** SHALL 顯示提示（電商訂單尚無接單業務，無法改派）

#### Scenario: 改派理由分類驅動分享成員與通知

- **WHEN** 業務主管以不同理由分類改派訂單
- **THEN** 理由 = 離職交接時，系統 SHALL 清空該訂單既有分享成員、且 SHALL NOT 通知原負責人（已離職）
- **AND** 理由 ∈ {長假代理、工作負荷平衡、客戶要求換窗口、其他} 時，系統 SHALL 保留既有分享成員、且 SHALL 知會原負責人
- **AND** 任一理由皆 SHALL 通知新負責人（Slack mock + 活動紀錄）

## MODIFIED Requirements

### Requirement: 訂單列表權限與可見範圍

訂單列表 SHALL 依使用者角色決定可見訂單範圍：業務與諮詢（業務平台語境）SHALL 僅見自己負責（訂單接單業務等於當前使用者）或被分享（分享成員含當前使用者）的訂單；訂單管理人與業務主管（中台語境）SHALL 見全公司訂單。此範圍過濾 SHALL 一併套用於印件子層——印件子層可見的印件繼承過濾後的訂單範圍，業務 SHALL NOT 透過子層看到其他業務未分享訂單的印件。

分享機制同需求單（授予他人檢視/編輯權限）。訂單分享成員的維護沿用訂單詳情頁既有分享機制，列表層僅消費該資料做過濾。

訂單列表 SHALL 在角色可見訂單集之上，提供多維度篩選（業務負責人、訂單狀態、訂單類型、付款狀態、售後狀態、關鍵字）與一鍵清空篩選。

**訂單列表 SHALL NOT 顯示任何交期欄位、SHALL NOT 提供任何交期篩選**：訂單層已無交期欄（見 § 印件內部完成日推導 § 輸入來源），交期的唯一事實在印件層。系統 SHALL NOT 以旗下印件的預計出貨日聚合出訂單層的交期欄供列表顯示或篩選。

**Priority**: P0

**Rationale**: 業務只能看到自己負責的訂單保護客戶資料不外洩，主管能看全公司訂單以做總覽管理，權限過濾連帶印件子層防止側漏。列表拿掉交期顯示與篩選，是因為交期已下沉到印件層：留一個聚合出來的訂單層日期，業務改了某一件印刷品的預計出貨日，列表篩出來的結果就和實際承諾對不上；要按交期找活，該去印件層的清單找。

#### Scenario: 業務僅見自己負責與被分享的訂單

- **WHEN** 業務角色開啟訂單列表
- **THEN** 列表僅顯示該業務負責的訂單，以及被分享給該業務的訂單；其他業務的未分享訂單不顯示

#### Scenario: 諮詢角色比照業務套用範圍過濾

- **WHEN** 諮詢角色開啟訂單列表
- **THEN** 套用與業務相同的範圍過濾（自己負責 ∪ 被分享）

#### Scenario: 訂單管理人查看全公司訂單

- **WHEN** 訂單管理人登入查看訂單列表
- **THEN** 系統顯示全公司所有訂單

#### Scenario: 業務主管見全公司訂單

- **WHEN** 業務主管角色開啟訂單列表
- **THEN** 列表顯示全公司所有訂單，不套用角色範圍過濾

#### Scenario: US-ORD-009 訂單管理人全公司訂單檢視與篩選

- **WHEN** 訂單管理人登入系統後進入訂單列表
- **THEN** 系統 SHALL 預設顯示全公司所有訂單；業務角色登入時 SHALL 預設僅顯示自己負責的訂單
- **AND** 系統 MUST 提供下列篩選維度，作用於「當前角色可見訂單集」：業務負責人、訂單狀態、訂單類型、付款狀態、售後狀態、關鍵字（訂單編號 / 客戶名稱 / 案名 / 印件名稱 / 印件編號）
- **AND** 「業務負責人」篩選 SHALL 對當前可見訂單集依訂單接單業務收斂（不擴大可見範圍）
- **AND** 「訂單狀態」篩選選項 SHALL 涵蓋所選「訂單類型」適用的**完整**狀態集（前段依類型分流 + 共用段 + 終態，對齊訂單狀態機），**不限於現有資料已出現的狀態**；線下單 = 線下前段 + 共用段 + 已取消、線上單與客製單 = 線上前段 + 共用段 + 已取消、諮詢訂單僅終態（訂單完成 / 已取消、不含共用段）；未選類型時涵蓋全部；切換訂單類型 SHALL 重置已選訂單狀態
- **AND** 「付款狀態」篩選 SHALL 以訂單層付款狀態（未付款 / 部分付款 / 已付款 / 已退款）為比較基準

#### Scenario: 訂單列表不提供交期篩選

- **GIVEN** 訂單管理人開啟訂單列表的篩選面板
- **WHEN** 檢視可用的篩選維度
- **THEN** 面板 SHALL NOT 出現交期起訖區間或任何交期相關的篩選項
- **AND** 列表欄位 SHALL NOT 出現訂單層的交期欄

#### Scenario: 角色範圍過濾套用於印件子層

- **WHEN** 業務展開訂單列表的印件子層
- **THEN** 僅能展開其可見訂單（自己負責或被分享）的印件子層，無法看到其他業務未分享訂單的印件

#### Scenario: 清空訂單列表篩選

- **WHEN** 使用者在訂單列表點擊「清空篩選」
- **THEN** 系統 SHALL 將所有篩選條件（含關鍵字）重置為預設，並回到列表第一頁

#### Scenario: US-ORD-004 訂單分享與代理授權

- **WHEN** 業務在訂單詳情頁進入「權限管理」Tab，搜尋同事並授予「檢視」或「編輯」權限
- **THEN** 被授權同事 SHALL 可依授予的權限等級存取該訂單；分享機制 MUST 與需求單權限管理機制一致

### Requirement: 訂單備註

訂單 SHALL 將備註拆為三類：客戶備註（線上單）、內部備註、製作備註（線下單），依訂單來源條件顯示。另新增三個訂單階段備註欄位（訂單須知 / 交貨備註 / 付款備註），各 500 字上限，與既有 5 個備註欄位並存。編輯權限對齊 user-roles spec 粗粒度模組權限，已取消訂單鎖定唯讀。

**Priority**: P0

**Rationale**: 備註分為客戶端、內部、製作三類避免混淆，搭配訂單階段備註（訂單條件 / 交貨條件 / 付款條件）讓報價單附註與內部溝通各有歸處。

#### Scenario: 線上單顯示客戶端備註

- **WHEN** 訂單為線上單
- **THEN** 訂單詳情頁 SHALL 顯示客戶備註（唯讀，自 EC 帶入）與內部備註（可編輯）
- **AND** MUST NOT 顯示製作備註

#### Scenario: 線下單顯示製作備註

- **WHEN** 訂單為線下單
- **THEN** 訂單詳情頁 SHALL 顯示內部備註（可編輯）與製作備註（可編輯）
- **AND** MUST NOT 顯示客戶備註

#### Scenario: 內部備註客戶端不可見

- **WHEN** EC 前台或任何客戶端介面查詢訂單
- **THEN** 系統 MUST NOT 對外提供內部備註內容

**訂單階段備註（訂單須知 / 交貨備註 / 付款備註）**

欄位正本：[wiki 訂單實體卡](../../../memory/Sens_wiki/wiki/erp/05-entities/訂單.md) § 備註。

三個欄位皆為 optional，預設空字串。儲存時 SHALL trim 前後空白；長度超過 500 字 SHALL 由 UI 顯示警告，但 spec 不強制截斷。

三個欄位與既有 5 個備註欄位（收款條件備註 / 付款明細 / 客戶備註 / 內部備註 / 製作備註）並存，**MUST NOT 合併或覆蓋既有欄位**。

#### Scenario: 新訂單初始化三個備註欄位為空

- **WHEN** 新訂單建立
- **THEN** 訂單須知 / 交貨備註 / 付款備註 SHALL 預設為空字串

#### Scenario: 三個欄位獨立儲存

- **GIVEN** 訂單須知 = "已知悉印刷須知"
- **AND** 交貨備註 = "預估 15-18 工作天"
- **AND** 付款備註 = "全額付款 3-5 工作天"
- **WHEN** 業務僅更新付款備註為新內容
- **THEN** 系統 SHALL 只更新付款備註欄位
- **AND** 訂單須知與交貨備註保持原值

**編輯權限與時機**

訂單階段的三個備註欄位（訂單須知 / 交貨備註 / 付款備註）編輯權限 SHALL 對齊各角色的模組權責（正本見 wiki [03-roles](../../../memory/Sens_wiki/wiki/erp/03-roles/)）。各角色的編輯權限 MUST 依下表配置：

| 角色                                                                   | 訂單模組粗粒度     | 訂單備註編輯權限                    | 備註                                                                                              |
| -------------------------------------------------------------------- | ----------- | --------------------------- | ----------------------------------------------------------------------------------------------- |
| Supervisor                                                           | R/W         | **唯讀模式**                    | 沿用 user-roles spec § Supervisor 角色行為限制（line 112-125），所有編輯按鈕 disabled                            |
| 訂單管理人                                                                | R/W         | 可編輯                         | —                                                                                               |
| 業務                                                                   | R/W         | 可編輯（限接單業務為自己的訂單或被分享編輯權限的訂單） | —                                                                                               |
| 諮詢                                                                   | R/W         | 可編輯（沿用業務範圍規則）               | —                                                                                               |
| 會計                                                                   | R/W（細粒度為讀取） | **唯讀**                      | 沿用 user-roles spec § 會計角色職責「報價單 / 訂單模組（讀取）+ 對帳檢視」                                               |
| 業務主管                                                                 | X           | **無權限編輯**                   | v1.13 移除既有「業務主管代編訂單備註」破例（對齊 user-roles spec 業務主管訂單模組 X 邊界）；如需協助由 Supervisor 重新指定訂單業務主管或業務分享編輯權限 |
| 其他角色（審稿主管 / 審稿 / 印務主管 / 印務 / 生管 / 師傅 / QC / 出貨 / 外包 / 中國廠商 / EC商品管理） | X           | 路由禁止進入                      | —                                                                                               |

編輯時機 SHALL 遵守以下規則：

- 訂單未取消（訂單狀態非「已取消」）：三個欄位皆可編輯（v1.13 放寬：不再受訂單完成時間限制；訂單完成後仍可編輯）
- 訂單已取消（訂單狀態 = 已取消）：三個欄位 SHALL 鎖定為唯讀，避免取消後改備註影響歷史對帳

對應「插入常用備註」按鈕 SHALL 與 textarea 編輯權限同步（textarea 唯讀時按鈕 disabled）。

#### Scenario: 業務於訂單未完成階段編輯訂單備註

- **GIVEN** 訂單狀態 = 製作等待中
- **AND** 使用者為訂單接單業務
- **WHEN** 業務點訂單備註 section 右上「編輯」按鈕並於 dialog 內修改
- **THEN** 系統 SHALL 允許編輯並於儲存後寫入

#### Scenario: 業務於訂單完成後編輯訂單備註（v1.13 放寬）

- **GIVEN** 訂單狀態 = 訂單完成、訂單完成時間非空
- **AND** 使用者為業務 / 諮詢 / 訂單管理人
- **WHEN** 該角色於訂單詳情頁查看訂單備註 section
- **THEN** Section header 右上「編輯」按鈕 SHALL 啟用
- **AND** 角色 SHALL 可開啟訂單備註編輯 Dialog 並修改三個欄位
- **AND** 活動紀錄 MUST 記錄變更內容

#### Scenario: 已取消訂單鎖定訂單備註

- **GIVEN** 訂單狀態 = 已取消
- **WHEN** 業務於訂單詳情頁查看訂單備註 section
- **THEN** Section header 右上的「編輯」按鈕 SHALL disabled
- **AND** Section header 右側 SHALL 顯示鎖定原因（「訂單已取消，無法編輯」）
- **AND** Section body 仍以 read-only 顯示既有填寫內容

#### Scenario: 業務主管查看訂單備註（v1.13 移除代編破例）

- **GIVEN** 訂單狀態 = 製作中
- **AND** 使用者為業務主管
- **WHEN** 業務主管於訂單詳情頁查看訂單備註 section
- **THEN** Section header 右上的「編輯」按鈕 SHALL disabled 或不顯示
- **AND** 業務主管 MUST NOT 透過任何路徑修改三個欄位（對齊 user-roles spec 業務主管訂單模組 X 邊界）
- **AND** 如需協助補備註，業務主管 SHALL 透過 Supervisor 重新指定訂單業務主管或請業務分享編輯權限

#### Scenario: Supervisor 查看訂單備註

- **GIVEN** 使用者為 Supervisor
- **WHEN** Supervisor 進入訂單詳情頁
- **THEN** 訂單備註 section 編輯按鈕 SHALL disabled（沿用 user-roles spec § Supervisor 角色行為限制）
- **AND** Section body 仍以 read-only 顯示

#### Scenario: 會計查看訂單備註

- **GIVEN** 使用者為會計
- **WHEN** 會計進入訂單詳情頁
- **THEN** 訂單備註 section 編輯按鈕 SHALL disabled（沿用 user-roles spec § 會計角色職責「報價單 / 訂單模組（讀取）」）
- **AND** Section body 仍以 read-only 顯示

### Requirement: 製作後印件規格異動系統自動通知

訂單狀態 ∈ 製作後狀態（製作等待中 / 工單已交付 / 製作中 / 製作完成 / 出貨中 / 訂單完成）時，業務於 Side Panel 編輯印件規格類欄位（規格備註 / 購買數量 / 單位 / 難易度）並儲存後，系統 SHALL 自動推送通知 + 寫入活動紀錄，承擔印務感知責任。

**規格類欄位 SHALL NOT 含急件選項與預計出貨日**：兩者的變更走 § 印件急件選項與預計出貨日變更留痕與同步（只留痕與同步非終態工單，SHALL NOT 通知任何角色），系統 SHALL NOT 對急件選項或預計出貨日變更推送本 Requirement 的通知。

**通知對象**（標準路徑）：

- 工單負責印務（印件關聯工單的負責印務對應使用者）
- 印務主管
- 訂單管理人

**Fallback 規則**（訂單管理人為空時）：通知對象退化為「接單業務 + 工單負責印務 + 印務主管」。理由：業務是訂單建立者，對訂單異動有第一手感知責任。

**通知形式**：in-app 通知（既有通知元件複用），通知內容含：

- 操作者姓名
- 印件編號 + 名稱
- 變更欄位 diff 摘要（before → after）
- 跳轉印件詳情 link

**活動紀錄**：

- 事件類型 = 製作後印件規格異動
- payload 含：變更前欄位值快照、變更後欄位值快照、實際通知對象（user_id 陣列，含 fallback 觸發紀錄）、操作者（user_id）

**何時不觸發通知**：

- 訂單狀態 ∈ 製作前狀態：直接更新印件，不推通知（無印務介入需要）
- 訂單狀態 = 已取消：所有編輯禁止（按鈕 disabled），不會觸發通知
- 編輯純金額欄位（單價；不影響要做多少）：不推通知
- 編輯急件選項或預計出貨日：走 § 印件急件選項與預計出貨日變更留痕與同步，本段不重複推送，且該二欄 SHALL NOT 通知任何角色

**Priority**: P0

**Rationale**: 製作階段業務改印件規格若印務不知情，會導致生產與規格不一致。系統自動通知承擔感知責任，免靠人工轉告。購買數量留在規格類欄位，是因為改數量直接牽動現場要做多少；只改單價不動生產內容，印務不必收到通知。急件選項與預計出貨日切出去自己一條、且不通知任何角色，是因為兩者的下游只是重推內部完成日並同步非終態工單，跟這裡規格類與金額類「印務不知情會做錯」的風險不同，不必額外通知。

#### Scenario: 製作中業務改 spec_note 觸發三組通知

- **GIVEN** 訂單 SO-001 狀態 = 製作中
- **WHEN** 業務於 Side Panel 修改某印件的規格備註並儲存
- **THEN** 系統 SHALL 推送 in-app 通知給工單負責印務、印務主管與訂單管理人
- **AND** 系統 SHALL 寫入活動紀錄（事件類型 = 製作後印件規格異動，含變更前後快照與實際通知對象）

#### Scenario: 製作中業務只改單價不推通知

- **GIVEN** 訂單 SO-001 狀態 = 製作中
- **WHEN** 業務於 Side Panel 只修改某印件的單價並儲存
- **THEN** 系統 SHALL 更新單價與售價、應收即時重算
- **AND** 系統 SHALL NOT 推送製作後印件規格異動通知

### Requirement: 新增印件欄位與必填檢核

訂單處於**非終態**時，業務 SHALL 可於訂單詳情頁的印件清單以空白建立方式新增一筆印件（相對於 § 加開印件（複製原印件規格）的複製路徑）。新增當下 SHALL 收齊下列欄位：

**必填（七欄）**：印件名稱、生產類型（印件屬性）、購買數量、單位、單價、急件選項、難易度。

**選填（八欄）**：預計出貨日（**空白、SHALL NOT 帶任何預設值**）、規格備註、出貨方式（預設帶入所屬訂單的出貨方式）、包裝備註、預計產線、免審稿（預設否）、稿件備註、印件配方。

七項必填欄位任一為空 SHALL 不得存檔，畫面 SHALL 逐欄提示；這是欄位必填檢核，SHALL NOT 視為新增審核關卡。預計出貨日為選填，留空 SHALL 可存檔。

新印件走既有正常流程：審稿維度初值 SHALL 為「待分派」（勾選免審稿者依既有免審規則直接為「合格」，見 § 多印件管理）、印製維度初值 SHALL 為「等待中」；之後由業務補上稿件、上傳走審稿，再由訂單管理人確認製作細節推進「製程已確認」並建立工單。本入口的把關 SHALL 沿用 § 訂單階段印件規格編輯時機——訂單非終態即可，SHALL NOT 因訂單已進入製作段而擋下；訂單進入終態後 SHALL NOT 提供本入口。

**Priority**: P0

**Rationale**: 新增印件與複製加開共用同一套規格欄位，收齊七項必填才能讓現場拿到完整可投產的規格。預計出貨日選填且不給預設值，是因為訂單層已無交期欄可取，而拿同訂單另一件印刷品的日期頂著等於替業務假設一個沒談過的承諾日；業務常常還沒跟客戶談定個別交期就要先把印件建起來卡位，空白才誠實。

#### Scenario: 業務新增印件收齊七項必填

- **GIVEN** 業務在訂單 SO-001 的印件清單點擊新增印件
- **WHEN** 業務填入印件名稱、生產類型、購買數量、單位、單價、急件選項、難易度並存檔
- **THEN** 系統 SHALL 建立新印件，審稿維度為「待分派」、印製維度為「等待中」

#### Scenario: 七項必填任一為空擋下存檔

- **GIVEN** 業務新增印件已填六項必填欄位、難易度留空
- **WHEN** 業務嘗試存檔
- **THEN** 系統 SHALL 擋下並於難易度欄位提示必填

#### Scenario: 預計出貨日無預設值、空白手填

- **GIVEN** 訂單 SO-001 旗下已有一件預計出貨日為 2026-09-20 的印件
- **WHEN** 業務開啟新增印件面板
- **THEN** 預計出貨日欄位 SHALL 為空白
- **AND** 系統 SHALL NOT 帶入 2026-09-20 或任何其他預設值
- **AND** 業務 MAY 逐件手填，留空亦 SHALL 可存檔

#### Scenario: 終態訂單不提供新增印件入口

- **GIVEN** 一張狀態為「訂單完成」或「已取消」的訂單
- **WHEN** 業務開啟訂單詳情頁的印件清單
- **THEN** 系統 SHALL NOT 提供新增印件入口

### Requirement: 加開印件（複製原印件規格）

訂單處於**非終態**時，業務 SHALL 可於訂單詳情頁的印件清單選一張既有印件、複製其規格加開一張新印件（用於同一款再買一批，以及訂單異動加印追加核可後把那一件開出來）。加開印件 SHALL 為可選路徑，SHALL NOT 為客戶加量的唯一承載方式——加量亦可直接調高原印件購買數量（見 § 訂單階段印件規格編輯時機 階段一）。

**帶入與不帶入**：新印件 SHALL 帶入來源印件的規格側內容（印件屬性、單位、單價、規格備註、包裝備註、稿件備註、印件配方、難易度、預計產線、免審稿），業務 MAY 於加開當下改寫印件名稱與規格備註。**購買數量 SHALL 留空、必填；預計出貨日 SHALL 留空、選填，系統 SHALL NOT 帶入來源印件的值、SHALL NOT 取任何預設值；稿件與審稿輪次歷史 SHALL NOT 繼承**；售價為衍生值，SHALL 於業務填入購買數量後由單價 × 購買數量取整算出。**來源印件 SHALL NOT 被本動作改動**。**已棄用的印件 SHALL NOT 列為可複製來源**。

**新印件走既有的正常流程、SHALL NOT 另設路徑**：審稿維度初值 SHALL 為「待分派」（勾選免審稿者依既有免審規則直接為「合格」，見 § 多印件管理）、印製維度初值 SHALL 為「等待中」；之後由業務補上購買數量與預計出貨日、上傳稿件走審稿，再由訂單管理人確認製作細節推進「製程已確認」並建立工單（見 § 印件印製維度狀態機）。系統 SHALL NOT 為加開的印件另立印件屬性（加開不形式化為列舉值）。

**把關**：本入口 SHALL 沿用既有印件新增與編輯的分界——訂單非終態即可（見 § 訂單階段印件規格編輯時機 階段一），SHALL NOT 另設條件、SHALL NOT 因訂單已進入製作段而擋下；訂單進入終態（訂單完成／已取消）後 SHALL NOT 提供本入口。加開造成的應收增加 SHALL 由新印件的售價直接反映；整筆訂單一起處理的金額增減走訂單異動（兩路並行的判準見 [order-adjustment spec](../order-adjustment/spec.md)）。

**Priority**: P1

**Rationale**: 同一款再買一批時，業務只能從空白重打一遍規格——單位、單價、配方、難易度、預計產線逐欄重填，填漏一欄現場就做出規格不同的貨，所以要有複製規格的入口。複製規格而不繼承數量、交期與稿件，是因為這三件是「這一批」專屬的：繼承數量會讓人以為加開的量已經填好，繼承稿件則會把上一批的定稿直接當成這一批的定稿、跳過審稿；預計出貨日留空且不給預設值同理，這一批的交貨日要業務重新跟客戶談，沿用上一批的承諾日或拿一個系統湊出來的日期頂著，都會讓現場照一個沒人談過的日期趕工。不列已棄用印件當來源，是因為那一款已經判定不做了，拿它當範本會把錯的規格帶下去。加開不再是加量的唯一路徑，是因為客戶買的還是同一款貨，硬要在訂單上裂成兩筆會讓報價單與客戶認知對不上。

#### Scenario: 業務複製既有印件規格加開新印件

- **GIVEN** 一張狀態為「製作中」的訂單含印件 PI-2026-0388（購買數量 500、單位 張、單價 100、預計出貨日 2026-09-05、規格備註與預計產線皆已填）
- **WHEN** 業務選該印件執行加開、填寫新印件名稱
- **THEN** 系統 SHALL 建立一張新印件並帶入來源印件的印件屬性、單位、單價、規格備註、包裝備註、稿件備註、印件配方、難易度與預計產線
- **AND** 新印件的購買數量與預計出貨日 SHALL 為空、SHALL NOT 帶入稿件與審稿輪次歷史
- **AND** 新印件的預計出貨日 SHALL NOT 帶入來源印件的 2026-09-05
- **AND** 新印件審稿維度 SHALL 為「待分派」、印製維度 SHALL 為「等待中」
- **AND** 來源印件的購買數量 SHALL 維持 500
- **AND** 活動紀錄 MUST 記錄本次加開與所複製的來源印件

#### Scenario: 已棄用印件不列為可複製來源

- **GIVEN** 一張訂單含一件印製維度為「已棄用」的印件與一件正常印件
- **WHEN** 業務開啟加開印件入口
- **THEN** 可複製來源 SHALL 只列該正常印件
- **AND** 已棄用的印件 SHALL NOT 可被選為來源

#### Scenario: 終態訂單不提供加開印件入口

- **GIVEN** 一張狀態為「訂單完成」或「已取消」的訂單
- **WHEN** 業務開啟訂單詳情頁的印件清單
- **THEN** 系統 SHALL NOT 提供加開印件入口
- **AND** 金額增減 SHALL 改走訂單異動（見 § 訂單階段印件規格編輯時機 階段二）

### Requirement: 訂單階段印件規格編輯時機

訂單階段的印件規格備註 / 購買數量 / 單位 / 難易度 / 免審稿 / 急件選項 / 預計出貨日 / 單價的可編輯性 SHALL 依訂單狀態 **以「訂單完成 / 已取消」終態為界**區分兩階段（明細時點分界 ＝ 訂單狀態終態集合）。**預計出貨日另受印件本身印製維度約束**：印製維度為「已棄用」的印件，其預計出貨日 SHALL 唯讀，即使所屬訂單仍為非終態。印件售價 SHALL 為單價 × 購買數量四捨五入至整數元的唯讀衍生值，SHALL NOT 直接編輯（計價規則正本見 wiki [報價邏輯](../../../memory/Sens_wiki/wiki/erp/04-business-logic/營運規則/售前/報價邏輯.md)）。

**階段一：訂單未進入終態（status ∉ {訂單完成, 已取消}）**

業務 / 諮詢 / 訂單管理人 SHALL 可於訂單詳情頁印件清單操作欄點「編輯印件」開啟印件編輯面板直接編輯下列欄位：規格備註 / 購買數量 / 單位 / 難易度 / 免審稿 / 急件選項 / 預計出貨日 / 單價（**含調降與加量，全程可改、SHALL NOT 因進入製作段而唯讀**）。系統 SHALL 直接更新印件 / 訂單明細對應值，印件售價（單價 × 購買數量，取整）與訂單應收即時重算；活動紀錄 MUST 記錄變更內容（before / after）。**難易度 / 免審稿屬審稿相關欄位，其審稿生效時點另依規則**（已分派後次一輪生效；審稿維度到達「已確認可製作」後改之無實質效果——見 § 多印件管理 § 免審可編輯性、prepress-review § 印件自動分配機制 § 分配後的維持與重派）。

- **數量異動重算並提示確認單價**：購買數量變更時系統 SHALL 以既有單價重算印件售價（取整），並 SHALL 提示業務確認單價是否跟著調整；業務改單價後售價 SHALL 再算一次。系統 SHALL NOT 靜默沿用原單價而不提示。
- **明細加量 SHALL NOT 回寫已建立的工單**：訂單側調整購買數量或新增印件 SHALL NOT 改動既有工單的每份印件生產數量與目標數量。系統 SHALL 以「印件購買數量 − 該印件跨工單目標數量加總」的差額事實提示印務，由印務在原工單發起工單異動（數量調整或加開生產任務）承接（規格見 [work-order spec § 訂單加量缺口以工單異動承接](../work-order/spec.md)、分流正本見 wiki [工單](../../../memory/Sens_wiki/wiki/erp/05-entities/工單.md) § 建立來源）。加開印件仍為「同一款再買一批」的入口，SHALL NOT 為加量的唯一路徑（見 § 加開印件（複製原印件規格））。
- **急件選項全程可改**：急件選項 SHALL 於本階段全程可改（含製作段），SHALL NOT 因進入製作段而鎖定——趕件的需求常在製作開始後才由客戶提出。改動 SHALL 觸發重推導、同步與留痕（見 § 印件急件選項與預計出貨日變更留痕與同步），推導出的內部完成日與非終態工單的內部完成日 SHALL 隨之更新；系統 SHALL NOT 通知任何角色。
- **預計出貨日全程可改**：預計出貨日 SHALL 於本階段全程可改（含製作段），SHALL NOT 因進入製作段而鎖定，直到該印件印製維度轉為「已棄用」為止。改動 SHALL 觸發重推導、同步與留痕（見 § 印件急件選項與預計出貨日變更留痕與同步），推導出的內部完成日與非終態工單的內部完成日 SHALL 隨之更新；系統 SHALL NOT 通知任何角色。
- **規格備註與包裝備註的可編輯角色 SHALL 另含印務**：訂單非終態期間，負責該印件任一張非終態工單的印務、其編輯（代理）層級的分享成員 SHALL 可改這兩欄（分享機制正本見 wiki [單據分享與職務代理](../../../memory/Sens_wiki/wiki/erp/07-scenarios/單據分享與職務代理.md)）。把關條件與製程說明、品檢需求同一套（見 § 印件製程說明與品檢需求）。一件印件的多張工單分屬不同印務時，每位負責人皆 SHALL 可改。
- **編輯入口 SHALL 有兩處、寫入同一筆印件事實**：印件詳情頁的印件編輯面板、工單詳情的印件備註側板。工單詳情的側板 SHALL 依角色鎖欄——印務開啟時規格備註、包裝備註與預計產線可改，稿件備註 SHALL 為唯讀。**稿件備註 SHALL 維持只有業務與 B2C 會員可改**（該欄寫給審稿人員，影響審稿判讀）；**預計產線 SHALL 由印務維護**。
- **金額調降留痕**：單價或購買數量調降（致印件售價減少）時，系統 MUST 寫活動紀錄事件類型「訂單完成前金額調降」（含時間 / 操作者 / before-after / 印件識別）。調降為弱把關（不阻擋、不送業務主管核可；主管事後可於活動紀錄查見）。
- **製作後規格異動通知**：製作後（訂單狀態 ∈ 製作等待中 / 工單已交付 / 製作中 / 製作完成 / 出貨中）業務於 Side Panel 編輯規格類欄位（含金額類）時，系統 SHALL 推送 in-app 通知 + 寫活動紀錄（詳見 § Requirement: 製作後印件規格異動系統自動通知；急件選項與預計出貨日不適用本條，見該 Requirement 的排除規則）。

**階段二：訂單進入終態（status ∈ {訂單完成, 已取消}）**

所有印件欄位（含金額類、急件選項與預計出貨日）MUST 為唯讀；金額異動一律走「訂單異動」Tab 建立訂單異動單（補收正 / 退款負）。訂單完成後的退款訂單異動經售後服務單容器；補收訂單異動為訂單完成後對客戶加收（極少，公司多吸收）。

**權責邊界**：

- 業務 / 諮詢：負責訂單中的資訊（含印件金額與規格）— 訂單詳情頁 Side Panel 為單一寫入入口
- 印務：負責工單中的資訊（生產任務 / 製程 / 材料規格）— 工單異動流程不寫回印件規格

**Priority**: P0

**Rationale**: 以訂單完成為分界——完成前業務可直接改印件規格與金額（含調降留痕與加量），完成後走訂單異動流程。製作段的加量改回由購買數量直接承載，是因為業務要記的就是「這張訂單客戶買了幾份」，硬要業務為加量另開一件印件會讓同一款貨在訂單上裂成兩筆、報價單與客戶認知對不上。訂單側改了數量而工單不跟著動，缺口就用差額事實提示印務、由印務在原工單發起異動承接——現場已經印完的那批製作帳保持不動，新增的量走與品檢缺口補做、超放損同一條承接路徑。鎖定點切在訂單完成，是因為報價單不會再重新給客戶，完成後改明細客戶手上那份就對不起來。預計出貨日與急件選項同納入全程可改、不通知——這兩欄的下游只是重推內部完成日並同步非終態工單，印務靠工單列表自行察覺即可。規格備註與包裝備註加開給印務，是因為現場發現規格寫得不清楚時，人正在工單頁；要他打電話請業務改，實際上改不到、或改完沒人知道。稿件備註不一併開放，是因為那一欄寫給審稿人員看，印務改了會讓審稿判讀依據無聲變動。

#### Scenario: 製作中客戶加量直接調高購買數量、缺口提示印務

- **GIVEN** 訂單 SO-001 狀態 ＝ 製作中，印件 PI-2026-0388 購買數量 ＝ 500、單價 ＝ 100（售價 50,000），其工單目標數量已依 500 建立且製作中
- **WHEN** 業務把該印件購買數量改為 800
- **THEN** 系統 SHALL 接受變更、以既有單價重算售價 ＝ 80,000、訂單應收即時 +30,000
- **AND** 系統 SHALL 提示業務確認單價是否調整
- **AND** 既有工單的目標數量與每份印件生產數量 SHALL NOT 被改寫
- **AND** 系統 SHALL 以差額 300 的事實提示印務，由印務在原工單發起工單異動承接
- **AND** 活動紀錄 MUST 記錄變更（before 500 / after 800）

#### Scenario: 製作前購買數量仍可直接編輯

- **GIVEN** 訂單 SO-001 狀態 ＝ 已回簽、某印件購買數量 ＝ 500、單價 ＝ 100，尚未建立工單
- **WHEN** 業務於印件編輯面板改購買數量 ＝ 800
- **THEN** 系統 SHALL 直接更新印件，印件售價即時重算 ＝ 80,000、訂單應收即時 +30,000
- **AND** 活動紀錄 MUST 記錄變更（before 500 / after 800）

#### Scenario: 製作中業務調整急件選項

- **GIVEN** 訂單 SO-001 狀態 ＝ 製作中，印件 PI-2026-0388 急件選項 ＝ 一般件
- **WHEN** 業務把該印件改為三天急件
- **THEN** 系統 SHALL 接受變更（急件選項 SHALL NOT 因製作段而唯讀）
- **AND** 該印件的內部完成日 SHALL 提前三天，其非終態工單的內部完成日 SHALL 同步
- **AND** 系統 SHALL 只留痕、SHALL NOT 通知負責印務

#### Scenario: 製作中業務調整印件預計出貨日

- **GIVEN** 訂單 SO-001 狀態 ＝ 製作中，印件 PI-2026-0388 預計出貨日 ＝ 2026-09-05、急件選項凍結天數 3（內部完成日 2026-09-01）
- **WHEN** 業務把該印件的預計出貨日改為 2026-09-12
- **THEN** 系統 SHALL 接受變更（預計出貨日 SHALL NOT 因製作段而唯讀）
- **AND** 該印件的內部完成日 SHALL 重推導為 2026-09-08，其非終態工單的內部完成日 SHALL 同步
- **AND** 系統 SHALL 只留痕、SHALL NOT 通知任何角色

#### Scenario: 製作中業務調降單價（含調降留痕）

- **GIVEN** 訂單 SO-001 狀態 ＝ 製作中、某印件單價 ＝ 100、購買數量 ＝ 500
- **WHEN** 業務於 Side Panel 改單價 ＝ 90（調降）
- **THEN** 系統 SHALL 直接更新印件，印件售價重算 ＝ 45,000、應收即時減少
- **AND** 系統 MUST 寫活動紀錄事件「訂單完成前金額調降」（含 before 100 / after 90 / 操作者 / 時間）
- **AND** 調降 MUST NOT 阻擋、MUST NOT 送業務主管核可（弱把關）

#### Scenario: 訂單完成後印件金額唯讀走 OA

- **GIVEN** 訂單 SO-001 狀態 ＝ 訂單完成
- **WHEN** 業務於印件清單嘗試編輯單價或購買數量
- **THEN** 系統 SHALL disabled 金額欄位、顯示 Tooltip「訂單已完成，金額變更需走『訂單異動』Tab」
- **AND** 金額異動 SHALL 走訂單異動單（補收 / 退款）

#### Scenario: 訂單完成後急件選項唯讀

- **GIVEN** 訂單 SO-001 狀態 ＝ 訂單完成
- **WHEN** 業務嘗試編輯某印件的急件選項
- **THEN** 系統 SHALL 擋下、該欄 SHALL 為唯讀
- **AND** 該印件的內部完成日 SHALL NOT 變動

#### Scenario: 訂單完成後印件預計出貨日唯讀

- **GIVEN** 訂單 SO-001 狀態 ＝ 訂單完成
- **WHEN** 業務嘗試編輯某印件的預計出貨日
- **THEN** 系統 SHALL 擋下、該欄 SHALL 為唯讀

#### Scenario: 印件已棄用後預計出貨日唯讀

- **GIVEN** 一件印件的印製維度為「已棄用」，其所屬訂單狀態為製作中（非終態）
- **WHEN** 業務嘗試編輯該印件的預計出貨日
- **THEN** 系統 SHALL 擋下、該欄 SHALL 為唯讀，即使所屬訂單仍為非終態

#### Scenario: 諮詢取消訂單（已取消終態）印件唯讀

- **GIVEN** 諮詢取消後諮詢訂單 status ＝ 已取消
- **THEN** 所有印件欄位 MUST 為唯讀（已取消為終態，與訂單完成同屬鎖定終態集合）

#### Scenario: 印務於工單詳情改規格備註，印件詳情同值

- **GIVEN** 一件印件旗下有工單甲與工單乙兩張製作中工單，印務甲為工單甲的負責人，所屬訂單為非終態
- **WHEN** 印務甲於工單甲詳情的印件備註側板改寫規格備註並存檔
- **THEN** 系統 SHALL 寫入該印件的規格備註
- **AND** 印件詳情頁與工單乙詳情 SHALL 顯示同一段文字
- **AND** 系統 SHALL NOT 於任一張工單保留獨立副本

#### Scenario: 印務於側板看不到稿件備註的編輯入口

- **GIVEN** 印務甲開啟工單詳情的印件備註側板
- **WHEN** 檢視可編輯欄位
- **THEN** 規格備註、包裝備註與預計產線 SHALL 可編輯
- **AND** 稿件備註 SHALL 為唯讀、SHALL NOT 提供編輯入口

#### Scenario: 訂單進終態後印務不可改兩欄

- **GIVEN** 一張狀態為訂單完成的訂單，其印件旗下仍有非終態工單
- **WHEN** 負責印務嘗試於工單詳情的側板改規格備註
- **THEN** 系統 SHALL 擋下、兩欄 SHALL 為唯讀


## REMOVED Requirements

### Requirement: 負責業務改派的狀態約束

**Reason**: 欄名「負責業務」全庫改為「接單業務」，Requirement 標題含該欄名；改標題後與舊標題無法比對，因此整條移除、另以新標題重寫。

**Migration**: 改派的狀態約束逐字不變，沿用至本檔 § ADDED Requirements 的「接單業務改派的狀態約束」，無資料遷移動作。

### Requirement: 訂單負責業務改派

**Reason**: 同上，標題含被改名的欄名。

**Migration**: 改派規則、售後負責人衍生與電商訂單防呆逐字不變，沿用至本檔 § ADDED Requirements 的「訂單接單業務改派」，無資料遷移動作。
