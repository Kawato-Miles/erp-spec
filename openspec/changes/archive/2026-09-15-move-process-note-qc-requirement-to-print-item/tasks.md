# 任務清單

> 實作範圍：erp repo `/Users/b-f-03-029/erp` 的 `apps/erp/src/app/(prototype)/`，MUST 經 skill `prototype-from-prompt`；只動 `(prototype)/` 目錄，共用元件與 app 層檔案不碰。
> 測試在 Sens repo `erp-prototype-tests/`，erp repo 不加測試檔。
> mock 異動順序固定：先改 `(prototype)/MOCK-DATA-CHAIN.md` → 再改各模組 `mock-data.js` → 最後改引用該鏈編號的測試。

## 1. OpenSpec 收尾

- [x] 1.1 執行 `openspec validate --changes`，三份規格差異檔（work-order、order-management、qc）皆通過；`openspec status` 顯示 proposal / design / specs 三項為 done。
- [x] 1.2 **與 delivery-date-naming-convergence 的交會**：兩個 change 都改 § 工單列印單據。該 change 的差異檔表頭句已同步寫入「品檢需求（取所屬印件）、製程說明（取所屬印件）」，兩者任一先歸檔皆得到同一份最終文字，不再有順序限制。
- [x] 1.3 Prototype 與測試施工完成、Miles 確認後執行 `openspec archive`，三份差異檔併回主 spec。
- [x] 1.4 歸檔後執行 `grep -rn "製程說明\|品檢需求" openspec/specs/`，輸出中的每一處 MUST 為「取所屬印件」或指向 order-management § 印件製程說明與品檢需求 的引用，無任何把兩欄寫成工單自持欄位的殘留。

## 2. 測試先行：兩欄歸屬與同源的失敗測試

> 每條先落地失敗測試再改 Prototype；期望值取自各規格差異檔的 THEN 描述，MUST NOT 由實作重算。

- [x] 2.1 於第七章新增情境「製程說明與品檢需求寫在印件上，工單詳情與印件詳情兩頁同值」：在工單甲詳情的印件基本資訊面板填入品檢需求後，斷言印件詳情頁與同印件工單乙皆顯示同一段文字；期望值取自 work-order § 製程規劃 Scenario「於工單詳情改品檢需求後印件詳情與同印件其他工單同值」。
- [x] 2.2 於第七章新增情境「工單自身不再有製程說明欄位」：斷言工單詳情的工單資訊區塊不出現製程說明與品檢需求兩欄，兩欄只出現在印件基本資訊面板；期望值取自 work-order § 製程規劃 Scenario「工單不提供自持的兩欄」。
- [x] 2.3 於第七章新增情境「兩欄的編輯把關看印件不看工單狀態」：涵蓋製作中仍可編輯、印件印製維度為已送達時唯讀、非印務角色唯讀三條斷言；期望值取自 order-management § 印件製程說明與品檢需求 對應 Scenario。
- [x] 2.4 於第七章新增情境 7.26「印件的製程說明與品檢需求都沒填，工單照樣送得出審核」，期望值取自同 Requirement 對應 Scenario。
- [x] 2.5 情境 8.6 的紙本工單表頭兩欄已斷言「取所屬印件的值」，另新增情境 8.11「同一件印件的兩張工單，紙本表頭印出同一份品檢需求與製程說明」；期望值取自 work-order § 工單列印單據 Scenario「同印件多張工單的紙本印出同一份品檢需求與製程說明」。
- [x] 2.6 新增情境 8.12「印件的兩欄都沒填時，紙本表頭各印破折號且不擋下列印」；期望值取自同 Requirement 對應 Scenario。
- [x] 2.7 於第十一章情境 11.11 補齊「驗收介面顯示該印件的品檢需求（唯讀）」：涵蓋有值顯示、唯讀無編輯入口、不顯示製程說明三條斷言；期望值取自 qc § 分次驗收記錄 Scenario「驗收介面顯示該印件的品檢需求」。
- [x] 2.8 於第十一章新增情境 11.12「印件的品檢需求沒填時顯示破折號，驗收照樣記得下去」，期望值取自同 Requirement 對應 Scenario。
- [x] 2.9 於第十一章新增情境 11.13「印務改過品檢需求後，驗收介面顯示新的一份，舊紀錄不留快照」：印務改過之後驗收介面顯示新值，既有品檢紀錄不含該欄；期望值取自同 Requirement 對應 Scenario。
- [x] 2.10 改寫情境 6.11（工單的事實只有一份，兩頁看到的一定一樣）：把印件層兩欄納入同源斷言範圍。
- [x] 2.11 改寫情境 14.7（印件詳情的工單與生產任務頁籤）：補斷言「印件基本資訊面板含製程說明與品檢需求兩欄」。

## 3. Prototype：印件側（訂單模組）

> 對應規格差異檔：order-management § 印件製程說明與品檢需求。

- [x] 3.1 印件資料結構新增 `process_note`（製程說明）與 `qc_requirement`（品檢需求）兩欄，皆為多行文字、可為空。
- [x] 3.2 印件詳情頁的印件基本資訊面板新增兩欄的顯示與編輯，欄位位置排在「預計產線」之後。
- [x] 3.3 兩欄的編輯把關：可編輯者為「負責該印件任一張非終態工單的印務」、該工單編輯（代理）層級的分享成員、印務主管；其他角色唯讀。工單狀態 MUST NOT 納入把關條件。
- [x] 3.4 印件印製維度為已送達或已棄用時兩欄唯讀。
- [x] 3.5 兩欄 MUST NOT 加必填檢核、MUST NOT 於工單送審時提示未填。

## 4. Prototype：工單側

> 對應規格差異檔：work-order § 製程規劃、§ 工單列印單據。

- [x] 4.1 工單資料結構移除 `process_note`（製程說明）欄位及其所有顯示與編輯入口。
- [x] 4.2 工單詳情新增印件基本資訊面板的兩欄顯示與編輯，讀寫對象為所屬印件；把關邏輯與 3.3、3.4 共用同一支判定函式，MUST NOT 各寫一份。
- [x] 4.3 確認兩個入口寫入同一筆印件事實：任一處存檔後，另一處與同印件其他工單重新開啟即為同值，MUST NOT 於工單側留任何副本或快取值。
- [x] 4.4 紙本工單 `print/page.js` 表頭的品檢需求與製程說明改取所屬印件的現值；無值各印「－」，MUST NOT 擋下列印、MUST NOT 留存快照。

## 5. Prototype：品檢側

> 對應規格差異檔：qc § 分次驗收記錄。

- [x] 5.1 品檢驗收介面新增該印件品檢需求的唯讀顯示區，無值顯示「－」。
- [x] 5.2 確認該區為唯讀、無編輯入口；製程說明 MUST NOT 出現在驗收介面。
- [x] 5.3 確認品檢紀錄資料結構未新增品檢需求欄位、未留存快照；每次開啟介面取印件現值。
- [x] 5.4 確認手機寬度下該顯示區可完整閱讀（品檢介面既有支援手機寬度）。

## 6. Mock 資料鏈同步

- [x] 6.1 更新 `(prototype)/MOCK-DATA-CHAIN.md`：印件層新增製程說明與品檢需求兩欄；工單層移除製程說明欄；補一段說明「兩欄各一份文字管旗下全部工單，工單側無副本」。
- [x] 6.2 依 6.1 改 `orders/mock-data.js`、`print-items/_lib/mock-data.js`、`work-orders/_lib/mock-data.js`。
- [x] 6.3 **多工單印件的既有說明合併**：同一印件的多張工單各自寫了不同製程說明時，合併成一段文字並以部件名分段（例：「本體：…／封面：…」），寫進印件的製程說明欄；MUST NOT 只留其中一張的內容。合併結果逐筆列進交付回報。
- [x] 6.4 於鏈中預置一件多工單印件（鏈七 PI-2026-0904，兩欄皆有值、以部件名分段），供情境 7.25、6.11、8.11、14.7 取用。兩欄皆空與印件終態兩種樣本不另建 mock：前者由情境 7.26、8.12、11.12 在測試內以印務自己的清空動作取得（兩欄選填，清空是真實可做的操作），後者由純函式測試 `tests/unit/work-orders/rules-print-item-process-qc-guard.test.mjs` 直接驗把關函式。
- [x] 6.5 執行 `npm run test:smoke`，確認主流程在欄位搬家後不斷。

## 7. 測試影響清單（Sens `erp-prototype-tests`）

> 依 `docs/scenario-catalog.md` 節號列出改動範圍；改哪章跑哪章，提交前跑全套。

**既有情境需修改**：

| 節號 | 情境 | 修改內容 |
|------|------|---------|
| 6.11 | 工單的事實只有一份，兩頁看到的一定一樣 | 同源斷言範圍納入印件層製程說明與品檢需求兩欄 |
| 8.6 | 印務列印紙本工單，版式與欄位範圍固定且不含價格 | 表頭兩欄改斷言「取所屬印件的值」 |
| 11.11 | 品檢驗收介面看得到印件的品檢需求 | 補「唯讀、無編輯入口」與「不顯示製程說明」斷言 |
| 14.7 | 印件詳情的工單與生產任務頁籤 | 補「印件基本資訊面板含製程說明與品檢需求兩欄」斷言 |

**新增情境**：

| 節號 | 新增情境 | 對應規格差異檔 Scenario |
|------|---------|---------------------|
| 7.25 | 製程說明與品檢需求記在印件層，一處改動兩頁同值（兩個方向各驗一次） | work-order § 製程規劃 |
| 7.3 | 工單資訊卡與編輯抽屜都不含兩欄（併入既有節） | work-order § 製程規劃 |
| 7.26 | 印件的製程說明與品檢需求都沒填，工單照樣送得出審核 | order-management § 印件製程說明與品檢需求 |
| 8.11 | 同一件印件的兩張工單，紙本表頭印出同一份品檢需求與製程說明 | work-order § 工單列印單據 |
| 8.12 | 印件的兩欄都沒填時，紙本表頭各印破折號且不擋下列印 | 同上 |
| 11.12 | 印件的品檢需求沒填時顯示破折號，驗收照樣記得下去 | qc § 分次驗收記錄 |
| 11.13 | 印務改過品檢需求後，驗收介面顯示新的一份，舊紀錄不留快照 | 同上 |

兩欄的編輯把關（製作中可改、印件終態唯讀、非印務唯讀）以純函式測試
`tests/unit/work-orders/rules-print-item-process-qc-guard.test.mjs` 驗算。

**Mock 異動範圍**：`MOCK-DATA-CHAIN.md`（先改）→ `orders/mock-data.js`、`print-items/_lib/mock-data.js`、`work-orders/_lib/mock-data.js`（後改）。新增欄位：印件層製程說明與品檢需求；移除欄位：工單層製程說明；新增樣本：一件多工單印件（鏈七 PI-2026-0904，兩欄有值、以部件名分段）。

**測試執行順序**：`npm run impact` 對映改動 → `npm run test:smoke` → 改動章節（六、七、八、十一、十四）→ 提交前 `npm test` 跑全套。

## 8. 交付與另案

- [x] 8.1 erp repo 開分支施工、提交前跑全套測試，commit 訊息附測試結果（Vitest 與 Playwright 通過數）。
- [x] 8.2 記錄後端另案清單供 Linear：工單 `製程說明` 欄位停用、印件新增製程說明與品檢需求兩欄的欄位對映、既有工單製程說明資料往印件的合併遷移。
