# 任務清單

> 實作範圍：erp repo `/Users/b-f-03-029/erp` 的 `apps/erp/src/app/(prototype)/`，MUST 經 skill `prototype-from-prompt`；只動 `(prototype)/` 目錄，共用元件與 app 層檔案不碰。
> 測試在 Sens repo `erp-prototype-tests/`，erp repo 不加測試檔。
> mock 異動順序固定：先改 `(prototype)/MOCK-DATA-CHAIN.md` → 再改各模組 `mock-data.js` → 最後改引用該鏈編號的測試。
> 本次不跑 Playwright（Miles 2026-09-21 指示）：畫面測試只補情境目錄節與待補註記，Vitest 純函式測試照寫照跑。

## 1. wiki 與 OpenSpec 收尾

- [x] 1.1 wiki 正本卡落卡（已完成）：新建工作天規則卡，印件三欄、訂單四欄、工單四欄、生產任務三欄、出貨單兩欄、需求單印件項目、急件主檔工作天、訂單異動規則、北極星指標、工單與生產任務與出貨單三張狀態機卡、五張業務情境卡與三張總覽卡——commit 9158ab1（共 43 檔）。本 change 不再改 wiki。
- [ ] 1.2 執行 `openspec validate delivery-date-redefinition --strict`，六份規格差異檔（order-management、work-order、quote-request、shipment、production-overview、prepress-review）皆通過。
- [ ] 1.3 歸檔順序 MUST 為：`delivery-date-naming-convergence` → `work-order-detail-company-alignment` → 本 change。順序顛倒時本 change 的條文會被舊文字蓋回去。
- [ ] 1.4 歸檔前逐份比對重疊 Requirement：前兩個 change 歸檔後，取主 spec 現值與本 change 各該 Requirement 的基準文字比對（清單見 design.md § Decisions D10）；有差異即以主 spec 現值重貼後再套本次改動。
- [ ] 1.5 Prototype 與測試施工完成、Miles 確認後執行 `openspec archive delivery-date-redefinition`。
- [ ] 1.6 歸檔後手改規格差異檔改不到的殘留用詞：`openspec/specs/order-management/spec.md` § Data Model 的實體關聯總覽、`openspec/specs/production-overview/spec.md` 檔頭的指標列，兩處的日期欄名改用公司名稱。
- [ ] 1.7 歸檔後執行 `grep -rn "預計完工日\|實際完工日" openspec/specs/`，輸出應為零筆。
- [ ] 1.8 歸檔後執行 `grep -rn "預計出貨日" openspec/specs/`，輸出應只剩三類：訂單的「審稿前預計出貨日」與「審稿後預計出貨日」（EC 另案不改名）、出貨單的「出貨單預計出貨日」、以及 order-management 名詞對照中標明公司另稱的括號註記。
- [ ] 1.9 歸檔後執行 `grep -rn "以曆日計\|曆日" openspec/specs/`，確認交期鏈相關條文無殘留曆日口徑。

## 2. 測試先行：Vitest 純函式失敗測試落地

> 每條先落地失敗測試再改 Prototype；期望值取自各規格差異檔的 THEN 描述，MUST NOT 由實作重算。節號依 `erp-prototype-tests/docs/scenario-catalog.md`。

- [x] 2.1 新增工作天算法純函式測試：週末跳過、國定假日跳過、補班日算工作天、加減自隔日起算、兩日之間的工作天數（起日不計迄日計）、假日表更新不回溯重算；期望值取自 order-management § 交期鏈日期以工作天計算 的四個 Scenario。
- [x] 2.2 新增急件扣減純函式測試：凍結天數 3 由 2026-10-15 得 2026-10-12、一般件兩日同天、延後型（凍結天數 −3）得 2026-10-20、未扣值為空時結果為空；期望值取自 order-management § 印件內部完成日推導。
- [x] 2.3 新增印件預計交期推算純函式測試：下一個工作天（10-16 得 10-19）、業務改晚保留、改早於印件內部完成日被擋、內部完成日變動時覆蓋；期望值取自 order-management § 印件交期欄變更的重算、覆蓋與同步。
- [x] 2.4 新增訂單層衍生值純函式測試：訂單內部完成時間取最晚、訂單預計交貨日期取未出貨最早、全部交完日為下一個工作天、已棄用印件不計、逾期未出不跳筆、全部出完停最後一筆、首張出貨單作廢後退回、全空時為空；期望值取自 order-management § 訂單層衍生交期日期。
- [x] 2.5 新增逾期標示純函式測試：印件逾期天數以工作天計、訂單部分延遲、整單逾期、已出貨不標、空值不判；期望值取自 order-management § 交期逾期標示。
- [x] 2.6 新增排程硬擋判定純函式測試：工單預排完成日晚於印件內部完成日即擋、等於不擋、兩者任一為空不擋、擋下訊息含兩個日期與相差工作天數、任務作廢後回落即放行；期望值取自 work-order § 工單排程硬擋。
- [x] 2.7 新增排程超期標示純函式測試：核可後超期即標、回落即消、終態工單不標、空值不判；期望值取自 work-order § 核可後的排程超期標示。
- [x] 2.8 新增工單準時判定純函式測試：不晚於即準時、晚於標逾期 N 個工作天、取工單凍結的印件內部完成日、補做重開取末次、空值不判；期望值取自 work-order § 工單準時判定。
- [x] 2.9 新增任務日期純函式測試：任務實際完成日取進入已完成的時間（報工達標與生管手動兩路徑）、作廢無值、今日到期、逾期 N 個工作天、終態不標、任務準時判定；期望值取自 work-order § 任務實際完成日與到期逾期標示。
- [x] 2.10 改寫 7.24 的超期判定純函式測試：欄名改任務預計完成日，補「本提示與硬擋分工不重疊」的斷言（同一組資料下軟提示標示但存檔成功）；期望值取自 work-order § 任務預計完成日超期軟提示。
- [x] 2.11 新增出貨準時判定純函式測試：實際出貨日取首次進入已出貨狀態群、自取不走特例、首張出貨單定義（同時間取編號較小者）、分批不改判、一單多印件各自比對、首張作廢後回到未出貨、印件預計交期為空不計、打樣與短出結案不計分母；期望值取自 shipment § 實際出貨日與出貨準時判定。
- [x] 2.12 改寫 5.15 的待審訂單母列純函式測試：取值改印件預計交期、天數改以工作天計、補「不取訂單預計交貨日期」的斷言；期望值取自 prepress-review § 審稿訂單脈絡查詢視圖。
- [x] 2.13 新增待審清單排序鍵純函式測試：排序取扣急件之後的印件內部完成日（急件件排前）、空值排最後；期望值取自 prepress-review § 待審清單排序與停滯規則。

## 3. Prototype：訂單與需求單的交期欄

> 對應規格差異檔：order-management、quote-request。

- [x] 3.1 訂單印件的編輯面板、新增印件 Dialog 與加開印件 Dialog：欄位改為「未扣急件內部完成日」（選填、可清空），並即時顯示唯讀的印件內部完成日與印件預計交期；印件內部完成日旁註明扣了幾個工作天。
- [x] 3.2 印件預計交期改為可編輯：預設帶系統建議值，早於印件內部完成日時擋下並提示；未扣急件內部完成日或急件選項變動時覆蓋該值並提示已重算。
- [x] 3.3 交期三欄的可改條件改為「訂單非終態且印製維度非已棄用」，印製維度為已送達時仍可改；訂單終態或印件已棄用時唯讀。
- [x] 3.4 訂單印件清單的欄名全面改為未扣急件內部完成日、印件內部完成日、印件預計交期；空值一律顯示無值符號。
- [x] 3.5 需求單印件項目列：欄位改為未扣急件內部完成日（逐列手填、無預設值），並顯示唯讀的印件內部完成日（等於未扣值）與印件預計交期；畫面不出現急件選項欄。
- [x] 3.6 成交轉訂單帶入未扣急件內部完成日原值，空值留空；轉入後急件選項待業務選定。

## 4. Prototype：訂單層衍生日期與標示

- [x] 4.1 訂單詳情新增三項唯讀衍生顯示：訂單內部完成時間、訂單預計交貨日期、全部交完日；空值顯示無值符號。
- [x] 4.2 訂單資訊 Section 新增「客戶指定收件日」編輯欄（選填），存檔時若早於旗下印件最晚的印件預計交期即顯示提醒、不擋；旗下印件預計交期全空時不提醒。
- [x] 4.3 訂單列表新增「訂單預計交貨日期」欄，可升降冪排序（空值排最後）並提供日期區間篩選；清除篩選一併重置該區間。
- [x] 4.4 訂單詳情與訂單列表新增逾期標示：印件層逾期、訂單層部分延遲與整單逾期；純標示、不擋不通知。

## 5. Prototype：工單與生產任務

> 對應規格差異檔：work-order。

- [x] 5.1 工單詳情摘要卡四格改名並改序：印件預計交期、印件內部完成日、工單預排完成日、工單實際完成日；空值顯示無值符號。
- [x] 5.2 工單資訊區與工單列表的欄名全面改名：預計完工日改工單預排完成日、實際完工日改工單實際完成日；工單多帶唯讀的印件預計交期。
- [x] 5.3 送審按鈕加排程硬擋：工單預排完成日晚於印件內部完成日時擋下，跳出 Dialog 列出工單預排完成日、印件內部完成日與相差的工作天數；兩者任一為空不擋。
- [x] 5.4 待審核工單列表與工單詳情的核可動作加同一套硬擋：擋下時不產生派單、工單留在列表；列上新增印件內部完成日與工單預排完成日兩欄。
- [x] 5.5 已核可工單的「排程超期」標示：工單列表與工單詳情兩處呈現，回落即消失，終態不標。
- [x] 5.6 工單完成後的準時標示：準時或逾期 N 個工作天，取工單凍結的印件內部完成日比對。
- [x] 5.7 生產任務列欄名改「任務預計完成日」，新增「任務實際完成日」欄（唯讀）；今日到期與逾期 N 個工作天標示，終態不標。
- [x] 5.8 紙本工單 `print/page.js` 表頭的交期欄名改「印件內部完成日」，無值印無值符號；單據不出現預計完工日與實際完工日字樣。
- [x] 5.9 工廠總覽待排區欄位改印件內部完成日與工單預排完成日，並帶排程超期標示；生產任務管理頁的欄名一併對齊。

## 6. Prototype：出貨

- [x] 6.1 出貨單詳情與檢視側板新增「實際出貨日」唯讀衍生欄，取首次進入已出貨狀態群的日期；自取不另設規則。
- [x] 6.2 出貨單的「預計出貨日」欄名改「出貨單預計出貨日」，涵蓋建立 Dialog、列表欄、側板與篩選。
- [x] 6.3 印件層新增出貨準時標示：取首張出貨單的實際出貨日比對印件預計交期；首張作廢或轉異常後回到未出貨、標示移除。

## 7. Prototype：工作天純函式與 mock 資料鏈同步

- [x] 7.1 於 `(prototype)/_lib/` 新增工作天純函式（對應後端 python 函式的 JS 版）：`isWorkingDay`、`nextWorkingDay`、`addWorkingDays`、`subtractWorkingDays`、`workingDaysBetween`，並附 2026 假日表 mock（非例假日的放假日 1/1、2/16 至 2/20、2/27、4/3、4/6、5/1、6/19、9/25、9/28、10/9、10/26、12/25，全年無補班日）。
- [x] 7.2 於同一目錄新增交期鏈純函式：印件內部完成日、印件預計交期建議值、訂單三個衍生日期、逾期天數、硬擋判定、準時判定，供訂單、工單、出貨三側共用。
- [x] 7.3 先改 `(prototype)/MOCK-DATA-CHAIN.md`：新增未扣急件內部完成日欄、改寫印件內部完成日與印件預計交期的取數方向、新增訂單四欄、工單兩欄改名加一欄印件預計交期、生產任務任務實際完成日、出貨單實際出貨日與出貨單預計出貨日改名；逐鏈標出日期推導的算式與結果。
- [x] 7.4 依 7.3 改各模組 mock：`quote-prototype/mock-data.js`、`orders/mock-data.js`、`print-items/_lib/mock-data.js`、`work-orders/_lib/mock-data.js`、`production-overview` 與 `shipments` 相關 mock。
- [x] 7.5 **鏈不可斷**：逐條資料鏈驗「需求單印件的未扣急件內部完成日 → 訂單印件的未扣急件內部完成日 → 印件內部完成日 → 印件預計交期 → 工單帶入的兩欄 → 任務預計完成日與任務實際完成日 → 出貨單實際出貨日」每一段都對得起來；任一段對不上即回頭改 7.3。
- [x] 7.6 於鏈中預置取數樣本：一件未扣急件內部完成日留空的印件（整條鏈顯示無值符號）、一張工單預排完成日晚於印件內部完成日的工單（供硬擋與排程超期驗）、一件分批出貨的印件（供首張出貨單判定驗）、一件延後型急件的印件。
- [x] 7.7 執行 `npm run impact` 對映改動範圍，再執行 `npm run test:smoke` 確認主流程不斷。
- [x] 7.8 執行 Vitest 全套（`npm run test:unit`），§ 2 各條純函式測試全部通過。

## 8. 測試影響清單（Sens `erp-prototype-tests`）

> 依 `docs/scenario-catalog.md` 節號列出改動範圍；先跑 `npm run impact` 對映影響。本次 Playwright 畫面測試不新寫、不改寫斷言，只更新情境目錄的敘述並標「待補畫面測試」；Vitest 純函式測試照 § 2 落地並跑過。

**既有情境需修改（本次只改目錄敘述，畫面斷言標待補）**：

| 節號 | 情境 | 修改內容 |
|------|------|---------|
| 1.1 | 業務建需求單、逐筆填印件項目後送印務評估 | 印件項目欄改未扣急件內部完成日；補兩個推得值唯讀顯示、無急件選項欄 |
| 1.10 | 成交轉訂單印件項目預計出貨日逐件帶入、空的就留空 | 改為未扣急件內部完成日逐件帶入；補「急件選項不帶入、轉入後由業務選定才扣天數」 |
| 4.5 | 業務在印件上選急件選項，系統推出印件層內部完成日 | 算式反轉：由未扣急件內部完成日扣急件工作天得印件內部完成日；錨例改 2026-10-15 扣 3 個工作天得 2026-10-12、印件預計交期 2026-10-13 |
| 4.6 | 改急件選項或印件預計出貨日只留痕與同步，不通知 | 觸發來源加未扣急件內部完成日；補「印件預計交期被重算覆蓋並提示」「同步工單兩欄」 |
| 4.7 | 新增印件收齊七項必填、預計出貨日選填且無預設值 | 選填欄改未扣急件內部完成日；補面板即時顯示兩個推得值 |
| 4.8 | 複製加開印件購買數量與預計出貨日留空 | 留空欄改未扣急件內部完成日；補兩個推得值顯示無值符號 |
| 4.9 | 印件已棄用後預計出貨日唯讀；訂單完成後唯讀 | 改為交期三欄；補「印製維度已送達後仍可改」的新斷言 |
| 4.10 | 三條複製路徑仍帶入原值 | 欄名改未扣急件內部完成日 |
| 5.13 | 待審清單依印件內部完成日排序、空值排最後 | 排序鍵語意改為扣急件之後的值；補「急件件排在未扣值較早的一般件之前」 |
| 5.14 | 待審訂單模組母列顯示預計出貨日 | 母列與子列欄名改印件預計交期、印件內部完成日；天數改以工作天計 |
| 5.15 | 母列的預計出貨日取旗下未收斂印件最早的那一個 | 取值改印件預計交期；補「不取訂單預計交貨日期」 |
| 6.13 | 交期一路留空時五個畫面照實留白、不當機 | 留空欄改未扣急件內部完成日；五處欄名對齊；補訂單層三個衍生值同顯示無值符號 |
| 7.4 | 同一個日期在三個頁面叫同一個名字 | 欄名改任務預計完成日；補工單三處（詳情、列表、紙本）的印件內部完成日同名斷言 |
| 7.12 | 預計完成日純手填，工單預計完工日取最大值 | 欄名改任務預計完成日與工單預排完成日 |
| 7.23 | 生產任務的預計完成日晚於工單內部完成日時軟提示 | 欄名改任務預計完成日；補「軟提示不擋存檔，但同一張工單送審時被硬擋」 |
| 7.24 | 超期判定納入哪些任務、排除哪些任務 | 欄名改任務預計完成日；純函式斷言照 2.10 更新 |
| 7.26 | 印務於製程規劃填工單確樣需求 | 側板第一段欄名改工單預排完成日 |
| 8.6 | 印務列印紙本工單，版式與欄位範圍固定 | 工單資訊表的「交期」改欄名印件內部完成日 |
| 8.8 | 急件工單的紙本標示與交期 | 交期取數改印件內部完成日（扣急件工作天後的值）；錨例日期改算 |
| 9.3 | 一包一設備：派工由生管在工作包頁執行 | 生產任務管理頁欄名改任務預計完成日、印件內部完成日 |
| 9.7 | 工廠總覽待排區顯示印件內部完成日 | 補工單預排完成日欄與排程超期標示 |
| 9.8 | 派工視窗上方列出這次要派的任務內容 | 欄名改任務預計完成日 |
| 10.12 | 首次報工把上游三層一起推進 | 補「該筆報工時間寫入任務實際完成日（達標時）」與「工單轉已完成時寫入工單實際完成日」 |
| 10.15 | 完成判定一律取生產數量（投入）累計 | 「實際完工日」改工單實際完成日 |
| 10.19 | 尚未報工的生產任務，任務實際開工為空 | 補任務實際完成日的四個取數情形 |
| 12.1 | 出貨單列表、預計出貨日欄與檢視側板 | 欄名改出貨單預計出貨日；側板補實際出貨日 |
| 12.2 | 業務建草稿再建立出貨單 | 必填欄名改出貨單預計出貨日；補「草稿不產生實際出貨日、不算首張出貨單」 |
| 14.7 | 印件詳情的工單與生產任務頁籤 | 工單列與任務列欄名全面改名，補任務實際完成日欄 |
| 14.16 | 訂單列表不顯示也不篩選交期 | **整條翻案改寫**：列表顯示訂單預計交貨日期並可排序與區間篩選；訂單內部完成時間與全部交完日不上列表 |
| 14.23 | 工單資訊的欄序照公司對照表 | 欄名改工單預排完成日、工單實際完成日 |
| 14.26 | 工單摘要卡每一格都留副標行 | 四格改名並改序（印件預計交期、印件內部完成日、工單預排完成日、工單實際完成日）|
| 15.2 | 五個指標與工單成本對照走同一支算式 | 漏單率的基準欄名改任務預計完成日 |
| 15.5 | 印件出貨統計反映已送達的出貨單 | 補實際出貨日取首次進入已出貨狀態群 |
| 15.7 | 印件與工單的內部完成日一致 | 欄名改印件內部完成日；補印件預計交期兩處一致的斷言 |

**新增情境（本次只在目錄建節、標「待補畫面測試」；括號內為已落地的 Vitest 純函式測試）**：

| 章節 | 新增情境 | 對應規格差異檔 Scenario |
|------|---------|---------------------|
| 四、訂單與印件 | 訂單詳情顯示三個衍生日期與客戶指定收件日提醒（純函式 2.4） | order-management § 訂單層衍生交期日期、§ 客戶指定收件日 |
| 四、訂單與印件 | 訂單預計交貨日期依出貨滾動、逾期未出不跳筆（純函式 2.4） | order-management § 訂單層衍生交期日期 |
| 四、訂單與印件 | 印件逾期、訂單部分延遲與整單逾期三種標示（純函式 2.5） | order-management § 交期逾期標示 |
| 七、印務登打製程 | 工單送審被排程硬擋，Dialog 列出兩個日期與相差工作天數（純函式 2.6） | work-order § 工單排程硬擋 |
| 七、印務登打製程 | 核可時再判一次硬擋、擋下不產生派單（純函式 2.6） | work-order § 製程審核流程 |
| 七、印務登打製程 | 已核可工單的排程超期標示（純函式 2.7） | work-order § 核可後的排程超期標示 |
| 十、師傅報工 | 任務實際完成日取進入已完成的時間、今日到期與逾期標示（純函式 2.9） | work-order § 任務實際完成日與到期逾期標示 |
| 十二、出貨 | 實際出貨日與出貨準時判定、分批以首張為準（純函式 2.11） | shipment § 實際出貨日與出貨準時判定 |
| 十四、查詢與畫面 | 訂單列表依訂單預計交貨日期排序與區間篩選 | order-management § 訂單列表權限與可見範圍 |
| 十五、跨頁一致性 | 工單完成後的準時標示取凍結值（純函式 2.8） | work-order § 工單準時判定 |
| 十五、跨頁一致性 | 工作天算法在五處取得同一個結果（純函式 2.1） | order-management § 交期鏈日期以工作天計算 |

**Mock 異動範圍**：`MOCK-DATA-CHAIN.md`（先改）→ `quote-prototype/mock-data.js`、`orders/mock-data.js`、`print-items/_lib/mock-data.js`、`work-orders/_lib/mock-data.js`、`production-overview` 與 `shipments` 相關 mock（後改）。新增資料：未扣急件內部完成日、訂單四項日期、工單的印件預計交期、任務實際完成日、出貨單實際出貨日、2026 假日表。改名欄位：工單預排完成日、工單實際完成日、任務預計完成日、印件預計交期、出貨單預計出貨日。

**測試執行順序**：`npm run impact` 對映改動 → `npm run test:smoke` → Vitest 全套（§ 2 各條）→ 提交前確認 Playwright 既有測試未因改名而紅；因改名而紅者本次只更新選擇器與文案，不新寫畫面情境。

## 9. 交付與另案

- [ ] 9.1 erp repo 開分支施工，提交前跑 smoke 與 Vitest，commit 訊息附測試結果。
- [ ] 9.2 記錄後端另案清單供 Linear：工作天維護表與工作天函式、既有印件日期的資料遷移與重推、急件主檔增減天數改以工作天解讀、訂單層三個衍生日期的查詢效能、出貨單實際出貨日的取數。
- [ ] 9.3 相關 OQ 追蹤（皆另案，不擋本 change）：PT-061 工作天維護表的初始值核對與年度更新責任、ORD-057 客戶指定收件日檢核所需運送天數的來源、ORD-051 印件準時交貨率在大面積留空時的代表性、PT-054 延後型急件照實顯示。
- [ ] 9.4 回頭向公司確認兩處明示差異：工單完成不以品檢未過阻擋、訂單預計交貨日期只依出貨事實滾動（逾期未出不跳筆）。
