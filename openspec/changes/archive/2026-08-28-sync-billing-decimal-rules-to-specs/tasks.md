## 1. 同步 order-billing spec

- [x] 1.1 收斂張減法基數由「訂單未稅總額」改為「未稅基準」（未稅總額加已認列訂單異動的未稅合計），並補一個訂單有已認列異動時的 Scenario
- [x] 1.2 品項編輯規則的「整數單價」改為單價可帶到小數點後兩位、小計照實帶小數不取整
- [x] 1.3 品項檢核由「加總等於未稅目標值、差額不為零擋下」放寬為「差在一元以內」，並補容許與擋下兩個對照 Scenario
- [x] 1.4 ezPay 硬約束段的「單價為純整數」改為最多兩位小數，Rationale 改寫為文件型別欄寫整數、實測以字串送出即接受小數
- [x] 1.5 「unitPrice 純整數，前端 lint 擋小數」Scenario 改為接受兩位小數、第三位擋下
- [x] 1.6 期次重分配由「以刪除加新建取代拆期 UI」改為兩條路並陳，移除系統內生拆期的保留條款，並補期次金額改動後發票須作廢重開的 Scenario
- [x] 1.7 活動紀錄刪去拆期專屬事件（事件型別由七個減為六個），對應 Scenario 改由取消與新建兩種事件承接
- [x] 1.8 收款變更率的分子事件清單刪去拆期事件，例子與「分子僅含幾種事件」的敘述同步改正

## 2. 驗收

- [x] 2.1 `openspec validate --change sync-billing-decimal-rules-to-specs --strict` 通過
- [x] 2.2 全文檢查 main spec 不再出現「純整數」「差額不為零」「splitBillingInstallment」「訂單未稅總額」四組舊措辭
- [x] 2.3 spec 的驗算數字與 wiki [對帳一致性] 例子 10 一致（123 × 8.13 ＝ 999.99、加總 12,849.99 對銷售額 12,850）
