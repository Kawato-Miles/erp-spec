# 任務清單

> 實作範圍：erp repo `/Users/b-f-03-029/erp` 的 `apps/erp/src/app/(prototype)/`，MUST 經 skill `prototype-from-prompt`；只動 `(prototype)/` 目錄，共用元件與 app 層檔案不碰。
> 測試在 Sens repo `erp-prototype-tests/`，erp repo 不加測試檔。
> mock 異動順序固定：先改 `(prototype)/MOCK-DATA-CHAIN.md` → 再改各模組 `mock-data.js` → 最後改引用該鏈編號的測試。

## 1. OpenSpec 收尾

- [ ] 1.1 執行 `openspec validate --changes`，五份規格差異檔（order-management、quote-request、work-order、prepress-review、production-overview）皆通過；`openspec status` 顯示 proposal / design / specs 三項為 done。
- [ ] 1.2 Prototype 與測試施工完成、Miles 確認後執行 `openspec archive`，五份差異檔併回主 spec。
- [ ] 1.3 歸檔後執行 `grep -rn "訂單交期\|預計交期\|內部製作截止日\|印件交期" openspec/specs/`，輸出應只剩本 change 回報中列為「不在範圍」的項目，無其他殘留。

## 2. 測試先行：命名與規則的失敗測試

> 每條先落地失敗測試再改 Prototype；期望值取自各規格差異檔的 THEN 描述，MUST NOT 由實作重算。

- [ ] 2.1 改寫 `erp-prototype-tests` 情境 1.10：由「單頭訂單交期預填至印件項目」改為「需求單無單頭交期欄，印件項目預計出貨日逐列手填、新增列為空白」；期望值取自 quote-request § 印件項目管理 Scenario「新增印件項目時預計出貨日為空白」「業務逐列手填印件項目預計出貨日」。
- [ ] 2.2 改寫情境 1.11：由「分層帶入、空值不取單頭補」改為「印件項目預計出貨日逐件帶入、空值即為空」；期望值取自 quote-request § 成交轉訂單 Scenario。
- [ ] 2.3 於第一章新增情境「複製需求單時印件項目預計出貨日照原值帶入」（緊鄰 1.6），期望值取自 quote-request § 印件項目管理 對應 Scenario。
- [ ] 2.4 改寫情境 4.5：欄名由「訂單交期／預計交期」改為「預計出貨日／內部完成日」，算式與錨例數值不變；期望值取自 order-management § 印件內部完成日推導 Scenario「依預計出貨日與急件天數推導」。
- [ ] 2.5 改寫情境 4.6：欄名改為預計出貨日，斷言維持「只留痕與同步、不通知」；另補一條斷言「清空預計出貨日時非終態工單的內部完成日同步為空」，期望值取自 order-management § 印件急件選項與預計出貨日變更留痕與同步 Scenario。
- [ ] 2.6 改寫情境 4.7：由「訂單訂單交期編輯不觸發印件同步」改為「訂單資訊 Section 不出現訂單交期、內部製作截止日與是否急件三欄，審稿前後預計出貨日仍可編輯」；期望值取自 order-management § 訂單詳情頁編輯型 Section Scenario「訂單資訊 Section 不出現交期欄位」。
- [ ] 2.7 改寫情境 4.8：由「訂單交期選填並預設帶入訂單訂單交期」改為「預計出貨日無預設值、空白手填」；期望值取自 order-management § 新增印件欄位與必填檢核 Scenario「預計出貨日無預設值、空白手填」。
- [ ] 2.8 改寫情境 4.9：斷言加開印件的預計出貨日留空，且 MUST NOT 帶入來源印件的日期；期望值取自 order-management § 加開印件 Scenario。
- [ ] 2.9 改寫情境 4.10：欄名改為預計出貨日，斷言維持「印件已棄用後唯讀、訂單完成後唯讀」。
- [ ] 2.10 刪除情境 4.11（內部製作截止日與印件預計交期不設比對關係）：該欄位已刪除，情境無對象可測；於 `docs/scenario-catalog.md` 同步移除該節並在 § 不列入 記一行原因。
- [ ] 2.11 於第四章新增情境「訂單列表不顯示交期欄、篩選面板無交期起訖」，期望值取自 order-management § 訂單列表權限與可見範圍 Scenario「訂單列表不提供交期篩選」。
- [ ] 2.12 改寫情境 5.13、5.14：排序鍵欄名改「內部完成日」；5.14 母列改斷言「取旗下未收斂印件最早的預計出貨日」與「皆無值時顯示－」，子列改「預計出貨日與內部完成日並列」；期望值取自 prepress-review 兩條 Requirement 的 Scenario。
- [ ] 2.13 改寫情境 7.4（同一個日期在三個頁面叫同一個名字）：三頁的欄名改為「內部完成日」，補一條斷言「三頁皆不出現預計交期或訂單交期字樣」。
- [ ] 2.14 改寫情境 8.6、8.8：紙本工單表頭欄名改「內部完成日」，維持「無值印－」斷言。
- [ ] 2.15 改寫情境 9.7：待排區欄名改「內部完成日」，新增斷言「畫面不出現建議開工日欄位」與「內部完成日為空時顯示－」；期望值取自 production-overview § 待排區 兩個 Scenario。
- [ ] 2.16 改寫情境 15.7：標題與斷言的欄名改為「印件與工單的內部完成日一致」，移除訂單層交期的對照斷言（訂單層已無交期欄）。
- [ ] 2.17 於第七章新增情境「生產任務預計完成日晚於工單內部完成日時提示、不擋存檔」，涵蓋存檔當下提示與任務列表持續標示兩處；期望值取自 work-order § 生產任務預計完成日超期提示 前三個 Scenario。
- [ ] 2.18 於第七章新增情境「超期提示的排除對象」，涵蓋已進終態的生產任務、售後補做任務不判定，品檢缺口補做任務照常判定；期望值取自同 Requirement 對應 Scenario。
- [ ] 2.19 於第七章新增情境「工單內部完成日為空時不比對、詳情顯示無交期基準」，期望值取自同 Requirement 對應 Scenario。
- [ ] 2.20 於第八章新增情境「終態工單不因後續交期變動回溯標超期」，期望值取自同 Requirement 對應 Scenario。

## 3. Prototype：需求單模組

> 對應規格差異檔：quote-request § 印件項目管理、§ 成交轉訂單。

- [ ] 3.1 `(prototype)/quote-prototype/` 需求單單頭移除「訂單交期」欄位與其表單欄、驗證與顯示。
- [ ] 3.2 需求單印件項目列的「訂單交期」欄改名「預計出貨日」；移除單頭值預填邏輯，新增列一律空白。
- [ ] 3.3 複製需求單建單的邏輯保留印件項目預計出貨日的原值帶入。
- [ ] 3.4 成交轉訂單邏輯移除單頭交期帶入；各印件項目的預計出貨日逐件帶入對應印件，空值維持空值。

## 4. Prototype：訂單模組

> 對應規格差異檔：order-management 全部 Requirement。核心邏輯檔：`(prototype)/orders/_lib/urgent-due-date.js`、`orders/_lib/store.js`。

- [ ] 4.1 印件欄位改名：`order_due_date` 對應欄名改為「預計出貨日」、推導欄對應欄名改為「內部完成日」，涵蓋印件清單、印件編輯面板、印件詳情頁與訂單詳情頁所有顯示位置。
- [ ] 4.2 `urgent-due-date.js` 的推導函式改名為對應「內部完成日」語意，算式維持 `預計出貨日 − 1 − 急件凍結天數`，預計出貨日為空時回傳 `null`、介面顯示「－」；函式註解內的 openspec 引用改指向 § 印件內部完成日推導。
- [ ] 4.3 訂單層移除 `deadline`（訂單交期）與內部製作截止日兩欄位及其所有顯示、編輯與推算邏輯；保留審稿前與審稿後預計出貨日兩欄不動。
- [ ] 4.4 訂單資訊 Section 編輯 Dialog 移除訂單交期與內部製作截止日兩個欄位入口。
- [ ] 4.5 訂單列表移除交期欄與交期起訖篩選；清空篩選的重置範圍同步移除交期條件。
- [ ] 4.6 新增印件 Dialog：預計出貨日欄位改為空白、移除預設帶入所屬訂單交期的邏輯，維持選填可存檔。
- [ ] 4.7 複製加開印件 Dialog：預計出貨日留空，確認 MUST NOT 自來源印件帶入日期。
- [ ] 4.8 打樣 NG-稿件問題複製新印件的邏輯：預計出貨日照原印件值帶入，並依急件選項重推內部完成日。

## 5. Prototype：工單模組

> 對應規格差異檔：work-order § 工單列印單據、§ 工單排程日期、§ 生產任務預計完成日超期提示。核心邏輯檔：`(prototype)/work-orders/_lib/due-date-sync.js`、`work-orders/print/page.js`。

- [ ] 5.1 工單「預計交期」欄名全面改為「內部完成日」，涵蓋工單列表、工單詳情、印件詳情的工單分組列、工廠總覽；「預計完工日」維持原名不動。
- [ ] 5.2 紙本工單 `print/page.js` 表頭的交期欄改名「內部完成日」，維持無值印「－」。
- [ ] 5.3 `due-date-sync.js` 的同步函式與欄位改名對齊 4.2 的輸出；印件推導值為 `null` 時非終態工單的內部完成日同步寫入 `null`。
- [ ] 5.4 新增超期判定函式：比對生產任務預計完成日與所屬工單內部完成日，回傳是否超期；排除生產任務狀態為已完成、已作廢、報廢者，排除補做來源為售後服務單的任務，品檢缺口補做的任務照常判定。
- [ ] 5.5 生產任務表單存檔當下即時提示超期，提示 MUST NOT 擋下存檔、MUST NOT 要求填理由。
- [ ] 5.6 工單詳情的生產任務列表對超期任務持續標示；工單內部完成日為空時不標示任何任務，並於工單詳情顯示一行「這張工單沒有交期基準」。
- [ ] 5.7 確認超期判定在工單內部完成日因印件同步而變動時重新執行，終態工單不重新判定。
- [ ] 5.8 確認超期提示不推播、不寄信、不進待辦：檢查程式碼內無通知派發呼叫。

## 6. Prototype：審稿模組

> 對應規格差異檔：prepress-review § 審稿訂單脈絡查詢視圖、§ 待審清單排序與停滯規則。核心檔：`(prototype)/prepress-review/_lib/selectors.js`、`ReviewOrderList.js`。

- [ ] 6.1 `selectors.js` 排序鍵欄名改為「內部完成日」，空值排最後、同組依印件編號排序的規則不變。
- [ ] 6.2 `ReviewOrderList.js` 母列的交期欄與距交期天數欄改取旗下未收斂印件（審稿維度尚未到達「已確認可製作」且非已棄用）最早的預計出貨日，唯讀衍生、不於訂單層存欄；皆無值時顯示「－」。
- [ ] 6.3 子列的兩個日期欄改名為「預計出貨日」與「內部完成日」並列。

## 7. Prototype：生產管理與工廠總覽

> 對應規格差異檔：production-overview § 待排區。

- [ ] 7.1 工廠總覽待排區欄名由「印件交期」改為「內部完成日」，無值顯示「－」。
- [ ] 7.2 待排區移除「建議開工日」欄位與其推算邏輯。

## 8. Mock 資料鏈同步

- [ ] 8.1 更新 `(prototype)/MOCK-DATA-CHAIN.md`：印件層兩欄改名（預計出貨日、內部完成日）；訂單層 `deadline` 與內部製作截止日兩欄自各鏈移除；需求單單頭交期欄移除；補一段說明「本鏈無任何上層交期欄，印件預計出貨日逐件手填」。
- [ ] 8.2 依 8.1 改 `quote-prototype/mock-data.js`、`orders/mock-data.js`、`work-orders/_lib/mock-data.js`、`print-items/_lib/mock-data.js`、`production-overview` 相關 mock。
- [ ] 8.3 於鏈中預置一筆超期樣本：某工單內部完成日早於旗下一筆生產任務的預計完成日，供情境 2.17 取用；另預置一張內部完成日為空的工單供情境 2.19 取用。
- [ ] 8.4 執行 `npm run test:smoke`，確認主流程在改名後不斷。

## 9. 測試影響清單（Sens `erp-prototype-tests`）

> 依 `docs/scenario-catalog.md` 節號列出改動範圍；改哪章跑哪章，提交前跑全套。

**既有情境需修改**：

| 節號 | 情境 | 修改內容 |
|------|------|---------|
| 1.1 | 業務建需求單、逐筆填印件項目後送印務評估 | 欄名改「預計出貨日」；移除單頭交期欄的填寫步驟 |
| 1.6 | 從既有需求單複製建單 | 補斷言：印件項目預計出貨日照原值帶入 |
| 1.10 | 單頭訂單交期預填至印件項目、業務逐列可改 | 整條改寫為「無單頭交期欄、印件項目逐列手填、新增列空白」 |
| 1.11 | 成交轉訂單印件項目訂單交期分層帶入、空值不取單頭補 | 改寫為「逐件帶入、空值即為空」，移除單頭層斷言 |
| 2.4 | 比價期間直接改印件單價、重出報價單 | 「數量必填、訂單交期選填」改為「數量必填、預計出貨日選填且無預設值」 |
| 4.5 | 業務在印件上選急件選項，系統推出印件層預計交期 | 欄名改「預計出貨日／內部完成日」，算式與錨例數值不變 |
| 4.6 | 改急件選項或印件訂單交期只留痕與同步，不通知 | 欄名改名；補「清空預計出貨日時工單內部完成日同步為空」斷言 |
| 4.7 | 印件訂單交期各自獨立，訂單訂單交期編輯不觸發印件同步 | 改寫為「訂單資訊 Section 不出現三個已刪欄位、審稿前後預計出貨日仍可編輯」 |
| 4.8 | 新增印件收齊七項必填、訂單交期選填並預設帶入訂單訂單交期 | 改為「預計出貨日無預設值、空白手填」 |
| 4.9 | 複製加開印件購買數量與訂單交期留空、規格側欄位帶入 | 欄名改名；補「不帶入來源印件日期」斷言 |
| 4.10 | 印件已棄用後訂單交期唯讀；訂單完成後印件訂單交期唯讀 | 欄名改「預計出貨日」 |
| 4.11 | 內部製作截止日與印件預計交期不設比對關係 | **刪除**（欄位已刪，無對象可測），目錄同步移除並於 § 不列入 記原因 |
| 5.13 | 待審清單依印件預計交期排序、空值排最後 | 排序鍵欄名改「內部完成日」 |
| 5.14 | 待審訂單模組母列顯示訂單交期、子列訂單交期與預計交期並列 | 母列改「取旗下未收斂印件最早的預計出貨日、皆無值顯示－」；子列改「預計出貨日與內部完成日並列」 |
| 7.4 | 同一個日期在三個頁面叫同一個名字 | 三頁欄名改「內部完成日」；補「不出現預計交期或訂單交期字樣」斷言 |
| 8.6 | 印務列印紙本工單，版式與欄位範圍固定且不含價格 | 表頭欄名改「內部完成日」 |
| 8.8 | 急件工單的紙本標示與交期 | 表頭欄名改「內部完成日」，維持無值印「－」 |
| 9.7 | 工廠總覽待排區顯示印件預計交期 | 欄名改「內部完成日」；補「無建議開工日欄位」與「為空顯示－」斷言 |
| 15.7 | 印件與工單預計交期一致；訂單訂單交期與印件預計交期允許不同 | 改為「印件與工單的內部完成日一致」，移除訂單層交期對照斷言 |

**新增情境**：

| 章節 | 新增情境 | 對應規格差異檔 Scenario |
|------|---------|---------------------|
| 一、需求單 | 複製需求單時印件項目預計出貨日照原值帶入 | quote-request § 印件項目管理 |
| 四、訂單成立與印件 | 訂單列表不顯示交期欄、篩選面板無交期起訖 | order-management § 訂單列表權限與可見範圍 |
| 五、審稿 | 打樣 NG-稿件問題複製新印件保留預計出貨日並重推內部完成日 | prepress-review § 打樣後棄用原印件建新印件 |
| 七、印務登打製程 | 生產任務預計完成日晚於工單內部完成日時提示、不擋存檔 | work-order § 生產任務預計完成日超期提示 |
| 七、印務登打製程 | 超期提示的排除對象（終態任務、售後補做不判定；品檢缺口補做照常判定） | 同上 |
| 七、印務登打製程 | 工單內部完成日為空時不比對、詳情顯示無交期基準 | 同上 |
| 八、製程審核與交付產線 | 終態工單不因後續交期變動回溯標超期 | 同上 |

**Mock 異動範圍**：`MOCK-DATA-CHAIN.md`（先改）→ `quote-prototype/mock-data.js`、`orders/mock-data.js`、`work-orders/_lib/mock-data.js`、`print-items/_lib/mock-data.js`（後改）。改名欄位：印件層兩個交期欄；移除欄位：訂單層交期與內部製作截止日、需求單單頭交期；新增樣本：一張超期工單、一張內部完成日為空的工單。

**測試執行順序**：`npm run impact` 對映改動 → `npm run test:smoke` → 改動章節（一、二、四、五、七、八、九、十五）→ 提交前 `npm test` 跑全套。

## 10. 交付與另案

- [ ] 10.1 erp repo 開分支施工、提交前跑全套測試，commit 訊息附測試結果（Vitest 與 Playwright 通過數）。
- [ ] 10.2 記錄後端另案清單供 Linear：訂單 `deadline` 與內部製作截止日兩欄停用、需求單單頭交期欄停用、印件兩個交期欄改名的欄位對映。
