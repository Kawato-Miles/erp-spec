# 段 4B 商業層設計：審稿段重寫（2026-08-17）

> 依 `plan-design` 模板產出。上游：grill 拍板 `production-stage-seg4-grill.md` 條 1-4、7-10（審稿段基準與範圍）、審稿段三方差異矩陣 #43-100（`production-stage-alignment-diff-matrix.md` L1005 起）、4A verify 歸 4B 殘留。
> 性質：**openspec prepress-review 整檔棄用重寫**（拍板 1：48 項債不逐項修）＋order-management／business-scenarios 審稿段修正＋prototype（erp repo）線下單必要流程衝突修正。基準＝wiki（拍板 2），wiki 與 prototype 衝突屬線下單必要流程者修 prototype、範圍外者不補。
> 舊 repo sens-erp-prototype 已全面棄用——差異矩陣中「舊 repo」欄位僅供參照，不修不搬。

## 〇、變動性質分級

**局部變動偏大（單領域重寫）**：不新增實體、不動跨模組關聯、不新增角色；狀態機不變（審稿八態與轉換照 wiki 現行）。重寫是承載形式的重建，商業規則全部來自既有 wiki 與已封存拍板（AR／PI 系列）。稽核形態＝單一稽核者完整 rubric（不加對抗推演——規則無新設計，皆為既有拍板的重申）。

## 本體

### 一、prepress-review spec 重寫骨架（棄用重寫）

重寫後的 spec 只承載**線下單必要流程**的行為規格，結構如下（每節標注規則正本）：

| 節 | 承載 | 正本 | 收斂的差異項 |
|----|------|------|-------------|
| 分派與換人 | 單一「分派審稿人員」功能（首次與換人同一支，依印件當下有無負責人分支）；候選三要件（啟用中＋能力等級有值＋能力 ≥ 難易度）僅作用於線上單自動分派；線下單一律訂單管理人／審稿主管手動、候選清單不過濾（能力僅參考標示）；混合批次分派；候選為空的處置（措辭「全員停用或無人可選」）；換人不改狀態、未完成輪次移交、留痕原因選填 | [[審稿分配規則]]、[[覆寫審稿分派]] | #43、#44、#46、#51、#52、#53、#54、#56 |
| **刪除：「在崗／不在崗」全部條文**（拍板 8：請假屬系統外人工判斷，不設欄位）——覆寫待辦清單、可用狀態判定整段刪 | [[人員]]（只有是否啟用）、[[覆寫審稿分派]] L22 | #45 |
| 待審清單排序 | 預設依交期近的先審（AR-5）；急單沿用訂單「是否急件」欄不另設標記；不設待分派停滯提醒、補件不設輪次上限與停滯提醒（AR-11／AR-17，明文 SHALL NOT 防回歸，依據＝已封存拍板） | [[印件審稿]] L107、L94、[[審稿分配規則]] L86 | #48、#49 |
| 稿件上傳 | 「稿件上傳開放」欄的行為承載（開放才可上傳，補 Requirement）；角色分流（B2B 業務代傳、B2C 會員自傳）；線下單上傳 MUST NOT 觸發自動分派；印件檔可暫 `round_id=null` 待綁（刪內部相斥的 641／667 兩句） | [[印件]] L66、[[印件審稿]] L32、[[稿件管理規則]]、[[審稿輪次]] L57 | #58、#61、#47、#59 |
| 審稿輪次與判定 | 輪次建立時點＝**上傳新稿即建下一輪**（刪「再次送出審核建輪」相斥句）；退件原因 10 項、不合格必填；**技術性退件不排除退件率**（AR-6——刪整個排除 Scenario）；審稿備註送出後可改＋稽核留痕；三種備註分層；批次審稿六項（含共用備註逐件覆寫）；合格落點（審稿後檔案＋縮圖＋當前合格輪次指針）；工單建立鎖檔不鎖備註 | [[審稿輪次]]、[[印件審稿]]、[[稿件管理規則]] | #60、#62、#63、#64、#65、#66、#67、#70 |
| 合格之後 | 業務確認可製作（B2B 手動／B2C 自動）；退回重審（限合格、退回原因**選填**、不建輪次、**合格非終態**——死引用 1174 刪）；審稿八態列舉改引 wiki 不複寫（消 #77 複寫違規） | [[印件狀態]]、[[印件審稿]] L92 | #71-76、#77 |
| 免審路徑 | 免審直通：**印件建立時**系統自動建 `source=免審稿` 合格輪次（拍板 9——含訂單成立時建的印件與訂單期間追加的印件）、直達合格、不進待審清單；免審輪次**不產生審稿後檔案與縮圖**（例外句補上，消 #68 無條件約束）；B2B 免審仍須業務手動確認可製作（#79 分流句統一）；免審為可編輯欄位次一輪生效（狀態同步表補「該輪免審 → 系統自動判合格」列）；免審印件退回重審後下一輪依該輪免審值決定、線下單不隨上傳重跑分派（刪 1521-1526 相斥句） | [[免審決策樹]]、[[審稿輪次]] L52、[[印件狀態]] | #78、#68、#79、#80、#81 |
| Slack 討論串 | 沿現行 Requirement（#55 對齊），mention 對象＝該訂單的訂單管理人（欄位取值非硬寫） | [[審稿討論Slack串接約束]] | #55 |
| 與打樣的銜接（段 3 四 Requirement 修正保留） | clone reset 清單修正：審稿維度初值「待分派」、負責審稿人員＝空（與待分派語意一致，補進 reset 清單）、印製維度「等待中」、`warehouseQty` 刪除（完工良品數歸零表達）、值域對齊印製九態；NG-稿件前提補「原打樣印件審稿維度已到已確認可製作」；clone 後走**線下單手動分派**（刪自動分派句）；打樣週期歸零字面釐清——「舊輪樣品實體報廢」指物理處置、帳上不記報廢數（L1121 既有句保留、與計算界線實作語意一致，4A verify 殘留在此結案）；reviewRound.source enum 刪「售後補印」值（統一案廢型別，4A 歸 4B 項） | [[打樣後稿件問題重審]]、[[印件狀態]]、[[印件]] | #82-#87、#88（spec 側已完整）、#89 |
| 判定者措辭 | 打樣結果判定者對齊 wiki「業務／諮詢」——**wiki 為準**，spec 刪「業務主管」；prototype 同步收窄 | [[印件]] L67 | #89 |
| **刪除：主管工作台／KPI 全部 Requirement**（拍板 3：dashboard 棄用刪除）——審稿總覽 Summary 卡、審稿關卡逾期指標（AR-14 拍板不做）、待派工指標（前提與難易度必填相斥）、能力等級維護 Requirement（wiki 卡保留標實作延後、spec 不留）、KPI 儀表板全節 | 拍板 3、AR-14 | #96、#97、#43、#50 |
| OQ 與引用勘誤 | PI-004→AR-10、PI-009 不存在值改引審稿輪次卡、三個封存 OQ 引用路徑改 `_archives/2026/`、刪舊 repo 原始碼行號引用 | OQ 規約 | #98 |
| 術語 | bubble-up 全檔清為「狀態向上傳遞」；「待分派」首次出現處括號註明「審稿維度，與工單分派待辦同名不同義」 | CLAUDE.md 術語表 | #95（prepress 側）、#100 |

### 二、order-management 審稿段修正（MODIFIED，非重寫）

1. **訂單層派生規則補「待分派」分支（#90，拍板 7）**：待分派＋稿已上傳 → 併入「等待審稿」（對業務而言＝稿交出去了等內部處理）；待分派＋稿未上傳 → 併入「稿件未上傳」。派生五規則改寫為涵蓋全八態的完備規則。
2. **免審快速路徑落點條件（#91）**：「所有印件合格或免審稿 → 製作等待中」改為 wiki 口徑「全部印件已確認可製作 → 製作等待中」（B2B 免審印件在合格時仍須業務確認）。
3. **bubble-up 用詞 6 處**（#95）：含 Requirement 標題「訂單審稿段 Bubble-up 派生」改「訂單審稿段狀態向上傳遞」。
4. 成品縮圖獨立欄位句（#69）：改為衍生顯示（取當前合格輪次縮圖），刪「儲存縮圖並更新印件的成品縮圖網址」。

### 三、business-scenarios 審稿相關修正（MODIFIED）

- #75：L292「審稿維度合格為終態…不回退」改為現行口徑（合格→待改稿退回重審為現行轉換，與棄用重建分工見 [[印件狀態]] 動機段）。
- #82／#86：打樣情境表格的審稿初值與「製程已確認」站（4A 已修部分，重寫時逐格查證補齊）。
- #99：QC 退役措辭三處（「QC 通過」→「品檢通過」、流程列舉去 QC）。
- #45：「在崗」判定條件句隨在崗欄刪除改寫。

### 四、prototype 修正清單（erp repo，線下單必要流程）

| # | 修正 | 差異項 |
|---|------|--------|
| P1 | 線下單上傳稿件不觸發自動分派（`uploadArtwork` 補 `order_source` 判斷） | #47 |
| P2 | 免審輪次補建：印件建立（含加開）時免審者自動建 `source=免審稿` 合格輪次（不產生審稿後檔案與縮圖）；`confirmSignBack` 的 `allSkip` 直達句改為 wiki 口徑（B2B 免審合格後仍等業務確認可製作） | #78、#79 |
| P3 | 訂單層審稿狀態歸納：實作派生規則（含待分派分支），掛在審稿事件後重算；「待補件」可被寫入；「等待審稿→製作等待中」隨歸納重算；製作段不退回、退回重審以警示標記呈現 | #90、#92、#93、#94 |
| P4 | 退回重審動作（合格→待改稿、原因選填、不建輪次、寫印件事件與訂單活動紀錄）——新 repo 缺、依 wiki 實作（不參照舊 repo） | #72 |
| P5 | Slack mention 對象改取該訂單的訂單管理人欄位（去硬寫人名） | #55 |
| P6 | 審稿主管分派權限修正（`pending-assign` 頁 403 改放行，與 selector 一致） | #56 |
| P7 | 待審清單排序改「交期近的先審」（交期已算出，改 sort 鍵） | #48 |
| P8 | 逾期指標移除（`isOrderOverdueInReview` 與相關顯示——AR-14 不做） | #96 |
| P9 | 上傳門控補齊：「稿件上傳開放」旗標＋角色分流（B2B 業務代傳）；候選為空的提示 | #58、#61、#51 |
| P10 | 審稿備註送出後修改（原審稿人員限定＋稽核留痕）；批次審稿補「共用備註＋逐件覆寫」 | #64、#66 |
| P11 | 打樣結果判定者收窄為業務／諮詢（去業務主管） | #89 |
| P12 | 審稿人員候選判準補「能力等級有值」（空值不進線上單自動分派候選；線下手動候選照舊不過濾）；「在崗」過濾刪除 | #43、#44、#45 |
| P13 | NG-稿件判定門控補審稿維度前提（已確認可製作） | #85 |

不修（範圍外，拍板 2）：主管工作台／KPI／能力等級維護介面（新 repo 本來就沒有，維持沒有）；#57 mock 身分對映（mock 層瑕疵，列 ACCEPTANCE 已知限制）；#54 註記的跨訂單批次分派（wiki 只寫同訂單——收窄至同訂單，輕修併 P9）。

### 五、wiki 落卡清單（grill 拍板 7-10 的機械修 6 項＋本設計新增 2 項）

1. 拍板 9：[[印件審稿]] L84 與 [[免審決策樹]] L25 的免審輪次時點「訂單成立時」改「印件建立時」（對齊 [[審稿輪次]] L36）。
2. 拍板 10 六項：AR-6 措辭（[[印件審稿]] L108 範圍外句）、AR-10 封存卡取代註記（候選過濾已由 2026-07-03 拍板推翻）、[[打樣流程]]／[[打樣決策與重新打樣]] 自動推進句（4A 落卡批三已做——查證後跳過）、[[印件狀態]] mermaid 免審自動弧、「能力等級上限」正名（[[審稿主管]] L28）、[[人員]] L39 實作進度資訊移出欄位表。
3. 拍板 7：[[訂單狀態]] 歸納句補一句（待分派＋稿已上傳併等待審稿）。
4. 拍板 8：[[訂單管理人]]／[[審稿主管]] 換人情境的「請假」註明系統外人工判斷。

### 六、總覽差異

**無實體、無關聯變動**。審稿輪次結構不變；印件「稿件上傳開放」欄為 wiki 既有欄（L66）的下游補承載，非新欄。

---

## 必含段① 邊界完整性表

| 實體 | 建立 | 修改 | 作廢 | 查詢 |
|------|------|------|------|------|
| 審稿輪次 | 上傳新稿即建下一輪（唯一時點）；免審＝印件建立時系統自動建合格輪 | 審稿備註送出後可改（限原審稿人員、稽核留痕）；送審／審稿判定寫入本輪 | 不適用（輪次無作廢，正本無此概念） | 依印件聚合顯示（既有） |
| 印件（審稿維度相關欄） | 審稿維度初值待分派（含 NG-稿件 clone）；免審者建立當下直達合格 | 免審欄可編輯次一輪生效；稿件上傳開放欄控上傳 | 不適用（隨印件生命週期） | 待審清單依交期排序；已棄用印件排除（4A 已修） |
| 人員（能力等級欄） | 不適用（欄位既有） | 維護介面實作延後（拍板 3）——能力等級綁 mock；空值＝不進線上單自動分派候選 | 不適用 | 分派對話框參考標示 |
| 訂單（審稿段） | 不適用 | 派生規則涵蓋全八態（含待分派分支）自動歸納 | 不適用 | 審稿段三值＋警示標記 |

| 狀態 | 進入路徑 | 離開路徑 |
|------|---------|---------|
| 審稿八態 | 照 [[印件狀態]] 現行十條轉換，本設計零新增零刪除 | 同左 |
| 「待分派」 | 印件建立（含 clone、加開）初值 | 分派→稿件未上傳或等待審稿（依稿件有無）；免審→系統自動合格 |
| 「合格」 | 審稿人員判定／免審系統自動 | 確認可製作（B2B 手動／B2C 自動）；退回重審→待改稿（**非終態**） |
| 訂單審稿段三值 | 派生規則自動歸納（全八態有落點） | 等待審稿→製作等待中（全部已確認可製作） |

「不適用」理由已逐格附註。

## 必含段② 管理指標段

**本案不新增指標**，並依已封存拍板**刪除三個既有 spec 指標**：審稿關卡逾期訂單數（AR-14 拍板不做）、待派工指標（前提與難易度必填相斥）、技術性退件排除後的不合格率（AR-6 拍板不排除——退件率若未來要做，分母含全部不合格輪次，reject_reason_category 只看分布）。既有指標不受影響（審稿段無其他指標掛勾）。

## 必含段③ 情境四分類表

| 分類 | 情境 | 吸收機制／協作三要素 |
|------|------|---------------------|
| 主情境・系統內 | 線下單審稿全流程（訂單管理人手動分派→業務代傳稿→審稿判定→補件迴圈→合格→業務確認可製作）；換人（含混合批次）；批次審稿；退回重審；免審直通（含訂單期間追加免審印件）；打樣 NG-稿件重審銜接 | 全程系統內走完 |
| 邊界・系統內 | 審稿中途改免審→次一輪系統自動合格；免審印件退回重審→下一輪依當下免審值；候選為空→提示且換人不成立；技術性退件→計入退件分布不排除 | 既有機制 |
| 邊界・系統外 | ① 請假換人：訂單管理人／審稿主管**自行判斷**改派（候選不過濾、清單上仍看得到請假者），結果以分派留痕回系統（拍板 8）；② 審稿爭議溝通：Slack 討論串（ERP 發起、連結留存）；③ B2C 會員上傳：電商會員中心（外部系統）上傳、稿件回 ERP 輪次 | 三要素如左 |
| 不支援 | 待分派停滯提醒、補件輪次上限與停滯提醒（AR-11／AR-17 拍板不設）；審稿專屬急單標記（AR-5 沿用訂單欄）；主管工作台／KPI（拍板 3 棄用）；能力等級維護介面（實作延後、user 功能）；「在崗」狀態（拍板 8 不設欄位） | 均有拍板依據 |

## 必含段④ grill 拍板紀錄段

正本：`production-stage-seg4-grill.md`。4B 承接條目：

| # | 問題 | 拍板 | 理由 |
|---|------|------|------|
| 1 | 審稿段處置方式 | openspec 棄用重寫、範圍收斂線下單必要流程 | 48 項債逐項修不如重寫 |
| 2 | 重寫基準 | wiki 為準；衝突屬必要流程者修 prototype，範圍外不補；wiki 過時條文停下呈報 | 全工程鐵則 |
| 3 | dashboard | 棄用刪除；wiki 卡標實作延後不刪卡 | dashboard 屬後驗 epic |
| 4 | 難易度 | 設定綁 mock（user 功能）；線下手動分配不用演算法 | 線上單才用自動分派 |
| 5 | 舊 repo | sens-erp-prototype 全面棄用 | 全部轉 erp repo |
| 7 | 三值空窗 | 待分派＋稿已上傳併入「等待審稿」 | 不加新值沿用列舉 |
| 8 | 請假換人 | 不設欄位、刪自造「在崗」欄 | 人工判斷不進系統 |
| 9 | 免審輪次時點 | 印件建立時（通例涵蓋特例） | 追加印件即刻有輪 |
| 10 | wiki 機械修 6 項 | 照修 | — |

**隱含假設清單**（設計者自補）：
1. 「能力等級有值」要件只作用於線上單自動分派候選（線下手動候選不過濾＝wiki 既有）——AR-9 拍板語境自補。
2. clone reset 清單補「負責審稿人員＝空」——「待分派」的判準（無負責人）反推，wiki 未逐欄明列。
3. #54 跨訂單批次分派收窄至同訂單——wiki [[審稿分配規則]] L51 只寫同訂單，prototype 多做的部分收回。
4. 重寫後的 prepress-review spec 保留「與打樣的銜接」四 Requirement（段 3 新增、4A 對齊過）的架構位置，僅修錯值與相斥句——不整節重造。
5. 「稿件上傳開放」欄的預設值＝依訂單流程開放時點（打樣 OK 後由業務開放大貨上傳等既有敘述），spec 只承載「開放才可上傳」行為、開放時點的操作歸業務判斷（PT-025 不設守衛的精神）。

## 必含段⑤ 守恆段

審稿段不涉數量／金額流動。本設計涉及的「量」：

| 量 | 流動等式 | 雙向機制 |
|----|---------|---------|
| 審稿輪次數 | 輪次數 ＝ 上傳新稿次數 ＋ 免審自動輪（0 或 1）；退回重審不建輪 | 上升：上傳新稿、免審建立；無下降（輪次只增不刪，歷史保留） |
| 待審工作量 | 待審清單 ＝ 等待審稿＋已補件的印件（依交期排序） | 上升：送審、補件；下降：判定（合格／不合格）、換人不變量 |
| 訂單審稿段落點 | 每張審稿段訂單恰有一個派生落點（三值之一）——派生規則涵蓋全八態即無懸空 | 派生為全函數（#90 修正後），無進出等式缺口 |

---

## 業界範式對照與否決方案

局部變動不強制（標準 5-4 限結構性），仍附一項：**重寫而非逐項修**——對照文件工程的「重寫閾值」慣例（缺陷密度過高時 rewrite 優於 patch：48 項差異中 21 項 openspec 衝突含多處內部相斥與死引用，patch 會留下修補痕跡交錯的規格）。具名否決方案：逐項 MODIFIED（48 個 delta 操作）——否決理由：內部相斥（#46、#59、#60、#75、#79、#82）意味同一份 spec 兩側都要動，逐項修的 delta 數量與重寫相當但可讀性更差（Miles 拍板 1 已定）。

## 待 OQ 項

無——所有行為語意均有 wiki 現行卡或已封存拍板（AR-5／6／9／10／11／14／17、PI-006、拍板 1-10）依據。
